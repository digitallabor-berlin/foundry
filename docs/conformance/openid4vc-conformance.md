# OpenID4VC Conformance Report

**Status:** complete — audit finished 2026-07-31
**Scope:** `foundry-issuer`, `foundry-verifier`, and the protocol HTTP routes in `crates/foundry/src/server.rs`

This is a **living document**. It is not a snapshot of one audit run: later work
that closes a gap edits the affected rows in place. Do not duplicate its
contents into a changelog or a run artifact — link to it instead.

Its internal consistency is enforced mechanically by
`crates/foundry/tests/conformance_report.rs`, which runs as part of
`cargo test --workspace`. Edits that break the cross-references below will fail
that test.

## Specifications Under Audit

The authoritative texts are the pinned copies in [`docs/specs/`](../specs/), per
[`AGENTS.md`](../../AGENTS.md) §4.4 — not any newer draft published elsewhere.

| Short name | File | Pinned version |
|---|---|---|
| OpenID4VCI | [`openid-4-verifiable-credential-issuance-1_0.md`](../specs/openid-4-verifiable-credential-issuance-1_0.md) | `openid-4-verifiable-credential-issuance-1_0-17` |
| OpenID4VP | [`openid-4-verifiable-presentations-1_0.md`](../specs/openid-4-verifiable-presentations-1_0.md) | `openid-4-verifiable-presentations-1_0-30` |
| HAIP | [`openid4vc-high-assurance-interoperability-profile-1_0.md`](../specs/openid4vc-high-assurance-interoperability-profile-1_0.md) | `openid4vc-high-assurance-interoperability-profile-1_0-06` |

Where HAIP is stricter than OpenID4VCI or OpenID4VP, **HAIP wins**.

## Audit Boundary

**In scope**

- `foundry-issuer`, all modules.
- `foundry-verifier`, all modules.
- The protocol routes in `crates/foundry/src/server.rs`: `/token`, `/authorize`,
  `/nonce`, `/credential`, `/vp/request/:id`, `/vp/response/:id`,
  `/statuslists/:id`, and the `.well-known` metadata routes.

**Clause selection.** Mandatory clauses only — MUST, MUST NOT, REQUIRED, SHALL,
SHALL NOT — over features foundry implements. Per `AGENTS.md` §4.4,
unimplemented *optional* features are acceptable and are recorded as
`not-implemented`. SHOULD and RECOMMENDED clauses may carry a verdict but are
not systematically inventoried.

**Out of scope**, recorded explicitly so that silence is never mistaken for a
pass:

| Area | Reason |
|---|---|
| SD-JWT VC format internals (disclosure encoding, KB-JWT structure) | Defining spec (IETF SD-JWT VC) not vendored under §4.4 |
| mdoc format internals (CBOR structure, MSO layout) | Defining spec (ISO/IEC 18013-5) not vendored and not vendorable — paid standard |
| Token Status List bitstring encoding | Defining spec not vendored |
| Wallet-side and third-party obligations | Recorded with `Applies to = wallet` / `other` and verdict `out-of-scope` |
| W3C Verifiable Credential format profiles (`jwt_vc_json`, `ldp_vc`, `jwt_vc_json-ld`) | foundry issues only `dc+sd-jwt` and `mso_mdoc`; excluded 2026-07-31 by scope decision |
| `di_vp` proof type | foundry implements only the `jwt` proof type; excluded 2026-07-31 by scope decision |

What *is* in scope for the credential formats is what the three vendored specs
say about their **usage**: which formats must be supported, required algorithms,
key binding requirements, and the profile's constraints on `vct` and doctype
handling. That a status check happens and is honoured is in scope; whether the
bitset is decoded correctly is not.

**Presentation-binding clauses are attributed to the `verifier`.** A few clauses
constrain the *content* of a presentation without naming the actor — the Key
Binding JWT `nonce` and `aud` values (OpenID4VP, *IETF SD-JWT VC / Presentation
Response*), and the mdoc `SessionTranscript` / `OpenID4VPHandover` structure
(*Mobile Documents or mdocs / Invocation via Redirects*). A Wallet writes
those values and a Verifier recomputes and compares them, so they are recorded
with `Applies to = verifier`: it is foundry's *verification* of the binding that
is testable here. Clauses that only govern how a response is *constructed* (body
encoding, `vp_token` assembly, JWE header selection) remain `wallet`.

## Legend — Verdicts

| Verdict | Meaning |
|---|---|
| `conforming` | Implemented and correct; `Evidence` cites code, `Test` cites the proving test |
| `gap` | Implemented incorrectly, or mandatory and absent; has a row in the gap register |
| `not-implemented` | Optional feature foundry does not offer; permitted by `AGENTS.md` §4.4. Rationale required |
| `not-unit-testable` | Transport, deployment, or operational requirement. Rationale required |
| `out-of-scope` | Outside the audit boundary above. Rationale required |
| `ambiguous` | Examined, but genuinely readable two ways. Terminal — makes no conformance claim, does not block completion. Listed under Unresolved Ambiguities |
| `unverified` | Not yet adjudicated. The remaining-work marker; must be zero when the audit is complete |

## Legend — Severity

| Severity | Meaning |
|---|---|
| `Critical` | Accepts something it must reject — a forged, replayed, or unauthorized credential or presentation |
| `Important` | A conformant counterparty fails to interoperate |
| `Minor` | No functional consequence — wording, ordering, or a redundant field |

## Identifiers

Clause identifiers are `VCI-NNNN`, `VP-NNNN`, `HAIP-NNNN`, zero-padded to four
digits, sequential in document order within each spec. Gap identifiers are
`GAP-VCI-NN`, `GAP-VP-NN`, `GAP-HAIP-NN`, `GAP-HTTP-NN`.

**Identifiers are never renumbered.** They are cited by `#[ignore]` reason
strings, commit messages, and follow-up work.

**Clauses discovered after initial extraction append at the end of their
spec's inventory rather than being inserted in spec-line order.** VCI-0231
and VCI-0232 (added 2026-08-01 while closing GAP-HAIP-04) are the first
instance: both cite Wallet Attestation text from earlier in the OpenID4VCI
document (L2555, L2600) but sit after VCI-0230 in the table, since renumbering
the existing sequence to preserve document order would violate the rule
above.

## Summary

| Spec | Total | conforming | gap | not-implemented | not-unit-testable | out-of-scope | ambiguous | unverified |
|---|---|---|---|---|---|---|---|---|
| OpenID4VCI | 232 | 76 | 18 | 78 | 6 | 54 | 0 | 0 |
| OpenID4VP | 266 | 89 | 9 | 53 | 8 | 107 | 0 | 0 |
| HAIP | 96 | 49 | 6 | 10 | 7 | 20 | 4 | 0 |

## Gap Register

| ID | Severity | Spec § | Requirement | Impact | Test |
|---|---|---|---|---|---|
| GAP-HAIP-01 | Important | OpenID4VCI (L199) | For Grant Type `authorization_code` the Issuer MUST include a scope value so the Wallet can identify the desired Credential Type | foundry's `authorization_code` flow identifies the Credential Type solely via the opaque `issuer_state` bound to the transaction at `create_offer` time; `CredentialConfigurationSupported` (metadata.rs) carries no `scope` field and `AuthorizeParams` (authorize.rs) has no `scope` parameter. A HAIP-conformant Wallet expecting to learn the Credential Type via `scope` receives none. Also cited by HAIP-0027/HAIP-0028, which restate the same mandate under "Authorization Endpoint". | haip_0023_credential_configuration_metadata_carries_a_scope_value |
| GAP-HAIP-03 | Important | OpenID4VCI (L163) | MUST support DPoP per RFC9449 for sender-constrained access tokens | `mint_and_save_tokens` (token.rs) always sets `token_type: "Bearer"`; there is no `DPoP` proof-header verification anywhere in the workspace and `TokenRequest`/`TokenResponse` (token.rs) carry no DPoP-related fields. Access tokens are bearer-only, not sender-constrained. | haip_0009_token_response_uses_dpop_token_type |
| GAP-VP-04 | Important | Response / VP Token Validation (L1523); Format / IETF SD-JWT VC / Transaction Data (L3144) | Verifiers MUST check that the set of Presentations returned satisfies all requirements of the Verifier's request, which for a request that included `transaction_data` means the presentation's `transaction_data_hashes` binding | Nothing in this workspace ever reads, computes, or checks a `transaction_data_hashes` claim: `attach_kb_jwt` (`foundry-sd-jwt-vc`'s builder, the only KB-JWT construction path this codebase has) has no parameter for it at all, and `do_verify_vp_response`/`verify_sd_jwt_vc` never look for one either. A Verifier that requests `transaction_data` (VP-0019/VP-0020, conforming -- the request-side plumbing works) has no way to confirm the wallet actually bound its presentation to that specific transaction; a presentation with no transaction binding at all verifies successfully exactly as if the binding had been checked and passed. This defeats the security purpose the spec's own text gives for the feature: "a binding between the user's identification/authentication and the user's authorization, for example to complete a payment transaction, or to sign specific document(s) using QES." Confined to deployments that actually opt into `transaction_data` (the field is not populated by default), so rated Important rather than Critical. | gap_vp_04_transaction_data_hashes_never_validated |
| GAP-VCI-05 | Minor | Key Attestation JWT (L2503); jwt Proof Type (L2639) | `iat` is REQUIRED in both the Key Attestation JWT payload and the jwt-proof-type payload | Neither `verify_key_attestation_jwt` nor `verify_holder_proof` (both in `crates/foundry-issuer/src/`, attestation.rs and proof.rs respectively) ever reads or validates an `iat` claim; both accept a JWT that omits it entirely. No functional consequence follows: freshness and replay are independently enforced via the `c_nonce` MAC and TTL (`nonce.rs`), which is the alternative mechanism Verifying Proof (L2777) explicitly allows for establishing the creation-time window (see VCI-0230, `conforming`). | vci_0199_0209_0224_proof_jwt_without_iat_is_rejected |
| GAP-VCI-06 | Minor | jwt Proof Type (L2637) | `iss` MUST be the Client's `client_id`, and MUST be omitted when the access token came from anonymous pre-authorized_code access | `verify_holder_proof` (proof.rs) never reads an `iss` claim at all, and no transaction field records which grant/access mode produced the access token (`IssuanceTransaction`, transaction.rs, has no `client_id` or grant-type field), so an `iss` value of any shape — present or absent, correct or spoofed — is accepted unconditionally. No functional consequence follows: `iss` is not consulted by any authorization decision at the Credential Endpoint today. | vci_0207_proof_iss_must_be_omitted_after_anonymous_pre_auth_access |
| GAP-VCI-07 | Minor | Credential Issuer Metadata (L1394, L1395) | `cryptographic_binding_methods_supported` MUST be present when Cryptographic Key Binding is required and omitted otherwise, and `proof_types_supported` MUST be present if `cryptographic_binding_methods_supported` is present and omitted otherwise | `build_issuer_metadata` (metadata.rs) serializes `cryptographic_binding_methods_supported` and `proof_types_supported` unconditionally for every Credential Configuration — neither field carries a `skip_serializing_if` annotation, and `proof_types_supported` is hardcoded to always include a `jwt` entry regardless of `ct.cryptographic_holder_binding`. Every credential type shipped in this repository hardcodes `cryptographic_holder_binding: true` (create_offer.rs, credential.rs, metadata.rs's own tests), so this path is currently unreachable in practice, but the metadata generator itself would misrepresent capability for the first credential type configured with key binding not required. | vci_0146_0147_metadata_omits_binding_and_proof_fields_when_key_binding_not_required |
| GAP-VCI-10 | Minor | Credential Issuer Metadata (L1402, L1403, L1405, L1409, L1410); Claims Description / Issuer Metadata (L2338, L2349) | Credential display objects MUST carry a required `name`, at most one object per language identifier, a required logo `uri`, and a required `background_image.uri`; claims description objects MUST NOT be repeated or contradictory | `ct.display` (foundry-core/src/config/model.rs) is untyped `Vec<serde_json::Value>`, passed straight through by `build_issuer_metadata` (metadata.rs) into `CredentialConfigurationSupported.display` with no structural validation anywhere — `Config::validate()` never inspects its contents. An operator can configure duplicate-locale display objects, or omit `name`/logo `uri`/`background_image.uri`, and the malformed metadata is served as-is. The same absence of structural validation extends to each `ClaimDef`'s own `display` array (same duplicate-locale exposure) and to a credential_type's `claims` array as a whole, which is never checked for two `ClaimDef`s repeating or contradicting the same `path`. | vci_0150_0151_0152_0153_0154_credential_display_objects_are_not_structurally_validated |
| GAP-VP-03 | Minor | DCQL / Credential Query (L743, L745, L756); DCQL / Claims Query (L780); Format / mdoc / `meta` (L2802, L2803); Format / IETF SD-JWT VC / `meta` (L3163, L3164) | A Credential Query `id` MUST be a non-empty string of alphanumeric, underscore or hyphen characters and MUST NOT repeat within one Authorization Request; `meta` is REQUIRED in a Credential Query, even if empty; Verifiers MUST NOT point to the same claim more than once in a single query; format-specific `meta` fields (`doctype_value` for mdoc, `vct_values` for SD-JWT VC) are REQUIRED and MUST be valid identifiers | None of these structural constraints is validated by `DcqlCredentialQuery`/`DcqlClaimsQuery` deserialization (dcql_model.rs): `id` is an unconstrained `String` with no charset or uniqueness check across `credentials`; `meta: Option<Value>` carries `#[serde(default)]`, so it may be entirely absent rather than merely an empty object; neither `credentials` nor a single Credential Query's `claims` array is checked for duplicate entries; and `credential_query_satisfied` (dcql.rs) reads `doctype_value`/`vct_values` out of `cq.meta()` only *if present*, silently skipping the corresponding match check when either is absent, with no validation of the string(s)' own registration validity when they are present. All are authored by the Verifier operator (via the `dcql_query` request field or a `named_query_ref` config entry), not by an external attacker, so the practical risk is confined to self-inflicted misconfiguration (e.g., two Credential Queries silently sharing an `id`, with `DcqlQuery::credentials().iter().find(..)`-style lookups always resolving to the first match, or an mdoc/SD-JWT-VC query silently matching any doctype/vct) rather than a wallet- or verifier-exploitable defect. | vp_0093_0094_0096_0097_dcql_structural_constraints_not_validated |
| GAP-VP-05 | Minor | HAIP OpenID4VP (L258) | Verifiers MUST list both `A128GCM` and `A256GCM` in `encrypted_response_enc_values_supported` in their client metadata | `response_encryption_params` (request.rs) resolves to exactly one `enc` value (the configured value, defaulting to `A128GCM`), and every transport advertises `"encrypted_response_enc_values_supported": [response_enc_method]` -- a single-element array, never both required values simultaneously. Decryption of an `A256GCM`-encrypted response works correctly when a wallet sends one regardless (see `haip_0049_0050_0053_ecdh_es_p256_and_a256gcm_supported`), so the practical impact is limited to a wallet that consults `client_metadata` alone and concludes `A256GCM` is unsupported when it is not. | haip_0052_encrypted_response_enc_values_supported_lists_only_one_value |
| GAP-VCI-11 | Minor | OpenID4VCI Credential Issuer Metadata (L1312, L1314) | The well-known metadata document MUST be served at the path formed by inserting `/.well-known/openid-credential-issuer` between the host and path components of the Credential Issuer Identifier | `wallet_router` (crates/foundry/src/server.rs) always registers the well-known route at the literal fixed path `/.well-known/openid-credential-issuer`, independent of `config.issuer.credential_issuer`. Per the spec's own worked example, a Credential Issuer Identifier of `https://issuer.example.com/tenant1` must serve metadata from `https://issuer.example.com/.well-known/openid-credential-issuer/tenant1` -- a URL foundry never registers a route for, so it 404s. Every credential_type fixture and deployment example in this repository configures `credential_issuer` as a bare origin with no path, so this is currently latent rather than commonly triggered, but nothing in `Config::validate()` prevents an operator from configuring a path-bearing identifier and getting an unreachable metadata endpoint as a result. | gap_vci_11_well_known_metadata_ignores_credential_issuer_path_component |
| GAP-VCI-12 | Minor | OpenID4VCI Format Profile / mdoc (L2235) | `doctype` is REQUIRED and identifies the Credential type per ISO 18013-5 | `handle_credential_request`'s (`crates/foundry-issuer/src/credential.rs`) mso_mdoc docType resolution falls back through `vct`, then `doctype`, then `tx.credential_type_id` -- it prefers `vct` over `doctype` whenever a credential_type config carries both, even though `vct` is an SD-JWT-VC-specific identifier (typically an HTTPS URL) with no relationship to ISO 18013-5's reverse-DNS `docType` convention. `Config::validate()` requires `doctype` to be *present* for `mso_mdoc` types but never forbids `vct` from also being set, so this dual-field state is entirely config-legal. Currently latent: every mso_mdoc credential_type fixture in this repository sets `vct: None`, so the fallback correctly reaches `doctype` in practice -- but nothing prevents an operator from configuring both, silently producing a non-ISO-18013-5-conformant docType. | gap_vci_12_mdoc_doc_type_prefers_vct_over_doctype_when_both_configured |
| GAP-VCI-13 | Minor | OpenID4VCI Claims Path Pointer (L2366); Claims Description / Issuer Metadata (L2323) | A claims path pointer MUST be a non-empty array of strings, nulls and integers | `ClaimDef.path` (`foundry-core/src/config/model.rs`) is typed `Vec<String>` -- it can never represent the `null` (address every element of an array) or non-negative-integer (address a specific array index) path segments the spec's claims path pointer grammar defines, restricting every claims description object in this workspace to plain object-key traversal. Separately, `Config::validate()` never checks that a configured `path` is non-empty, so `path: []` (a syntactically well-formed but semantically meaningless claims path pointer) passes validation unchanged. | gap_vci_13_claims_path_pointer_emptiness_and_shape_are_never_validated |

## Clause Inventory — OpenID4VCI

| ID | § | Requirement | Applies to | Verdict | Evidence | Test |
|---|---|---|---|---|---|---|
| VCI-0001 | Overview / Core Concepts (L181) | Credentials batched in one response MUST share the same Credential Format and Credential Dataset | issuer | `conforming` | `handle_credential_request` (credential.rs) resolves exactly one `cred_type`/`tx.claims` pair per issuance transaction and loops over `verified_proofs` reusing that identical pair for every credential in the batch -- there is no code path through which two credentials in one response could differ in format or dataset | haip_0011_multiple_proofs_yield_one_credential_per_proof |
| VCI-0002 | Overview / Core Concepts (L183) | To issue Credentials of differing Formats or Datasets, multiple requests MUST be sent to the Credential Endpoint | wallet | `out-of-scope` | Obligation falls on the Wallet or a third party, not on foundry's issuer/verifier surface |  |
| VCI-0003 | Pre-Authorized Code Flow (L354) | Implementers MUST implement mitigations suitable to the use case against pre-authorized code theft | issuer | `conforming` | `handle_pre_authorized_code_grant` (token.rs) now invalidates the `pre-authorized_code` via `invalidate_pre_authorized_code` immediately after `tx_code` validation passes, mirroring the `authorization_code` branch's existing single-use enforcement | vci_0012_pre_authorized_code_grant_rejects_replay_after_token_issuance |
| VCI-0004 | Credential Offer (L374) | `credential_offer` MUST NOT be present when `credential_offer_uri` is present | issuer | `conforming` | `build_offer_uri` (offer.rs) always emits exactly one `credential_offer=` query parameter and never emits `credential_offer_uri=`; foundry has no by-reference offer delivery mode | vci_0004_0005_offer_uri_carries_exactly_one_delivery_parameter |
| VCI-0005 | Credential Offer (L375) | `credential_offer_uri` MUST NOT be present when `credential_offer` is present | issuer | `conforming` | Same evidence as VCI-0004: `build_offer_uri` never emits `credential_offer_uri=` | vci_0004_0005_offer_uri_carries_exactly_one_delivery_parameter |
| VCI-0006 | Credential Offer (L383) | `credential_issuer` is REQUIRED in the Credential Offer | issuer | `conforming` | `CredentialOffer.credential_issuer` (offer.rs) is a non-optional field, always populated from `cfg.issuer.credential_issuer` in `create_offer` | vci_0006_credential_issuer_is_always_present_and_matches_config |
| VCI-0007 | Credential Offer (L384) | `credential_configuration_ids` is REQUIRED and MUST be a non-empty array of unique strings keyed into `credential_configurations_supported` | issuer | `conforming` | `create_offer` sets `credential_configuration_ids` to a single-element vec containing the resolved credential type's id, which `build_issuer_metadata` (metadata.rs) always keys into `credential_configurations_supported` | vci_0007_credential_configuration_ids_are_nonempty_and_resolve_against_metadata |
| VCI-0008 | Credential Offer (L385) | Wallet MUST use the grant per its parameters; if `grants` is absent or empty the Wallet MUST determine grant types from metadata | wallet | `out-of-scope` | Obligation falls on the Wallet or a third party, not on foundry's issuer/verifier surface |  |
| VCI-0009 | Credential Offer (L388) | Wallet MUST ignore unrecognized Credential Offer parameters | wallet | `out-of-scope` | Obligation falls on the Wallet or a third party, not on foundry's issuer/verifier surface |  |
| VCI-0010 | Credential Offer (L393) | If `issuer_state` was received the Wallet MUST include it in the subsequent Authorization Request | wallet | `out-of-scope` | Obligation falls on the Wallet or a third party, not on foundry's issuer/verifier surface |  |
| VCI-0011 | Credential Offer (L394) | `authorization_server` (authorization_code grant) MUST NOT be used unless metadata `authorization_servers` has multiple entries, and MUST match one of its values | issuer | `not-implemented` | foundry supports a single implicit Authorization Server: `CredentialIssuerMetadata.authorization_servers` (metadata.rs) is always empty, and neither `AuthorizationCodeGrant` nor `PreAuthorizedCodeGrant` (offer.rs) carries an `authorization_server` field |  |
| VCI-0012 | Credential Offer (L396) | `pre-authorized_code` is REQUIRED and MUST be short lived and single use | issuer | `conforming` | Presence and per-offer distinctness are conforming (`vci_0012_pre_authorized_code_is_present_and_distinct_per_offer`); single use is now enforced by `invalidate_pre_authorized_code` | vci_0012_pre_authorized_code_grant_rejects_replay_after_token_issuance |
| VCI-0013 | Credential Offer (L396) | Wallet MUST include the `pre-authorized_code` value in the subsequent Token Request | wallet | `out-of-scope` | Obligation falls on the Wallet or a third party, not on foundry's issuer/verifier surface |  |
| VCI-0014 | Credential Offer (L397) | If a `tx_code` object was present the Wallet MUST send the Transaction Code in the `tx_code` Token Request parameter | wallet | `out-of-scope` | Obligation falls on the Wallet or a third party, not on foundry's issuer/verifier surface |  |
| VCI-0015 | Credential Offer (L400) | `tx_code.description` length MUST NOT exceed 300 characters | issuer | `not-implemented` | `TxCodeDefinition` (offer.rs) has only `input_mode` and `length`; no `description` field is ever emitted, so the 300-character constraint has no value to violate |  |
| VCI-0016 | Credential Offer (L401) | `authorization_server` (pre-authorized_code grant) MUST NOT be used unless metadata has multiple entries, and MUST match one of its values | issuer | `not-implemented` | Same evidence as VCI-0011: single implicit Authorization Server, no `authorization_server` field ever emitted |  |
| VCI-0017 | Credential Offer (L434) | Wallet MUST send an HTTP GET to `credential_offer_uri` to retrieve the Credential Offer Object | wallet | `out-of-scope` | Obligation falls on the Wallet or a third party, not on foundry's issuer/verifier surface |  |
| VCI-0018 | Credential Offer (L445) | The response carrying a Credential Offer Object MUST use media type `application/json` | http | `not-implemented` | foundry has no HTTP endpoint serving a Credential Offer Object by reference — `build_offer_uri` (offer.rs) always inlines the offer via the `credential_offer` deep-link parameter; the by-reference retrieval flow (VCI §4.2) is not implemented |  |
| VCI-0019 | Credential Offer (L465) | Credential Offer retrieval MUST use `application/json` and MUST NOT use `application/jwt` with `alg: none` | http | `not-implemented` | Same evidence as VCI-0018: no by-reference retrieval endpoint exists |  |
| VCI-0020 | Authorization Request (L489) | `authorization_details` MUST be used to convey the details of the Credentials requested | issuer | `not-implemented` | `AuthorizeParams` (authorize.rs) has no `authorization_details` field; foundry fixes the Credential Type entirely via `issuer_state` at `create_offer` time, one of VCI's own permitted alternatives to wallet-driven selection |  |
| VCI-0021 | Authorization Request (L491) | `type` is REQUIRED and MUST be set to `openid_credential` | issuer | `not-implemented` | Same evidence as VCI-0020: the `authorization_details` mechanism this constrains is not implemented |  |
| VCI-0022 | Authorization Request (L492) | `credential_configuration_id` is REQUIRED and MUST identify an entry in `credential_configurations_supported` | issuer | `not-implemented` | Same evidence as VCI-0020 |  |
| VCI-0023 | Authorization Request (L502) | If metadata contains `authorization_servers`, the authorization detail `locations` MUST be set to the Credential Issuer Identifier | issuer | `not-implemented` | Same evidence as VCI-0020; also vacuous per VCI-0011/0016 (foundry's metadata never populates `authorization_servers`) |  |
| VCI-0024 | Authorization Request (L541) | Credential Issuers MUST interpret each scope value as a request to access the Credential Endpoint for that Credential type | issuer | `not-implemented` | `AuthorizeParams` (authorize.rs) has no `scope` field; foundry fixes the Credential Type via `issuer_state`, VCI's other permitted alternative to scope-based selection |  |
| VCI-0025 | Authorization Request (L542) | Each scope occurrence MUST be interpreted individually | issuer | `not-implemented` | Same evidence as VCI-0024: the scope mechanism this constrains is not implemented |  |
| VCI-0026 | Authorization Request (L544) | Credential Issuers MUST ignore unknown scope values | issuer | `not-implemented` | Same evidence as VCI-0024 |  |
| VCI-0027 | Authorization Request (L562) | When both scope and `authorization_details` are present the Credential Issuer MUST interpret them individually | issuer | `not-implemented` | Neither mechanism exists, so the two can never co-occur |  |
| VCI-0028 | Authorization Request (L562) | When scope and `authorization_details` request the same Credential type the Issuer MUST follow the authorization details object | issuer | `not-implemented` | Same evidence as VCI-0027 |  |
| VCI-0029 | Authorization Request (L570) | The Credential Issuer MUST account for `issuer_state` not being guaranteed to originate from it | issuer | `conforming` | `load_transaction_by_issuer_state` (transaction.rs) treats `issuer_state` purely as an opaque key into foundry's own storage; an unrecognized value simply fails to resolve, with no trust placed in any embedded claims | unresolvable_issuer_state_is_a_direct_error |
| VCI-0030 | Authorization Request (L574) | The Authorization Server MUST ignore unrecognized Authorization Request parameters | issuer | `conforming` | `AuthorizeQuery` (server.rs) derives `Deserialize` with `#[serde(default)]` on every field and no `deny_unknown_fields`; axum's `Query` extractor silently drops unrecognized query parameters rather than rejecting the request | vci_0030_authorize_ignores_unrecognized_query_parameters |
| VCI-0031 | Successful Authorization Response (L620) | Authorization Responses MUST be made as defined in RFC6749 | http | `conforming` | `AuthorizeOutcome::Success` carries exactly the RFC6749 §4.1.2 redirect-response shape (`code`, optional `state`), and `append_query` (server.rs) encodes them onto `redirect_uri` as query parameters on a 303 redirect | valid_request_mints_a_code_and_persists_pkce_fields |
| VCI-0032 | Authorization Error Response (L632) | The Authorization Error Response MUST be made as defined in RFC6749 | http | `conforming` | `authorize_handler` (server.rs) renders `AuthorizeOutcome::ErrorRedirect` as an RFC6749 SS4.1.2.1 redirect via `append_query`, carrying `error` (and `state`, when the client sent one) as query parameters on the `redirect_uri` with an HTTP 303 status | vci_0032_authorize_error_redirect_encodes_error_per_rfc6749 |
| VCI-0033 | Token Request (L653) | `pre-authorized_code` MUST be present when `grant_type` is the pre-authorized code grant | issuer | `conforming` | `handle_pre_authorized_code_grant` (token.rs) rejects the request when `pre_authorized_code` is `None` | vci_0033_pre_authorized_code_is_required_for_that_grant |
| VCI-0034 | Token Request (L654) | `tx_code` MUST be present if a `tx_code` object was present in the Credential Offer, including when empty | issuer | `conforming` | If `tx.tx_code` is `Some`, `handle_pre_authorized_code_grant` (token.rs) rejects any request whose `tx_code` does not match, including `None` | vci_0034_tx_code_is_required_when_the_offer_carried_one |
| VCI-0035 | Token Request (L654) | `tx_code` MUST only be used when `grant_type` is the pre-authorized code grant | issuer | `conforming` | `handle_authorization_code_grant` (token.rs) never reads `req.tx_code`; a stray value has no effect on that grant | vci_0035_tx_code_is_ignored_by_the_authorization_code_grant |
| VCI-0036 | Token Request (L656) | Client identification/authentication MUST follow RFC6749 Sections 4.1.3 and 3.2.1 | issuer | `conforming` | Per §3.2.1, client authentication is OPTIONAL for public clients; the pre-authorized_code grant never requires `client_id`, and the authorization_code grant substitutes PKCE binding (`code_challenge`/`code_verifier`) for a client secret | handles_valid_token_request_and_issues_access_token_and_nonce, authorization_code_grant_happy_path_issues_tokens_and_burns_the_code |
| VCI-0037 | Token Request (L660) | If the Token Request carries `authorization_details` of type `openid_credential` and metadata has `authorization_servers`, `locations` MUST contain the Credential Issuer identifier | issuer | `not-implemented` | `TokenRequest` (token.rs) has no `authorization_details` field; see VCI-0020's family (Task 7) — the mechanism this constrains is not implemented |  |
| VCI-0038 | Token Request (L668) | The Authorization Server MUST ignore unrecognized Token Request parameters | issuer | `conforming` | `TokenRequest` derives `Deserialize` with no `deny_unknown_fields`; both the JSON and form-urlencoded decode paths in `token_handler` (server.rs) silently drop unrecognized fields | vci_0038_token_request_ignores_unrecognized_parameters |
| VCI-0039 | Successful Token Response (L715) | `authorization_details` is REQUIRED in the Token Response when it was used in the Authorization or Token Request | issuer | `not-implemented` | Vacuous per VCI-0037: `authorization_details` is never used in a Token Request, so this REQUIRED-when-used clause never triggers |  |
| VCI-0040 | Successful Token Response (L716) | `credential_identifiers` is REQUIRED and MUST be a non-empty array of strings each identifying an issuable Credential Dataset | issuer | `not-implemented` | Same family as VCI-0037/0039: `TokenResponse` (token.rs) never returns `authorization_details`, so `credential_identifiers` never applies |  |
| VCI-0041 | Successful Token Response (L716) | Wallet MUST use `credential_identifiers` together with the Access Token in subsequent Credential Requests | wallet | `out-of-scope` | Obligation falls on the Wallet or a third party, not on foundry's issuer/verifier surface |  |
| VCI-0042 | Successful Token Response (L721) | Wallet MUST ignore unrecognized Token Response parameters | wallet | `out-of-scope` | Obligation falls on the Wallet or a third party, not on foundry's issuer/verifier surface |  |
| VCI-0043 | Successful Token Response (L724) | Wallet MUST ignore unrecognized data fields in Token Response `authorization_details` | wallet | `out-of-scope` | Obligation falls on the Wallet or a third party, not on foundry's issuer/verifier surface |  |
| VCI-0044 | Nonce Endpoint (L784) | A Credential Issuer that requires `c_nonce` values in proofs MUST offer a Nonce Endpoint | issuer | `conforming` | `build_issuer_metadata` (metadata.rs) sets `nonce_endpoint` unconditionally, and the `/nonce` route always exists in `server.rs` — the endpoint is offered regardless of whether it is required, a stronger guarantee | builds_issuer_metadata_from_credential_types |
| VCI-0045 | Nonce Response (L805) | `c_nonce` is REQUIRED in the Nonce Response | issuer | `conforming` | `NonceResponse.c_nonce` (nonce.rs) is a non-optional `String`, always populated by `issue_nonce` | minted_nonce_verifies_within_its_ttl |
| VCI-0046 | Nonce Response (L805) | New `c_nonce` challenge values MUST be unpredictable | issuer | `conforming` | `issue_nonce` (nonce.rs) draws its 16-byte salt from `rand::thread_rng()` | successive_nonces_differ |
| VCI-0047 | Nonce Response (L807) | The Nonce Response MUST be uncacheable via a `Cache-Control: no-store` header | http | `conforming` | `nonce_handler` (server.rs) sets `Cache-Control: no-store` on every response | nonce_endpoint_requires_no_access_token_and_is_uncacheable |
| VCI-0048 | Credential Endpoint (L827) | Support for the Credential Endpoint is REQUIRED | issuer | `conforming` | `/credential` route (server.rs) and `handle_credential_request` (credential.rs) implement the endpoint | issues_sd_jwt_vc_credential_successfully |
| VCI-0049 | Credential Endpoint (L829) | Communication with the Credential Endpoint MUST utilize TLS | http | `not-unit-testable` | TLS termination is a deployment/transport concern outside this crate's unit-test scope |  |
| VCI-0050 | Credential Request (L850) | `credential_identifier` is REQUIRED when authorization details of type `openid_credential` were returned, and MUST NOT be used otherwise | issuer | `not-implemented` | `CredentialRequest` (credential.rs) has no `credential_identifier` field; foundry never returns `credential_identifiers` (Task 8, VCI-0040), so this mechanism is never applicable |  |
| VCI-0051 | Credential Request (L850) | When `credential_identifier` is used, `credential_configuration_id` MUST NOT be present | issuer | `not-implemented` | Same evidence as VCI-0050: `credential_identifier` never exists |  |
| VCI-0052 | Credential Request (L851) | `credential_configuration_id` is REQUIRED when `credential_identifiers` was not returned, and MUST NOT be used otherwise | issuer | `conforming` | `handle_credential_request` (credential.rs) now validates `req.credential_configuration_id` against `tx.credential_type_id` before proof verification: absent, or present but naming a configured-yet-unbound Credential Type, is `IssuanceError::InvalidCredentialRequest`; naming a Credential Type this issuer does not have at all is `IssuanceError::UnknownCredentialConfiguration`. `req.format` remains deliberately unread -- it is not a Credential Request parameter in OpenID4VCI 1.0 | vci_0052_credential_configuration_id_mismatch_is_rejected |
| VCI-0053 | Credential Request (L851) | The `credential_configurations_supported` entry MUST contain one of the scope values used in the Authorization Request | issuer | `not-implemented` | `AuthorizeParams` (authorize.rs) has no `scope` field (Task 7 family); this constraint on scope-based selection never applies |  |
| VCI-0054 | Credential Request (L854) | `credential_response_encryption.jwk` is REQUIRED when response encryption is requested | issuer | `not-implemented` | `CredentialRequest` (credential.rs) has no `credential_response_encryption` field; Credential Response encryption is not implemented |  |
| VCI-0055 | Credential Request (L855) | `credential_response_encryption.enc` is REQUIRED when response encryption is requested | issuer | `not-implemented` | Same evidence as VCI-0054 |  |
| VCI-0056 | Credential Request (L856) | If `zip` is absent, compression MUST NOT be used | issuer | `not-implemented` | Same evidence as VCI-0054: compression only applies alongside response encryption, which is not implemented |  |
| VCI-0057 | Credential Request (L862) | Proofs MUST incorporate the Credential Issuer Identifier as audience and, if a Nonce Endpoint exists, a `c_nonce` | issuer | `conforming` | `verify_holder_proof` (proof.rs) validates the `aud` claim against `config.issuer.credential_issuer` and the `nonce` claim against the Nonce Endpoint's minted challenge | verifies_valid_proof_jwt |
| VCI-0058 | Credential Request (L864) | `proofs` MUST be present when `proof_types_supported` is present for the requested Credential | issuer | `conforming` | `handle_credential_request` (credential.rs) rejects the request when `proofs` is absent; `proof_types_supported` is unconditionally advertised for every Credential Configuration (metadata.rs), so the requirement is unconditional in practice | vci_0058_proofs_are_required_when_proof_types_supported |
| VCI-0059 | Credential Request (L869) | The Credential Issuer MUST ignore unrecognized Credential Request parameters | issuer | `conforming` | `CredentialRequest` derives `Deserialize` with no `deny_unknown_fields` | vci_0059_credential_request_ignores_unrecognized_parameters |
| VCI-0060 | Credential Request (L871) | The Client MUST encrypt the request when `encryption_required` is true | wallet | `out-of-scope` | Obligation falls on the Wallet or a third party, not on foundry's issuer/verifier surface |  |
| VCI-0061 | Credential Request (L873) | The Client MUST encode an encrypted Credential Request as a JWT per the encrypted-messages rules | wallet | `out-of-scope` | Obligation falls on the Wallet or a third party, not on foundry's issuer/verifier surface |  |
| VCI-0062 | Credential Request (L875) | An unencrypted Credential Request MUST use media type `application/json` | http | `conforming` | `credential_handler` (server.rs) extracts the body via axum's `Json<CredentialRequest>`, which rejects a non-`application/json` Content-Type with 415 before parsing | vci_0062_credential_request_requires_json_content_type |
| VCI-0063 | Credential Request (L960) | Credential Request encryption MUST be used whenever `credential_response_encryption` is included, to prevent substitution | issuer | `not-implemented` | Same family as VCI-0054: Credential Request encryption is not implemented, so there is no substitution risk to prevent |  |
| VCI-0064 | Credential Response (L966) | On immediate issuance the Credential Issuer MUST respond with HTTP 200 | http | `conforming` | `credential_handler` (server.rs) returns `Ok(Json(response))`, which axum serializes with the default 200 status | vci_0064_0067_credential_response_uses_http_200_and_json_content_type |
| VCI-0065 | Credential Response (L967) | On deferred issuance the Credential Issuer MUST return `transaction_id` and MUST use HTTP 202 | http | `not-implemented` | `handle_credential_request` (credential.rs) always builds and returns credentials synchronously; deferred issuance (a `transaction_id`-carrying response) never occurs |  |
| VCI-0066 | Credential Response (L969) | If encryption was requested the Credential Response MUST be encoded per the encrypted-messages rules | issuer | `not-implemented` | Same family as VCI-0054: Credential Response encryption is not implemented |  |
| VCI-0067 | Credential Response (L971) | An unencrypted Credential Response MUST use media type `application/json` | http | `conforming` | axum's `Json<T>` return type sets `Content-Type: application/json` on the response | vci_0064_0067_credential_response_uses_http_200_and_json_content_type |
| VCI-0068 | Credential Response (L975) | `credentials` MUST NOT be used when `transaction_id` is present | issuer | `not-implemented` | Same evidence as VCI-0065: `transaction_id` never exists in `CredentialResponse` (credential.rs), so this mutual-exclusion constraint never applies |  |
| VCI-0069 | Credential Response (L975) | The elements of the `credentials` array MUST be objects | issuer | `conforming` | `IssuedCredential { credential: String }` (credential.rs) always serializes as a JSON object | issues_sd_jwt_vc_credential_successfully |
| VCI-0070 | Credential Response (L976) | `credential` is REQUIRED within each `credentials` element | issuer | `conforming` | `IssuedCredential.credential` (credential.rs) is a non-optional `String`, always populated | issues_sd_jwt_vc_credential_successfully |
| VCI-0071 | Credential Response (L976) | Credential Formats expressed as binary data MUST be base64url-encoded and returned as a string | issuer | `conforming` | The `mso_mdoc` branch of `handle_credential_request` (credential.rs) now encodes the CBOR credential bytes with `base64::engine::general_purpose::URL_SAFE_NO_PAD` (`B64URL`), citing OpenID4VCI L976 in a code comment | vci_0071_mdoc_credential_string_is_base64url_encoded |
| VCI-0072 | Credential Response (L977) | `transaction_id` MUST NOT be used when `credentials` is present | issuer | `not-implemented` | Same evidence as VCI-0065/0068: `transaction_id` never exists in `CredentialResponse` |  |
| VCI-0073 | Credential Response (L977) | `transaction_id` MUST be invalidated once the Credential has been obtained | issuer | `not-implemented` | Same family as VCI-0065: deferred issuance never occurs, so no `transaction_id` is ever minted to invalidate |  |
| VCI-0074 | Credential Response (L978) | `interval` is REQUIRED when `transaction_id` is present and MUST NOT be used when `credentials` is present | issuer | `not-implemented` | Same family as VCI-0065: `interval` has no corresponding field in `CredentialResponse` and deferred issuance never occurs |  |
| VCI-0075 | Credential Response (L979) | `notification_id` MUST NOT be used when `credentials` is absent | issuer | `conforming` | `handle_credential_request` (credential.rs) always returns `notification_id: None`, and `credentials` is always present on a successful response — the constraint holds vacuously and unconditionally | issues_sd_jwt_vc_credential_successfully |
| VCI-0076 | Credential Response (L979) | Wallet MUST include `notification_id` in the Notification Request | wallet | `out-of-scope` | Obligation falls on the Wallet or a third party, not on foundry's issuer/verifier surface |  |
| VCI-0077 | Credential Response (L981) | Wallet MUST ignore unrecognized Credential Response parameters | wallet | `out-of-scope` | Obligation falls on the Wallet or a third party, not on foundry's issuer/verifier surface |  |
| VCI-0078 | Credential Error Response (L1041) | Payload-related errors MUST use the specific error codes of this section rather than generic `invalid_request` | issuer | `conforming` | `IssuanceError` now has a dedicated `InvalidNonce` variant (error.rs); all four `verify_nonce` (nonce.rs) failure modes -- malformed, forged, or expired -- report `invalid_nonce`, while a *missing* `nonce` claim (L1049 clause 3) stays `invalid_proof`. The key-attestation nonce path (attestation.rs) propagates the same `InvalidNonce` with a `key_attestation:` prefix rather than collapsing back to `invalid_proof` | vci_0078_expired_nonce_reports_invalid_nonce_not_invalid_proof |
| VCI-0079 | Credential Error Response (L1043) | An unsupported Credential request MUST return HTTP 400 with content type `application/json` | http | `conforming` | `wallet_error_response` (server.rs) maps every `IssuanceError` variant reachable from `handle_credential_request` to `(StatusCode::BAD_REQUEST, Json(...))` | credential_request_with_proof_aud_mismatch_is_rejected |
| VCI-0080 | Credential Error Response (L1045) | `error` is REQUIRED in the Credential Error Response | http | `conforming` | `wallet_error_response`'s JSON body (server.rs) always includes an `error` key | credential_request_with_proof_aud_mismatch_is_rejected |
| VCI-0081 | Credential Error Response (L1053) | `error_description` MUST be human-readable ASCII text | http | `not-unit-testable` | The only client-triggerable Credential-endpoint errors carry hard-coded English `error_description` text; a non-ASCII value would require an operator to configure a non-ASCII `credential_type_id`, which is a deployment/configuration decision outside a client-facing conformance test's reach |  |
| VCI-0082 | Credential Error Response (L1053) | `error_description` MUST NOT include characters outside %x20-21 / %x23-5B / %x5D-7E | http | `not-unit-testable` | Same reasoning as VCI-0081 |  |
| VCI-0083 | Deferred Credential Endpoint (L1075) | Wallet MUST present an Access Token valid for the previously requested Credentials | wallet | `out-of-scope` | Obligation falls on the Wallet or a third party, not on foundry's issuer/verifier surface |  |
| VCI-0084 | Deferred Credential Endpoint (L1077) | Communication with the Deferred Credential Endpoint MUST utilize TLS | http | `not-implemented` | No Deferred Credential Endpoint route exists in `server.rs`; per `AGENTS.md` §4.4, this OPTIONAL endpoint is unimplemented |  |
| VCI-0085 | Deferred Credential Request (L1085) | `transaction_id` is REQUIRED in the Deferred Credential Request | issuer | `not-implemented` | Same evidence as VCI-0084 |  |
| VCI-0086 | Deferred Credential Request (L1088) | The Credential Issuer MUST invalidate `transaction_id` once the Credential has been obtained | issuer | `not-implemented` | Same evidence as VCI-0084 |  |
| VCI-0087 | Deferred Credential Request (L1091) | The Credential Issuer MUST ignore unrecognized Deferred Credential Request parameters | issuer | `not-implemented` | Same evidence as VCI-0084 |  |
| VCI-0088 | Deferred Credential Request (L1093) | The Client MUST encrypt the deferred request when `encryption_required` is true | wallet | `out-of-scope` | Obligation falls on the Wallet or a third party, not on foundry's issuer/verifier surface |  |
| VCI-0089 | Deferred Credential Request (L1095) | An encrypted Deferred Credential Request MUST be encoded as a JWT per the encrypted-messages rules | wallet | `out-of-scope` | Obligation falls on the Wallet or a third party, not on foundry's issuer/verifier surface |  |
| VCI-0090 | Deferred Credential Request (L1097) | An unencrypted Deferred Credential Request MUST use media type `application/json` | http | `not-implemented` | Same evidence as VCI-0084 |  |
| VCI-0091 | Deferred Credential Request (L1112) | Deferred Credential Request encryption MUST be used whenever `credential_response_encryption` is included | issuer | `not-implemented` | Same evidence as VCI-0084 |  |
| VCI-0092 | Deferred Credential Response (L1118) | On success the Deferred Credential Response MUST use `credentials` and MUST respond with HTTP 200 | http | `not-implemented` | Same evidence as VCI-0084 |  |
| VCI-0093 | Deferred Credential Response (L1119) | When more time is needed the response MUST use `interval` and `transaction_id`, MUST use HTTP 202, and `transaction_id` MUST equal the request value | http | `not-implemented` | Same evidence as VCI-0084 |  |
| VCI-0094 | Deferred Credential Response (L1124) | Wallet MUST ignore unrecognized Deferred Credential Response parameters | wallet | `out-of-scope` | Obligation falls on the Wallet or a third party, not on foundry's issuer/verifier surface |  |
| VCI-0095 | Deferred Credential Response (L1126) | If encryption was requested the Issuer MUST encode the deferred response accordingly and MUST use the newly provided encryption object | issuer | `not-implemented` | Same evidence as VCI-0084 |  |
| VCI-0096 | Deferred Credential Response (L1128) | An unencrypted Deferred Credential Response MUST use media type `application/json` | http | `not-implemented` | Same evidence as VCI-0084 |  |
| VCI-0097 | Encrypted Messages (L1186) | The contents of an encrypted message MUST be encoded as a JWT | issuer | `not-implemented` | Credential Request/Response encryption (`credential_response_encryption`) is not implemented anywhere in `foundry-issuer`; the Encrypted Messages mechanism this section defines is never invoked |  |
| VCI-0098 | Encrypted Messages (L1186) | The media type of an encrypted message MUST be set to `application/jwt` | http | `not-implemented` | Same evidence as VCI-0097 |  |
| VCI-0099 | Encrypted Messages (L1188) | The `alg` parameter MUST be present on the encryption key | issuer | `not-implemented` | Same evidence as VCI-0097 |  |
| VCI-0100 | Encrypted Messages (L1188) | The JWE `alg` algorithm used MUST equal the `alg` value of the chosen JWK | issuer | `not-implemented` | Same evidence as VCI-0097 |  |
| VCI-0101 | Encrypted Messages (L1188) | If the selected public key has a `kid`, the JWE MUST include the same `kid` header parameter | issuer | `not-implemented` | Same evidence as VCI-0097 |  |
| VCI-0102 | Notification Endpoint (L1200) | Wallet MUST present a valid Access Token to the Notification Endpoint | wallet | `out-of-scope` | Obligation falls on the Wallet or a third party, not on foundry's issuer/verifier surface |  |
| VCI-0103 | Notification Endpoint (L1202) | A Credential Issuer requiring notification MUST ensure the Access Token is valid at the Notification Endpoint | issuer | `not-implemented` | No Notification Endpoint route exists in `server.rs`; per `AGENTS.md` §4.4, this OPTIONAL endpoint is unimplemented |  |
| VCI-0104 | Notification Endpoint (L1206) | Communication with the Notification Endpoint MUST utilize TLS | http | `not-implemented` | Same evidence as VCI-0103 |  |
| VCI-0105 | Notification Request (L1212) | `notification_id` is REQUIRED in the Notification Request | issuer | `not-implemented` | Same evidence as VCI-0103 |  |
| VCI-0106 | Notification Request (L1213) | `event` is REQUIRED and MUST be a case-sensitive string from the defined set | issuer | `not-implemented` | Same evidence as VCI-0103 |  |
| VCI-0107 | Notification Request (L1213) | Partial errors when issuing a batch MUST be treated as the overall issuance flow failing | issuer | `not-implemented` | Same evidence as VCI-0103 |  |
| VCI-0108 | Notification Request (L1214) | `event_description` MUST NOT include characters outside %x20-21 / %x23-5B / %x5D-7E | issuer | `not-implemented` | Same evidence as VCI-0103 |  |
| VCI-0109 | Notification Request (L1217) | The Credential Issuer MUST ignore unrecognized Notification Request parameters | issuer | `not-implemented` | Same evidence as VCI-0103 |  |
| VCI-0110 | Notification Response (L1250) | On success the Credential Issuer MUST respond with a 2xx HTTP status code | http | `not-implemented` | Same evidence as VCI-0103 |  |
| VCI-0111 | Notification Error Response (L1262) | An invalid `notification_id` MUST return HTTP 400 with content type `application/json` | http | `not-implemented` | Same evidence as VCI-0103 |  |
| VCI-0112 | Notification Error Response (L1264) | `error` is REQUIRED in the Notification Error Response | http | `not-implemented` | Same evidence as VCI-0103 |  |
| VCI-0113 | Client Metadata (L1292) | Wallet MUST ignore unrecognized Client Metadata parameters | wallet | `out-of-scope` | Obligation falls on the Wallet or a third party, not on foundry's issuer/verifier surface |  |
| VCI-0114 | Credential Issuer Metadata (L1312) | Issuers publishing metadata MUST serve it at `/.well-known/openid-credential-issuer` inserted between host and path | http | `gap` | `wallet_router` (server.rs) registers the well-known route at the literal fixed path `/.well-known/openid-credential-issuer` regardless of `config.issuer.credential_issuer`; per the spec's own worked example (L1314), a Credential Issuer Identifier with a path component (e.g. `https://issuer.example.com/tenant1`) must serve its metadata from `https://issuer.example.com/.well-known/openid-credential-issuer/tenant1`, a URL foundry never registers a route for. Currently latent -- every credential_type fixture and deployment example in this repository configures `credential_issuer` as a bare origin with no path -- but nothing prevents an operator from configuring one that does. See GAP-VCI-11 | gap_vci_11_well_known_metadata_ignores_credential_issuer_path_component |
| VCI-0115 | Credential Issuer Metadata (L1316) | Communication with the Credential Issuer Metadata Endpoint MUST utilize TLS | http | `not-unit-testable` | Same reasoning as VCI-0049: TLS termination is a deployment/transport concern outside this crate's unit-test scope |  |
| VCI-0116 | Credential Issuer Metadata (L1318) | Wallet MUST fetch Credential Issuer Metadata using HTTP GET | wallet | `out-of-scope` | Obligation falls on the Wallet or a third party, not on foundry's issuer/verifier surface |  |
| VCI-0117 | Credential Issuer Metadata (L1320) | The Credential Issuer MUST respond with HTTP 200 and the metadata parameters | http | `conforming` | `issuer_metadata` (server.rs) returns `Json(build_issuer_metadata(&state.config))` unconditionally -- there is no error branch, so the endpoint always answers 200 with the metadata body | vci_0117_0119_issuer_metadata_endpoint_returns_http_200_and_json_content_type |
| VCI-0118 | Credential Issuer Metadata (L1325) | The Credential Issuer MUST support returning metadata unsigned as `application/json` | issuer | `conforming` | `CredentialIssuerMetadata` (metadata.rs) is a plain `Serialize` struct with no signing wrapper, and `issuer_metadata` (server.rs) returns it via `Json<..>`, which always produces `application/json` | vci_0118_metadata_round_trips_as_plain_json |
| VCI-0119 | Credential Issuer Metadata (L1325) | The Credential Issuer MUST indicate the media type of returned metadata via the `Content-Type` header | http | `conforming` | axum's `Json<T>` responder (used by `issuer_metadata`, server.rs) always sets `Content-Type: application/json` | vci_0117_0119_issuer_metadata_endpoint_returns_http_200_and_json_content_type |
| VCI-0120 | Credential Issuer Metadata (L1332) | `Accept-Language` and `Content-Language` values MUST follow RFC3066 | http | `not-implemented` | No route anywhere in `server.rs` reads an `Accept-Language` request header or sets a `Content-Language` response header; `CredentialIssuerMetadata`'s `display` field carries locale tags inline in JSON (per-object `locale`, see GAP-VCI-10) rather than through content negotiation, so this RFC3066-conformance clause has no code path to apply to at all |  |
| VCI-0121 | Credential Issuer Metadata (L1344) | Signed metadata MUST be secured using a JWS | issuer | `not-implemented` | `issuer_metadata` (server.rs) always returns unsigned JSON; no signing logic exists anywhere in `metadata.rs`. Signed metadata is OPTIONAL per this section ("MUST support ... unsigned ... MAY support ... signed"), so its absence is a legitimate unimplemented feature |  |
| VCI-0122 | Credential Issuer Metadata (L1347) | Signed metadata `alg` is REQUIRED and MUST NOT be `none` or a symmetric algorithm | issuer | `not-implemented` | Same evidence as VCI-0121 |  |
| VCI-0123 | Credential Issuer Metadata (L1348) | Signed metadata `typ` is REQUIRED and MUST be `openidvci-issuer-metadata+jwt` | issuer | `not-implemented` | Same evidence as VCI-0121 |  |
| VCI-0124 | Credential Issuer Metadata (L1352) | Signed metadata `sub` is REQUIRED and MUST match the Credential Issuer Identifier | issuer | `not-implemented` | Same evidence as VCI-0121 |  |
| VCI-0125 | Credential Issuer Metadata (L1353) | Signed metadata `iat` is REQUIRED | issuer | `not-implemented` | Same evidence as VCI-0121 |  |
| VCI-0126 | Credential Issuer Metadata (L1356) | All metadata parameters MUST appear as top-level claims in the JWS payload | issuer | `not-implemented` | Same evidence as VCI-0121 |  |
| VCI-0127 | Credential Issuer Metadata (L1358) | Wallet MUST establish trust in the signer of signed metadata, and MUST otherwise reject it | wallet | `out-of-scope` | Obligation falls on the Wallet or a third party, not on foundry's issuer/verifier surface |  |
| VCI-0128 | Credential Issuer Metadata (L1366) | `credential_issuer` is REQUIRED and MUST be identical to the identifier used to build the well-known URL | issuer | `conforming` | `Config::validate()` (foundry-core/src/config/validate.rs) now requires `issuer.credential_issuer` to be byte-identical to `server.wallet_facing.public_base_url` -- a simple string comparison with no normalization, per this clause -- rejecting any mismatch, including a trailing-slash-only difference, before the server ever starts | vci_0128_config_validation_does_not_enforce_credential_issuer_identity_match |
| VCI-0129 | Credential Issuer Metadata (L1367) | `authorization_servers`, when present, MUST be a non-empty array of Authorization Server identifiers | issuer | `conforming` | `build_issuer_metadata` (metadata.rs) always sets `authorization_servers: Vec::new()`, and `#[serde(skip_serializing_if = "Vec::is_empty")]` omits it entirely rather than serializing an empty array — foundry always acts as its own single implicit Authorization Server (the same topology already established for VCI-0011/0016) | vci_0129_authorization_servers_omitted_when_empty |
| VCI-0130 | Credential Issuer Metadata (L1368) | `credential_endpoint` is REQUIRED and MUST use the `https` scheme | issuer | `conforming` | `Config::validate()` now rejects a non-`https` `issuer.credential_issuer`, from which `credential_endpoint` is derived, unless its host is a loopback address (`localhost`, `127.0.0.1`, `::1`, `[::1]`) -- a deliberate documented exemption so the shipped dev config keeps booting; see `foundry-core/AGENTS.md` Gotchas | vci_0130_0131_config_validation_does_not_enforce_https_scheme_for_issuer_urls |
| VCI-0131 | Credential Issuer Metadata (L1369) | `nonce_endpoint`, when present, MUST use the `https` scheme | issuer | `conforming` | Same `Config::validate()` check as VCI-0130 -- `nonce_endpoint` is derived from the same `issuer.credential_issuer` | vci_0130_0131_config_validation_does_not_enforce_https_scheme_for_issuer_urls |
| VCI-0132 | Credential Issuer Metadata (L1370) | `deferred_credential_endpoint`, when present, MUST use the `https` scheme | issuer | `not-implemented` | The Deferred Credential Endpoint is not implemented (see VCI-0084); `CredentialIssuerMetadata` (metadata.rs) has no `deferred_credential_endpoint` field, so it is never present to violate this conditional MUST |  |
| VCI-0133 | Credential Issuer Metadata (L1371) | `notification_endpoint`, when present, MUST use the `https` scheme | issuer | `not-implemented` | The Notification Endpoint is not implemented (see VCI-0103); `CredentialIssuerMetadata` (metadata.rs) has no `notification_endpoint` field, so it is never present to violate this conditional MUST |  |
| VCI-0134 | Credential Issuer Metadata (L1373) | `credential_request_encryption.jwks` is REQUIRED and every JWK MUST carry a unique `kid` | issuer | `not-implemented` | Credential Request/Response encryption is not implemented (same evidence as VCI-0054); `CredentialIssuerMetadata` (metadata.rs) has no `credential_request_encryption` field |  |
| VCI-0135 | Credential Issuer Metadata (L1374) | `credential_request_encryption.enc_values_supported` is REQUIRED and non-empty | issuer | `not-implemented` | Same evidence as VCI-0134 |  |
| VCI-0136 | Credential Issuer Metadata (L1376) | `credential_request_encryption.encryption_required` is REQUIRED | issuer | `not-implemented` | Same evidence as VCI-0134 |  |
| VCI-0137 | Credential Issuer Metadata (L1378) | `credential_response_encryption.alg_values_supported` is REQUIRED and non-empty | issuer | `not-implemented` | Same evidence as VCI-0054: `CredentialIssuerMetadata` (metadata.rs) has no `credential_response_encryption` field |  |
| VCI-0138 | Credential Issuer Metadata (L1379) | `credential_response_encryption.enc_values_supported` is REQUIRED and non-empty | issuer | `not-implemented` | Same evidence as VCI-0137 |  |
| VCI-0139 | Credential Issuer Metadata (L1381) | `credential_response_encryption.encryption_required` is REQUIRED | issuer | `not-implemented` | Same evidence as VCI-0137 |  |
| VCI-0140 | Credential Issuer Metadata (L1383) | `batch_size` is REQUIRED and MUST be 2 or greater | issuer | `not-implemented` | `CredentialIssuerMetadata` (metadata.rs) has no `batch_credential_issuance` field at all, even though `handle_credential_request` (credential.rs) functionally accepts multiple proofs and issues one Credential per proof for the same Credential Dataset (see HAIP-0011, `conforming`) — the object advertising this capability, and its `batch_size` member, are simply never implemented | haip_0011_multiple_proofs_yield_one_credential_per_proof |
| VCI-0141 | Credential Issuer Metadata (L1386) | There MUST be only one issuer `display` object per language identifier | issuer | `conforming` | `CredentialIssuerMetadata.display` (metadata.rs) is hardcoded to `Vec::new()` — issuer-level display is not configurable at all, so zero objects trivially satisfies "at most one per language identifier" | vci_0118_metadata_round_trips_as_plain_json |
| VCI-0142 | Credential Issuer Metadata (L1388) | Issuer logo `uri` is REQUIRED | issuer | `not-implemented` | Same evidence as VCI-0141: issuer-level `display` (and any logo within it) is not configurable at all, so this OPTIONAL sub-object is simply never present |  |
| VCI-0143 | Credential Issuer Metadata (L1390) | `credential_configurations_supported` is REQUIRED | issuer | `conforming` | `CredentialIssuerMetadata.credential_configurations_supported` (metadata.rs) is a non-optional field, always populated from `cfg.credential_types` | builds_issuer_metadata_from_credential_types |
| VCI-0144 | Credential Issuer Metadata (L1391) | `format` is REQUIRED in each supported credential configuration | issuer | `conforming` | `CredentialConfigurationSupported.format` (metadata.rs) is a non-optional `String`, always populated from `ct.format` | builds_issuer_metadata_from_credential_types |
| VCI-0145 | Credential Issuer Metadata (L1392) | The Authorization Server MUST be able to uniquely identify the Credential Issuer from the `scope` value | issuer | `not-implemented` | `scope` is OPTIONAL per this section and, per VCI-0053, is never populated in `CredentialConfigurationSupported` (metadata.rs) at all; this clause only applies when `scope` is used, and it never is (see also GAP-HAIP-01, which files the corresponding HAIP-level MUST) |  |
| VCI-0146 | Credential Issuer Metadata (L1394) | `cryptographic_binding_methods_supported` MUST be present when Cryptographic Key Binding is required and omitted otherwise | issuer | `gap` | GAP-VCI-07 | vci_0146_0147_metadata_omits_binding_and_proof_fields_when_key_binding_not_required |
| VCI-0147 | Credential Issuer Metadata (L1395) | `proof_types_supported` MUST be present if `cryptographic_binding_methods_supported` is present and omitted otherwise | issuer | `gap` | GAP-VCI-07 | vci_0146_0147_metadata_omits_binding_and_proof_fields_when_key_binding_not_required |
| VCI-0148 | Credential Issuer Metadata (L1396) | `proof_signing_alg_values_supported` is REQUIRED and non-empty for each proof type | issuer | `conforming` | `ProofTypeSupported.proof_signing_alg_values_supported` (metadata.rs) is always `vec!["ES256".to_string()]` — a non-optional, non-empty field | builds_issuer_metadata_from_credential_types |
| VCI-0149 | Credential Issuer Metadata (L1397) | `key_attestations_required` MUST NOT be present when key attestation is not required | issuer | `conforming` | `build_issuer_metadata` (metadata.rs) sets `key_attestations_required: None` (omitted via `skip_serializing_if`) whenever `key_attestation.mode != Required` | key_attestations_required_absent_when_mode_optional_or_disabled |
| VCI-0150 | Credential Issuer Metadata (L1402) | Credential display `name` is REQUIRED | issuer | `gap` | GAP-VCI-10 | vci_0150_0151_0152_0153_0154_credential_display_objects_are_not_structurally_validated |
| VCI-0151 | Credential Issuer Metadata (L1403) | There MUST be only one credential `display` object per language identifier | issuer | `gap` | GAP-VCI-10 | vci_0150_0151_0152_0153_0154_credential_display_objects_are_not_structurally_validated |
| VCI-0152 | Credential Issuer Metadata (L1405) | Credential logo `uri` is REQUIRED | issuer | `gap` | GAP-VCI-10 | vci_0150_0151_0152_0153_0154_credential_display_objects_are_not_structurally_validated |
| VCI-0153 | Credential Issuer Metadata (L1409) | `background_image` MUST include a `uri` parameter | issuer | `gap` | GAP-VCI-10 | vci_0150_0151_0152_0153_0154_credential_display_objects_are_not_structurally_validated |
| VCI-0154 | Credential Issuer Metadata (L1410) | `background_image.uri` is REQUIRED | issuer | `gap` | GAP-VCI-10 | vci_0150_0151_0152_0153_0154_credential_display_objects_are_not_structurally_validated |
| VCI-0155 | Credential Issuer Metadata (L1420) | The Authorization Server MUST be able to determine from Issuer metadata which claims the requested Credentials disclose | issuer | `conforming` | `CredentialConfigurationSupported.claims` (metadata.rs) is always populated from `ct.claims`, carrying each claim's `path` and `selectively_disclosable` flag | vci_0155_credential_configuration_claims_reveal_disclosed_paths |
| VCI-0156 | Credential Issuer Metadata (L1423) | Wallet MUST ignore unrecognized Credential Issuer Metadata parameters | wallet | `out-of-scope` | Obligation falls on the Wallet or a third party, not on foundry's issuer/verifier surface |  |
| VCI-0157 | AS Metadata (L1441) | Wallet MUST ignore unrecognized Authorization Server Metadata parameters | wallet | `out-of-scope` | Obligation falls on the Wallet or a third party, not on foundry's issuer/verifier surface |  |
| VCI-0158 | Security / Credential Offer (L1486) | Wallet MUST treat Credential Offer parameter values as untrustworthy | wallet | `out-of-scope` | Obligation falls on the Wallet or a third party, not on foundry's issuer/verifier surface |  |
| VCI-0159 | Security / Credential Offer (L1488) | Wallet MUST NOT accept Credentials merely because a Credential Offer was used; all protocol steps MUST still be performed | wallet | `out-of-scope` | Obligation falls on the Wallet or a third party, not on foundry's issuer/verifier surface |  |
| VCI-0160 | Security / Credential Offer (L1490) | The Credential Issuer MUST ensure release of privacy-sensitive data in a Credential Offer is legal | issuer | `out-of-scope` | This is a legal/regulatory compliance obligation on the deployment operator (which jurisdiction's privacy law applies to which claims), not a mechanism `foundry-issuer`'s code could itself enforce or a unit test could adjudicate |  |
| VCI-0161 | Security / TLS Requirements (L1522) | Implementations MUST follow BCP195 | http | `not-unit-testable` | Same reasoning as VCI-0049: BCP195 cipher-suite/protocol-version selection is a deployment/transport concern (reverse proxy, TLS library configuration) outside this crate's unit-test scope |  |
| VCI-0162 | Security / TLS Requirements (L1523) | A TLS server certificate check MUST be performed per RFC6125 whenever TLS is used | http | `not-unit-testable` | Same reasoning as VCI-0049: certificate hostname verification is performed by the TLS client (the Wallet), not by anything foundry's server-side code controls or could unit-test |  |
| VCI-0163 | Security / Protecting the Access Token (L1527) | Long-lived Access Tokens MUST NOT be issued unless sender-constrained | issuer | `conforming` | `mint_and_save_tokens` (token.rs) always sets `expires_in: 600` (10 minutes) unconditionally -- access tokens are never "long-lived" in any deployment, so this clause's precondition never triggers and the sender-constraining requirement is vacuously satisfied. This is a narrower, conditional mandate than HAIP's own unconditional DPoP requirement (HAIP-0009, already filed as GAP-HAIP-03) | handles_valid_token_request_and_issues_access_token_and_nonce |
| VCI-0164 | Security / Protecting the Access Token (L1529) | Bearer Access Tokens stored by the Wallet MUST be stored securely | wallet | `out-of-scope` | Obligation falls on the Wallet or a third party, not on foundry's issuer/verifier surface |  |
| VCI-0165 | Format Profile / jwt_vc_json (L2116) | With format `jwt_vc_json` the Offer, Authorization Details, Credential Request and Issuer metadata MUST NOT be processed using JSON-LD rules | issuer | `out-of-scope` | Excluded 2026-07-31 by scope decision: foundry issues only `dc+sd-jwt` and `mso_mdoc` (`crates/foundry-core/src/config/validate.rs`); the W3C VC format-profile clause set would adjudicate uniformly to not-implemented |  |
| VCI-0166 | Format Profile / jwt_vc_json (L2124) | `credential_definition` is REQUIRED | issuer | `out-of-scope` | Excluded 2026-07-31 by scope decision: foundry issues only `dc+sd-jwt` and `mso_mdoc` (`crates/foundry-core/src/config/validate.rs`); the W3C VC format-profile clause set would adjudicate uniformly to not-implemented |  |
| VCI-0167 | Format Profile / jwt_vc_json (L2125) | `credential_definition.type` is REQUIRED | issuer | `out-of-scope` | Excluded 2026-07-31 by scope decision: foundry issues only `dc+sd-jwt` and `mso_mdoc` (`crates/foundry-core/src/config/validate.rs`); the W3C VC format-profile clause set would adjudicate uniformly to not-implemented |  |
| VCI-0168 | Format Profile / jwt_vc_json (L2139) | The `credential` claim MUST be a JWT and MUST NOT be re-encoded | issuer | `out-of-scope` | Excluded 2026-07-31 by scope decision: foundry issues only `dc+sd-jwt` and `mso_mdoc` (`crates/foundry-core/src/config/validate.rs`); the W3C VC format-profile clause set would adjudicate uniformly to not-implemented |  |
| VCI-0169 | Format Profile / ldp_vc (L2155) | With format `ldp_vc` the Offer, Authorization Details, Credential Request and Issuer metadata MUST NOT be processed using JSON-LD rules | issuer | `out-of-scope` | Excluded 2026-07-31 by scope decision: foundry issues only `dc+sd-jwt` and `mso_mdoc` (`crates/foundry-core/src/config/validate.rs`); the W3C VC format-profile clause set would adjudicate uniformly to not-implemented |  |
| VCI-0170 | Format Profile / ldp_vc (L2167) | `credential_definition` is REQUIRED | issuer | `out-of-scope` | Excluded 2026-07-31 by scope decision: foundry issues only `dc+sd-jwt` and `mso_mdoc` (`crates/foundry-core/src/config/validate.rs`); the W3C VC format-profile clause set would adjudicate uniformly to not-implemented |  |
| VCI-0171 | Format Profile / ldp_vc (L2168) | `credential_definition.@context` is REQUIRED | issuer | `out-of-scope` | Excluded 2026-07-31 by scope decision: foundry issues only `dc+sd-jwt` and `mso_mdoc` (`crates/foundry-core/src/config/validate.rs`); the W3C VC format-profile clause set would adjudicate uniformly to not-implemented |  |
| VCI-0172 | Format Profile / ldp_vc (L2169) | `credential_definition.type` is REQUIRED | issuer | `out-of-scope` | Excluded 2026-07-31 by scope decision: foundry issues only `dc+sd-jwt` and `mso_mdoc` (`crates/foundry-core/src/config/validate.rs`); the W3C VC format-profile clause set would adjudicate uniformly to not-implemented |  |
| VCI-0173 | Format Profile / ldp_vc (L2183) | The `credential` claim MUST be a JSON object and MUST NOT be re-encoded | issuer | `out-of-scope` | Excluded 2026-07-31 by scope decision: foundry issues only `dc+sd-jwt` and `mso_mdoc` (`crates/foundry-core/src/config/validate.rs`); the W3C VC format-profile clause set would adjudicate uniformly to not-implemented |  |
| VCI-0174 | Format Profile / jwt_vc_json-ld (L2195) | With format `jwt_vc_json-ld` the Offer, Authorization Details, Credential Request and Issuer metadata MUST NOT be processed using JSON-LD rules | issuer | `out-of-scope` | Excluded 2026-07-31 by scope decision: foundry issues only `dc+sd-jwt` and `mso_mdoc` (`crates/foundry-core/src/config/validate.rs`); the W3C VC format-profile clause set would adjudicate uniformly to not-implemented |  |
| VCI-0175 | Format Profile / mdoc (L2235) | `doctype` is REQUIRED and identifies the Credential type per ISO 18013-5 | issuer | `gap` | `Config::validate()` does enforce `doctype`'s *presence* for `mso_mdoc` credential types, but `handle_credential_request`'s docType resolution prefers `vct` over `doctype` whenever a config carries both -- producing a docType that is an SD-JWT-VC-style URL, not an ISO 18013-5 identifier. See GAP-VCI-12 | gap_vci_12_mdoc_doc_type_prefers_vct_over_doctype_when_both_configured |
| VCI-0176 | Format Profile / mdoc (L2249) | The `credential` claim MUST be the base64url-encoded CBOR `IssuerSigned` structure | issuer | `conforming` | Same underlying finding as VCI-0071: the mso_mdoc branch of `handle_credential_request` now encodes with `base64::engine::general_purpose::URL_SAFE_NO_PAD` | vci_0071_mdoc_credential_string_is_base64url_encoded |
| VCI-0177 | Format Profile / SD-JWT VC (L2270) | `vct` is REQUIRED and designates the Credential type | issuer | `conforming` | The dc+sd-jwt branch of `handle_credential_request` always resolves `vct`, falling back to `tx.credential_type_id` when unconfigured -- never absent | test_verify_vp_response_sd_jwt_vc |
| VCI-0178 | Format Profile / SD-JWT VC (L2284) | The `credential` claim MUST be an SD-JWT VC string and MUST NOT be re-encoded | issuer | `conforming` | `build_sd_jwt_vc`'s compact SD-JWT string is placed directly into `IssuedCredential.credential` with no additional encoding layer | vci_0064_0067_credential_response_uses_http_200_and_json_content_type |
| VCI-0179 | Claims Description / Authorization Details (L2306) | `path` is REQUIRED and MUST be a non-empty array | issuer | `not-implemented` | Same root cause as VCI-0020: the `authorization_details` mechanism this constrains is not implemented anywhere in `foundry-issuer` |  |
| VCI-0180 | Claims Description / Issuer Metadata (L2323) | `path` MUST be a non-empty array representing a claims path pointer | issuer | `gap` | `ClaimDef.path` is typed `Vec<String>`, which cannot represent the `null`/integer path segments the spec's claims path pointer grammar allows at all, and `Config::validate()` never checks that a configured `path` is non-empty. See GAP-VCI-13 | gap_vci_13_claims_path_pointer_emptiness_and_shape_are_never_validated |
| VCI-0181 | Claims Description / Issuer Metadata (L2338) | There MUST be only one claims display object per language identifier | issuer | `gap` | Same underlying finding as VCI-0150-0154/GAP-VCI-10: `ct.display` (and equally `claims[].display`) is untyped `Vec<serde_json::Value>` passed straight through by `build_issuer_metadata` with no structural validation of locale duplication anywhere | vci_0150_0151_0152_0153_0154_credential_display_objects_are_not_structurally_validated |
| VCI-0182 | Claims Description / Processing Rules (L2349) | Processing MUST be aborted when a claims description object cannot be resolved | issuer | `gap` | Same underlying finding as GAP-VCI-10: `Config::validate()` never checks a credential_type's `claims` array for repeated or contradictory claims description objects (e.g. two `ClaimDef`s addressing the same `path`) before `build_issuer_metadata` publishes it as-is | vci_0150_0151_0152_0153_0154_credential_display_objects_are_not_structurally_validated |
| VCI-0183 | Claims Path Pointer (L2366) | A claims path pointer MUST be a non-empty array of strings, nulls and integers | issuer | `gap` | Same finding as VCI-0180: `ClaimDef.path: Vec<String>` cannot express `null`/integer segments and its non-emptiness is never validated. See GAP-VCI-13 | gap_vci_13_claims_path_pointer_emptiness_and_shape_are_never_validated |
| VCI-0184 | Key Attestation JWT (L2497) | `alg` is REQUIRED and MUST NOT be `none` or a symmetric algorithm | issuer | `conforming` | `verify_key_attestation_jwt` (attestation.rs) rejects a header `alg` of `none` or any `HS*` symmetric identifier before ever attempting signature verification | vci_0184_key_attestation_rejects_symmetric_alg |
| VCI-0185 | Key Attestation JWT (L2498) | `typ` is REQUIRED and MUST be `key-attestation+jwt` | issuer | `conforming` | `verify_key_attestation_jwt` (attestation.rs) requires the header `typ` to equal `key-attestation+jwt` | vci_0185_key_attestation_requires_correct_typ |
| VCI-0186 | Key Attestation JWT (L2503) | `iat` is REQUIRED | issuer | `gap` | GAP-VCI-05 | vci_0186_key_attestation_without_iat_is_rejected |
| VCI-0187 | Key Attestation JWT (L2504) | `exp` MUST be present when the attestation is used with the `jwt` proof type | issuer | `conforming` | `verify_key_attestation_jwt` (attestation.rs) requires `exp` unconditionally — a superset of "present when used with the jwt proof type", since foundry only implements the jwt proof type's key-attestation path | vci_0187_key_attestation_without_exp_is_rejected |
| VCI-0188 | Key Attestation JWT (L2505) | `attested_keys` is REQUIRED and MUST be a non-empty array of JWKs from the same key storage component | issuer | `conforming` | `verify_key_attestation_jwt` (attestation.rs) requires `attested_keys` to be present and non-empty; "from the same key storage component" is an attribute of how the Wallet generated the keys, not independently verifiable by the issuer | rejects_empty_attested_keys |
| VCI-0189 | Key Attestation JWT (L2512) | With the `jwt` proof type the Credential Issuer MUST validate that the proof JWT is signed by a key contained in the attestation | issuer | `conforming` | `verify_holder_proof` (proof.rs) resolves the `kid` header claim as an index into `claims.attested_keys` and verifies the outer proof JWT's signature against that specific attested key | accepts_kid_plus_key_attestation_proof |
| VCI-0190 | Attack Potential Resistance (L2544) | `iso_18045_high` MUST be used when resistant to attack potential High (VAN.5) | wallet | `out-of-scope` | Obligation falls on whichever party mints the Key Attestation JWT's `key_storage`/`user_authentication` values (the Wallet Provider); foundry-issuer only consumes `attested_keys` and never inspects or emits these optional vocabulary fields |  |
| VCI-0191 | Attack Potential Resistance (L2545) | `iso_18045_moderate` MUST be used when resistant to attack potential Moderate (VAN.4) | wallet | `out-of-scope` | Same reasoning as VCI-0190 |  |
| VCI-0192 | Attack Potential Resistance (L2546) | `iso_18045_enhanced-basic` MUST be used when resistant to attack potential Enhanced-Basic (VAN.3) | wallet | `out-of-scope` | Same reasoning as VCI-0190 |  |
| VCI-0193 | Attack Potential Resistance (L2547) | `iso_18045_basic` MUST be used when resistant to attack potential Basic (VAN.2) | wallet | `out-of-scope` | Same reasoning as VCI-0190 |  |
| VCI-0194 | Attack Potential Resistance (L2549) | Specifications extending the resistance list MUST choose collision-resistant values | other | `out-of-scope` | Obligation falls on specification authors, not on an implementation |  |
| VCI-0195 | Wallet Attestation (L2600) | Wallet MUST generate a proof of possession per Client Attestation PoP JWT | wallet | `out-of-scope` | Obligation falls on the Wallet or a third party, not on foundry's issuer/verifier surface |  |
| VCI-0196 | Proof Types (L2610) | A `jwt` proof object MUST include a `jwt` parameter whose value is a non-empty array of JWTs | issuer | `conforming` | `handle_credential_request` (credential.rs) rejects an empty `proofs.jwt` array the same way it rejects a missing `proofs` object entirely | vci_0196_proofs_jwt_array_must_be_non_empty |
| VCI-0197 | Proof Types (L2611) | A `di_vp` proof object MUST include a `di_vp` parameter whose value is a non-empty array of W3C Verifiable Presentations | issuer | `out-of-scope` | Excluded 2026-07-31 by scope decision: foundry implements only the `jwt` proof type (`crates/foundry-issuer/src/proof.rs`); the `di_vp` clause set would adjudicate uniformly to not-implemented |  |
| VCI-0198 | Proof Types (L2612) | An `attestation` proof object MUST include an `attestation` parameter containing exactly one key attestation JWT | issuer | `not-implemented` | `ProofsRequest` (proof.rs) has only a `jwt: Vec<String>` field; the top-level `attestation` proof type is not accepted at all, per the module's own doc comment ("`di_vp` and `attestation` proof types are intentionally not accepted") |  |
| VCI-0199 | jwt Proof Type (L2625) | The proof JWT MUST contain the header and payload elements defined for the `jwt` proof type | issuer | `gap` | GAP-VCI-05 | vci_0199_0209_0224_proof_jwt_without_iat_is_rejected |
| VCI-0200 | jwt Proof Type (L2628) | Proof `alg` is REQUIRED and MUST NOT be `none` or a symmetric algorithm | issuer | `conforming` | `verify_holder_proof` (proof.rs) always builds an ES256 verifier from the resolved JWK; `proof_signing_alg_values_supported` (metadata.rs) is hardcoded to `["ES256"]`, so any other `alg` — including `none` or a symmetric MAC — fails signature verification | vci_0200_0212_0226_haip_0090_proof_alg_must_be_es256 |
| VCI-0201 | jwt Proof Type (L2629) | Proof `typ` is REQUIRED and MUST be `openid4vci-proof+jwt` | issuer | `conforming` | `verify_holder_proof` (proof.rs) requires the header `typ` to equal `openid4vci-proof+jwt` | vci_0201_0225_proof_requires_correct_typ_header |
| VCI-0202 | jwt Proof Type (L2630) | `kid` MUST NOT be present if `jwk` or `x5c` is present | issuer | `conforming` | `verify_holder_proof` (proof.rs) requires exactly one of the `jwk`/`kid`/`x5c` header claims to be present | vci_0202_0203_0204_proof_header_key_fields_are_mutually_exclusive |
| VCI-0203 | jwt Proof Type (L2631) | `jwk` MUST NOT be present if `kid` or `x5c` is present | issuer | `conforming` | Same mechanism as VCI-0202 | vci_0202_0203_0204_proof_header_key_fields_are_mutually_exclusive |
| VCI-0204 | jwt Proof Type (L2632) | `x5c` MUST NOT be present if `kid` or `jwk` is present | issuer | `conforming` | Same mechanism as VCI-0202 | vci_0202_0203_0204_proof_header_key_fields_are_mutually_exclusive |
| VCI-0205 | jwt Proof Type (L2633) | When a `c_nonce` was provided, the `nonce` claim in a header key attestation MUST be set to that `c_nonce` | issuer | `conforming` | `verify_holder_proof` (proof.rs) independently validates both the outer proof's `nonce` and the `key_attestation`'s `nonce` as authentic minted `c_nonce`s, and additionally rejects the pair when they differ | vci_0205_proof_nonce_must_match_key_attestation_nonce |
| VCI-0206 | jwt Proof Type (L2634) | When `trust_chain` is used for signature verification the `kid` header parameter MUST be present | issuer | `not-implemented` | `verify_holder_proof` (proof.rs) never reads a `trust_chain` header claim; only the `jwk` and `kid`+`key_attestation` key-identification paths are implemented |  |
| VCI-0207 | jwt Proof Type (L2637) | `iss` MUST be the `client_id` of the Client, and MUST be omitted when the access token came from anonymous pre-authorized code access | issuer | `gap` | GAP-VCI-06 | vci_0207_proof_iss_must_be_omitted_after_anonymous_pre_auth_access |
| VCI-0208 | jwt Proof Type (L2638) | `aud` is REQUIRED and MUST be the Credential Issuer Identifier | issuer | `conforming` | `verify_holder_proof` (proof.rs) requires the payload `aud` claim to equal the configured Credential Issuer identifier | vci_0208_proof_aud_mismatch_is_rejected |
| VCI-0209 | jwt Proof Type (L2639) | `iat` is REQUIRED and MUST be the time the key proof was issued | issuer | `gap` | GAP-VCI-05 | vci_0199_0209_0224_proof_jwt_without_iat_is_rejected |
| VCI-0210 | jwt Proof Type (L2640) | `nonce` MUST be present when the Issuer has a Nonce Endpoint and MUST carry the server-provided `c_nonce` | issuer | `conforming` | `verify_holder_proof` (proof.rs) requires the `nonce` claim unconditionally and validates it via `verify_nonce` against this issuer's minted `c_nonce`s; the issuer always exposes a Nonce Endpoint (see HAIP-0021) | rejects_nonce_not_minted_by_this_issuer |
| VCI-0211 | jwt Proof Type (L2642) | The Credential Issuer MUST validate that the proof JWT is signed by the key identified in the JOSE header via `kid`, `jwk` or `x5c` | issuer | `conforming` | For the `jwk` and `kid`+`key_attestation` paths, `verify_holder_proof` (proof.rs) builds an ES256 verifier from the resolved key and calls `decode_with_verifier`. The `x5c` path is accepted at the header-shape (mutual-exclusivity) level but always rejected with "not yet supported" — not-implemented for that specific mechanism; no interop impact, since the wallet this issuer serves never emits `x5c` proofs (proof.rs module doc comment) | verifies_valid_proof_jwt |
| VCI-0212 | jwt Proof Type (L2647) | The proof `alg`, and the `alg` of `key_attestation` and `trust_chain` when present, MUST match `proof_signing_alg_values_supported` | issuer | `conforming` | Same mechanism as VCI-0200: `proof_signing_alg_values_supported` (metadata.rs) is hardcoded to `["ES256"]` and only ES256-signed material ever verifies | vci_0200_0212_0226_haip_0090_proof_alg_must_be_es256 |
| VCI-0213 | di_vp Proof Type (L2704) | A Data Integrity secured W3C VP used as key proof MUST contain the defined properties | issuer | `out-of-scope` | Excluded 2026-07-31 by scope decision: foundry implements only the `jwt` proof type (`crates/foundry-issuer/src/proof.rs`); the `di_vp` clause set would adjudicate uniformly to not-implemented |  |
| VCI-0214 | di_vp Proof Type (L2706) | `holder`, when present, MUST equal the controller identifier of the `proof.verificationMethod` | issuer | `out-of-scope` | Excluded 2026-07-31 by scope decision: foundry implements only the `jwt` proof type (`crates/foundry-issuer/src/proof.rs`); the `di_vp` clause set would adjudicate uniformly to not-implemented |  |
| VCI-0215 | di_vp Proof Type (L2707) | `proof` is REQUIRED and MUST be a Data Integrity Proof | issuer | `out-of-scope` | Excluded 2026-07-31 by scope decision: foundry implements only the `jwt` proof type (`crates/foundry-issuer/src/proof.rs`); the `di_vp` clause set would adjudicate uniformly to not-implemented |  |
| VCI-0216 | di_vp Proof Type (L2708) | `cryptosuite` is REQUIRED and MUST match `proof_signing_alg_values_supported` | issuer | `out-of-scope` | Excluded 2026-07-31 by scope decision: foundry implements only the `jwt` proof type (`crates/foundry-issuer/src/proof.rs`); the `di_vp` clause set would adjudicate uniformly to not-implemented |  |
| VCI-0217 | di_vp Proof Type (L2709) | `proofPurpose` is REQUIRED and MUST be `authentication` | issuer | `out-of-scope` | Excluded 2026-07-31 by scope decision: foundry implements only the `jwt` proof type (`crates/foundry-issuer/src/proof.rs`); the `di_vp` clause set would adjudicate uniformly to not-implemented |  |
| VCI-0218 | di_vp Proof Type (L2710) | `domain` is REQUIRED and MUST be the Credential Issuer Identifier | issuer | `out-of-scope` | Excluded 2026-07-31 by scope decision: foundry implements only the `jwt` proof type (`crates/foundry-issuer/src/proof.rs`); the `di_vp` clause set would adjudicate uniformly to not-implemented |  |
| VCI-0219 | di_vp Proof Type (L2711) | `challenge` is REQUIRED when a `c_nonce` was provided and MUST NOT be used otherwise | issuer | `out-of-scope` | Excluded 2026-07-31 by scope decision: foundry implements only the `jwt` proof type (`crates/foundry-issuer/src/proof.rs`); the `di_vp` clause set would adjudicate uniformly to not-implemented |  |
| VCI-0220 | di_vp Proof Type (L2713) | The Credential Issuer MUST validate the W3C VP proof is signed with a key held by the Holder | issuer | `out-of-scope` | Excluded 2026-07-31 by scope decision: foundry implements only the `jwt` proof type (`crates/foundry-issuer/src/proof.rs`); the `di_vp` clause set would adjudicate uniformly to not-implemented |  |
| VCI-0221 | di_vp Proof Type (L2715) | Additional properties not understood MUST be ignored | issuer | `out-of-scope` | Excluded 2026-07-31 by scope decision: foundry implements only the `jwt` proof type (`crates/foundry-issuer/src/proof.rs`); the `di_vp` clause set would adjudicate uniformly to not-implemented |  |
| VCI-0222 | attestation Proof Type (L2756) | When the Issuer has a Nonce Endpoint the `c_nonce` MUST be provided in the key attestation `nonce` | issuer | `not-implemented` | The top-level `attestation` proof type is not accepted at all (see VCI-0198); its `nonce` requirement is therefore moot |  |
| VCI-0223 | attestation Proof Type (L2772) | The key attestation `alg` MUST match one of `proof_signing_alg_values_supported` | issuer | `not-implemented` | The top-level `attestation` proof type is not accepted at all (see VCI-0198); its `alg` requirement is therefore moot |  |
| VCI-0224 | Verifying Proof (L2777) | All required claims for the proof type MUST be present | issuer | `gap` | GAP-VCI-05 | vci_0199_0209_0224_proof_jwt_without_iat_is_rejected |
| VCI-0225 | Verifying Proof (L2778) | The key proof MUST be explicitly typed using the header parameters defined for its proof type | issuer | `conforming` | Same mechanism as VCI-0201 | vci_0201_0225_proof_requires_correct_typ_header |
| VCI-0226 | Verifying Proof (L2779) | The `alg` header MUST indicate a registered asymmetric signature algorithm, MUST NOT be `none`, and MUST be supported and acceptable per local policy | issuer | `conforming` | Same mechanism as VCI-0200 | vci_0200_0212_0226_haip_0090_proof_alg_must_be_es256 |
| VCI-0227 | Verifying Proof (L2780) | The signature on the key proof MUST verify with the public key in the header parameter | issuer | `conforming` | `verify_holder_proof` (proof.rs) calls `decode_with_verifier` using the ES256 verifier derived from the header-identified key | verifies_valid_proof_jwt |
| VCI-0228 | Verifying Proof (L2781) | The header parameter MUST NOT contain a private key | issuer | `conforming` | A `jwk` header carrying the private key (`d`) fails ES256 verifier construction in `verify_holder_proof` (proof.rs) via josekit's `verifier_from_jwk` | vci_0228_proof_jwk_header_must_not_contain_private_key |
| VCI-0229 | Verifying Proof (L2782) | When the server has a Nonce Endpoint the nonce in the key proof MUST match the server-provided `c_nonce` | issuer | `conforming` | Same mechanism as VCI-0210 | rejects_nonce_not_minted_by_this_issuer |
| VCI-0230 | Verifying Proof (L2783) | The key proof creation time MUST be within an acceptable window | issuer | `conforming` | Satisfied via the nonce-based alternative the clause explicitly allows ("as determined by either the issuance time, or a server managed timestamp via the nonce claim"): `verify_nonce` (nonce.rs) enforces the minted `c_nonce`'s TTL. The `iat`-based determination is unimplemented (see VCI-0209/GAP-VCI-05) but the clause's disjunction is satisfied by the nonce path alone | rejects_expired_nonce |

| VCI-0231 | Wallet Attestation (L2555) | The Authorization Server MUST verify that the Wallet Attestation is signed by an issuer that the Credential Issuer trusts for this purpose | issuer | `conforming` | `handle_token_request` (token.rs) now builds a `TrustStore` from `issuer.wallet_attestation.trusted_anchors` and `validate_wallet_attestation_jwt` (attestation.rs) calls `validate_chain` against it before accepting any attestation | rejects_attestation_signed_by_an_untrusted_anchor |
| VCI-0232 | Wallet Attestation (L2600) | The Wallet's Client Attestation PoP JWT (`draft-ietf-oauth-attestation-based-client-auth` §5.2) MUST be verified by the Authorization Server as part of accepting the Wallet Attestation | issuer | `conforming` | Closed 2026-08-01 (GAP-VCI-14): `validate_client_attestation_pop_jwt` (attestation.rs) verifies all 9 ABCA §5.2 checks (JWS structure, `typ`, `alg == ES256`, signature against the attestation's `cnf.jwk`, `iss == sub`, `aud` exact match, `jti` presence, `iat` sliding window, `nbf` skew), and `claim_pop_jti` enforces `jti` anti-replay (§10.6/§12.1) via `Storage::insert_kv_if_absent` keyed on a hash of `(iss, jti)`. Supporting ABCA rules are enforced alongside: §9 rule 6 (`cnf.jwk` MUST NOT be a private key) in `validate_wallet_attestation_jwt`, and §6.2 rules 1–2 (each header MUST appear *precisely once*) in `token_handler`'s `exactly_one_header` (server.rs). `handle_token_request` (token.rs) wires the PoP path in, rejecting a present Wallet Attestation with no accompanying PoP (§6.2 rule 2) under both `Mode::Required` and `Mode::Optional` | vci_0232_rejects_a_wallet_attestation_presented_without_a_pop_jwt |

## Clause Inventory — OpenID4VP

| ID | § | Requirement | Applies to | Verdict | Evidence | Test |
|---|---|---|---|---|---|---|
| VP-0001 | Authorization Request (L268) | Verifiers MUST include the `typ` Header Parameter in Request Objects with the value `oauth-authz-req+jwt` | verifier | `conforming` | `build_signed_request_object` (request.rs) sets the header `typ` to the literal `oauth-authz-req+jwt` for every signed Request Object | test_build_signed_request_object_and_verify_jws |
| VP-0002 | Authorization Request (L268) | Wallets MUST NOT process Request Objects whose `typ` Header Parameter is absent or not `oauth-authz-req+jwt` | wallet | `out-of-scope` | Obligation falls on the Wallet or a third party, not on foundry's issuer/verifier surface |  |
| VP-0003 | Authorization Request (L270) | If an `iss` claim is present in the Request Object the Wallet MUST ignore it | wallet | `out-of-scope` | Obligation falls on the Wallet or a third party, not on foundry's issuer/verifier surface |  |
| VP-0004 | Authorization Request (L279) | Wallets MUST process the DCQL query and select candidate Credentials using the defined evaluation process | wallet | `out-of-scope` | Obligation falls on the Wallet or a third party, not on foundry's issuer/verifier surface |  |
| VP-0005 | Authorization Request (L286) | The Wallet MUST ignore any unrecognized request parameters, other than `transaction_data` | wallet | `out-of-scope` | Obligation falls on the Wallet or a third party, not on foundry's issuer/verifier surface |  |
| VP-0006 | Authorization Request (L287) | Wallets that do not support `transaction_data` MUST reject requests that contain it | wallet | `out-of-scope` | Obligation falls on the Wallet or a third party, not on foundry's issuer/verifier surface |  |
| VP-0007 | Authorization Request / New Parameters (L295) | Either `dcql_query` or a `scope` representing a DCQL query MUST be present in the Authorization Request, but not both | verifier | `conforming` | `CreateVerificationRequest` (request.rs) has no `scope` field at all; `dcql_query` (or the foundry-proprietary `named_query_ref` alias, resolved to a `dcql_query` before the request is ever built) is always present, so the disjunction is trivially and permanently satisfied on the `dcql_query` side | test_create_verification_request_dcql |
| VP-0008 | Authorization Request / New Parameters (L300) | `client_metadata` MUST be UTF-8 encoded | verifier | `conforming` | `client_metadata` is always built as a `serde_json::Value` and serialized with `serde_json::to_vec`/JSON serialization, which is UTF-8 by construction | test_dc_api_client_metadata_encryption_jwk_is_wallet_selectable |
| VP-0009 | Authorization Request / New Parameters (L302) | Public keys in `client_metadata.jwks` MUST NOT be used to verify the signature of signed Authorization Requests | wallet | `out-of-scope` | Obligation falls on the Wallet or a third party, not on foundry's issuer/verifier surface |  |
| VP-0010 | Authorization Request / New Parameters (L302) | Each JWK in `client_metadata.jwks` MUST have a `kid` that uniquely identifies the key within the request | verifier | `conforming` | `annotate_encryption_jwk` (request.rs) sets a fresh UUID `kid` on the sole ephemeral encryption JWK in `jwks.keys`; with exactly one key ever present, uniqueness within the request is automatic | test_dc_api_client_metadata_encryption_jwk_is_wallet_selectable, test_signed_request_object_encryption_jwk_is_wallet_selectable |
| VP-0011 | Authorization Request / New Parameters (L303) | When a `response_mode` requiring encryption is used, `encrypted_response_enc_values_supported` MUST be present for any value other than the default `A128GCM` | verifier | `conforming` | `response_encryption_params` (request.rs) always populates `encrypted_response_enc_values_supported`, a superset of "present for any non-default value" (this specification's own text separately SHOULDs it absent for the default value, which foundry does not do, but that is a SHOULD, not a MUST) | test_client_metadata_response_encryption_honours_config |
| VP-0012 | Authorization Request / New Parameters (L304) | `vp_formats_supported` in `client_metadata` is REQUIRED when not available to the Wallet via another mechanism | verifier | `conforming` | `vp_formats_supported` (request.rs) is always included in `client_metadata`, for both `request_uri` and `dc_api` transports | test_client_metadata_advertises_vp_formats_supported |
| VP-0013 | Authorization Request / New Parameters (L308) | Metadata parameters other than those defined MUST be ignored unless a profile explicitly permits them in `client_metadata` | wallet | `out-of-scope` | Obligation falls on the Wallet or a third party, not on foundry's issuer/verifier surface |  |
| VP-0014 | Authorization Request / New Parameters (L311) | If `request_uri_method` is `get` the Wallet MUST retrieve the Request Object using HTTP GET | wallet | `out-of-scope` | Obligation falls on the Wallet or a third party, not on foundry's issuer/verifier surface |  |
| VP-0015 | Authorization Request / New Parameters (L311) | If `request_uri_method` is `post` a supporting Wallet MUST send the request using HTTP POST | wallet | `out-of-scope` | Obligation falls on the Wallet or a third party, not on foundry's issuer/verifier surface |  |
| VP-0016 | Authorization Request / New Parameters (L311) | If `request_uri_method` is absent the Wallet MUST process `request_uri` as defined in RFC9101 | wallet | `out-of-scope` | Obligation falls on the Wallet or a third party, not on foundry's issuer/verifier surface |  |
| VP-0017 | Authorization Request / New Parameters (L311) | `request_uri_method` MUST NOT be present if a `request_uri` parameter is not present | verifier | `conforming` | `request_uri_method` (the HTTP POST retrieval variant, see VP-0076) is not implemented at all and is never emitted by `request.rs` in any transport, so it can never be present without `request_uri` | test_create_verification_request_dcql |
| VP-0018 | Authorization Request / New Parameters (L317) | The Wallet MUST return an error if a request contains an unrecognized transaction data type or transaction data not conforming to its type definition | wallet | `out-of-scope` | Obligation falls on the Wallet or a third party, not on foundry's issuer/verifier surface |  |
| VP-0019 | Authorization Request / New Parameters (L319) | `transaction_data[].type` is REQUIRED | verifier | `conforming` | `encode_transaction_data` (request.rs) rejects any entry whose `type` is absent or empty | test_transaction_data_requires_type_and_credential_ids |
| VP-0020 | Authorization Request / New Parameters (L320) | `transaction_data[].credential_ids` is REQUIRED and non-empty | verifier | `conforming` | `encode_transaction_data` (request.rs) rejects any entry whose `credential_ids` is absent or empty | test_transaction_data_requires_type_and_credential_ids |
| VP-0021 | Authorization Request / New Parameters (L320) | If `credential_ids` holds more than one element the Wallet MUST use only one of the referenced Credentials for transaction authorization | wallet | `out-of-scope` | Obligation falls on the Wallet or a third party, not on foundry's issuer/verifier surface |  |
| VP-0022 | Authorization Request / New Parameters (L337) | `verifier_info[].format` is REQUIRED | verifier | `not-implemented` | `verifier_info` is not a field of `CreateVerificationRequest` and is never emitted anywhere in `request.rs` |  |
| VP-0023 | Authorization Request / New Parameters (L338) | `verifier_info[].data` is REQUIRED | verifier | `not-implemented` | Same evidence as VP-0022 |  |
| VP-0024 | Authorization Request / New Parameters (L338) | If the Wallet uses information from `verifier_info` it MUST validate the signature and ensure binding | wallet | `out-of-scope` | Obligation falls on the Wallet or a third party, not on foundry's issuer/verifier surface |  |
| VP-0025 | Authorization Request / Existing Parameters (L358) | `nonce` is REQUIRED in the Authorization Request | verifier | `conforming` | `create_verification_request` (request.rs) always sets `nonce` in both the `dc_api_obj` and (via `tx.nonce`) the signed Request Object payload | test_create_verification_request_dcql |
| VP-0026 | Authorization Request / Existing Parameters (L358) | The Verifier MUST create a fresh, cryptographically random `nonce` with sufficient entropy for every Authorization Request | verifier | `conforming` | `nonce = format!("vn_{}", Uuid::new_v4().simple())` (request.rs) — a UUID v4 carries 122 bits of randomness, and a fresh one is minted per request | vp_0026_0028_nonce_has_sufficient_entropy_and_is_ascii_url_safe |
| VP-0027 | Authorization Request / Existing Parameters (L358) | The Verifier MUST store the `nonce` with the current session and pass it in the `nonce` request parameter | verifier | `conforming` | `nonce` is stored on `VerificationTransaction.nonce` via `save_verification_transaction` and the identical value is emitted in the request payload | test_create_verification_request_dcql |
| VP-0028 | Authorization Request / Existing Parameters (L358) | `nonce` values MUST only contain ASCII URL-safe characters | verifier | `conforming` | `"vn_"` plus 32 lowercase hex characters (UUID v4 simple form) is composed entirely of ASCII letters, digits and underscore | vp_0026_0028_nonce_has_sufficient_entropy_and_is_ascii_url_safe |
| VP-0029 | Authorization Request / Existing Parameters (L364) | `response_mode` is REQUIRED | verifier | `conforming` | `response_mode` (request.rs) is always set to `direct_post.jwt` or `dc_api.jwt` depending on transport | test_create_verification_request_dcql |
| VP-0030 | Authorization Request / Existing Parameters (L367) | `client_id` is REQUIRED | verifier | `conforming` | `client_id` (request.rs) is always derived as `x509_san_dns:<host>` and included in both the `openid4vp://` URI and the signed Request Object | test_build_signed_request_object_and_verify_jws |
| VP-0031 | Authorization Request / Existing Parameters (L370) | `state` is REQUIRED when at least one Presentation without Holder Binding is requested | verifier | `conforming` | `state` (request.rs) is set to `tx.id` unconditionally, regardless of whether a Presentation without Holder Binding is requested — a strict superset of the conditional requirement | test_build_signed_request_object_and_verify_jws |
| VP-0032 | Authorization Request / Existing Parameters (L370) | `state` values MUST only contain ASCII URL-safe characters | verifier | `conforming` | `state = tx.id = "v_" + <UUID v4 simple>` (request.rs), composed entirely of ASCII letters, digits and underscore | test_build_signed_request_object_and_verify_jws |
| VP-0033 | Requesting Presentations without Holder Binding Proofs (L396) | The Verifier MUST include a `state` parameter in the Authorization Request | verifier | `not-implemented` | Scoped to Presentations without Holder Binding: `require_cryptographic_holder_binding` (the DCQL parameter that requests this) is not a field of `DcqlCredentialQuery` (dcql_model.rs), so this nkb-specific flow can never be triggered. `state` is, independently, always present regardless (see VP-0031, `conforming`) |  |
| VP-0034 | Requesting Presentations without Holder Binding Proofs (L398) | The Verifier MUST ensure `state` is a cryptographically strong pseudo-random number with at least 128 bits of entropy | verifier | `not-implemented` | Scoped to Presentations without Holder Binding: `require_cryptographic_holder_binding` (the DCQL parameter that requests this) is not a field of `DcqlCredentialQuery` (dcql_model.rs), so this nkb-specific flow can never be triggered |  |
| VP-0035 | Requesting Presentations without Holder Binding Proofs (L400) | The Verifier MUST ensure `state` is chosen fresh for each Authorization Request | verifier | `not-implemented` | Same evidence as VP-0034 |  |
| VP-0036 | Requesting Presentations without Holder Binding Proofs (L401) | The Verifier MUST store `state` in the Verifier's session state | verifier | `not-implemented` | Same evidence as VP-0034 |  |
| VP-0037 | Requesting Presentations without Holder Binding Proofs (L402) | The Verifier MUST check that the same `state` value is returned in the Authorization Response | verifier | `not-implemented` | Same evidence as VP-0034 |  |
| VP-0038 | Using `scope` to Request Presentations (L494) | A `scope` value used to request presentations MUST be an alias for a well-defined DCQL query | verifier | `not-implemented` | The `scope`-based DCQL aliasing mechanism is not implemented: `CreateVerificationRequest` (request.rs) has no `scope` field, and the wallet-facing request never carries a `scope` parameter (see VP-0007) |  |
| VP-0039 | Using `scope` to Request Presentations (L494) | Credential and claim identifiers within the DCQL queries associated with `scope` values MUST be unique | verifier | `not-implemented` | Same evidence as VP-0038 |  |
| VP-0040 | Response Type `vp_token` (L521) | When `vp_token` is the `response_type` a successful response MUST include the `vp_token` parameter | wallet | `out-of-scope` | Obligation falls on the Wallet or a third party, not on foundry's issuer/verifier surface |  |
| VP-0041 | `aud` of a Request Object (L535) | The `aud` claim MUST equal the `iss` claim value when Dynamic Discovery is performed | verifier | `not-implemented` | Dynamic Discovery (via the `openid_federation` Client Identifier Prefix, VP-0048) is not implemented; foundry only ever performs Static Discovery via inline `client_metadata` |  |
| VP-0042 | `aud` of a Request Object (L536) | The `aud` claim MUST be `https://self-issued.me/v2` when Static Discovery metadata is used | verifier | `conforming` | `build_signed_request_object` (crates/foundry-verifier/src/request.rs) now inserts `"aud": "https://self-issued.me/v2"` into every Request Object payload -- correct unconditionally, since foundry never performs Dynamic Discovery (see VP-0041) | vp_0042_request_object_missing_aud_claim |
| VP-0043 | Client Identifier Prefix / Syntax (L557) | Wallets MUST use the presence of a `:` character and the content preceding it to determine whether a Client Identifier Prefix is used | wallet | `out-of-scope` | Obligation falls on the Wallet or a third party, not on foundry's issuer/verifier surface |  |
| VP-0044 | Client Identifier Prefix / Syntax (L557) | If the content before the first `:` is a recognized and supported prefix the Wallet MUST interpret the Client Identifier according to that prefix | wallet | `out-of-scope` | Obligation falls on the Wallet or a third party, not on foundry's issuer/verifier surface |  |
| VP-0045 | Client Identifier Prefix / Fallback (L567) | If no `:` character is present the Wallet MUST treat the Client Identifier as referencing a pre-registered client | wallet | `out-of-scope` | Obligation falls on the Wallet or a third party, not on foundry's issuer/verifier surface |  |
| VP-0046 | Client Identifier Prefix / Fallback (L573) | Pre-registered clients MUST NOT contain a `:` character preceded immediately by a supported Client Identifier Prefix value | verifier | `conforming` | `client_id` (request.rs) is always emitted as `x509_san_dns:<host>` — foundry never emits a bare pre-registered-style client id at all, so this constraint on the fallback case never applies to foundry's own output | vp_0085_client_id_matches_between_uri_and_request_object |
| VP-0047 | Defined Client Identifier Prefixes / `redirect_uri` (L581) | With the `redirect_uri` prefix all Verifier metadata parameters MUST be passed using the `client_metadata` parameter | verifier | `not-implemented` | The `redirect_uri` Client Identifier Prefix is not implemented; `client_id` (request.rs) is always `x509_san_dns:<host>` |  |
| VP-0048 | Defined Client Identifier Prefixes / `openid_federation` (L598) | With the `openid_federation` prefix the processing rules of OpenID Federation MUST be followed | verifier | `not-implemented` | The `openid_federation` Client Identifier Prefix is not implemented; same evidence as VP-0047 |  |
| VP-0049 | Defined Client Identifier Prefixes / `openid_federation` (L598) | With the `openid_federation` prefix a `client_metadata` parameter present in the request MUST be ignored | wallet | `out-of-scope` | Obligation falls on the Wallet or a third party, not on foundry's issuer/verifier surface |  |
| VP-0050 | Defined Client Identifier Prefixes / `decentralized_identifier` (L600) | With the `decentralized_identifier` prefix the request MUST be signed with a private key associated with the DID | verifier | `not-implemented` | The `decentralized_identifier` Client Identifier Prefix is not implemented; same evidence as VP-0047 |  |
| VP-0051 | Defined Client Identifier Prefixes / `decentralized_identifier` (L600) | The public key verifying the request MUST be obtained from the `verificationMethod` property of the DID Document | wallet | `out-of-scope` | Obligation falls on the Wallet or a third party, not on foundry's issuer/verifier surface |  |
| VP-0052 | Defined Client Identifier Prefixes / `decentralized_identifier` (L600) | The particular public key used to sign the request MUST be identified by the `kid` in the JOSE header | verifier | `not-implemented` | Same evidence as VP-0050 |  |
| VP-0053 | Defined Client Identifier Prefixes / `decentralized_identifier` (L600) | The Wallet MUST use DID Resolution defined by the DID method used by the Verifier to obtain the DID Document | wallet | `out-of-scope` | Obligation falls on the Wallet or a third party, not on foundry's issuer/verifier surface |  |
| VP-0054 | Defined Client Identifier Prefixes / `decentralized_identifier` (L600) | All Verifier metadata other than the public key MUST be obtained from the `client_metadata` parameter | wallet | `out-of-scope` | Obligation falls on the Wallet or a third party, not on foundry's issuer/verifier surface |  |
| VP-0055 | Defined Client Identifier Prefixes / `verifier_attestation` (L612) | The Client Identifier without the prefix MUST equal the `sub` claim of the Verifier Attestation JWT | verifier | `not-implemented` | The `verifier_attestation` Client Identifier Prefix is not implemented; same evidence as VP-0047 |  |
| VP-0056 | Defined Client Identifier Prefixes / `verifier_attestation` (L612) | The request MUST be signed with the private key corresponding to the public key in the `cnf` claim | verifier | `not-implemented` | Same evidence as VP-0055 |  |
| VP-0057 | Defined Client Identifier Prefixes / `verifier_attestation` (L612) | The Verifier Attestation JWT MUST be added to the `jwt` JOSE header of the request object | verifier | `not-implemented` | Same evidence as VP-0055 |  |
| VP-0058 | Defined Client Identifier Prefixes / `verifier_attestation` (L612) | The Wallet MUST validate the signature on the Verifier Attestation JWT | wallet | `out-of-scope` | Obligation falls on the Wallet or a third party, not on foundry's issuer/verifier surface |  |
| VP-0059 | Defined Client Identifier Prefixes / `verifier_attestation` (L612) | The `iss` of the Verifier Attestation JWT MUST identify a party the Wallet trusts for issuing them | wallet | `out-of-scope` | Obligation falls on the Wallet or a third party, not on foundry's issuer/verifier surface |  |
| VP-0060 | Defined Client Identifier Prefixes / `verifier_attestation` (L612) | If the Wallet cannot establish trust it MUST refuse the request | wallet | `out-of-scope` | Obligation falls on the Wallet or a third party, not on foundry's issuer/verifier surface |  |
| VP-0061 | Defined Client Identifier Prefixes / `verifier_attestation` (L612) | If the attestation carries a `redirect_uris` claim the Wallet MUST ensure `redirect_uri` exactly matches one of its entries | wallet | `out-of-scope` | Obligation falls on the Wallet or a third party, not on foundry's issuer/verifier surface |  |
| VP-0062 | Defined Client Identifier Prefixes / `verifier_attestation` (L612) | All Verifier metadata other than the public key MUST be obtained from the `client_metadata` parameter | wallet | `out-of-scope` | Obligation falls on the Wallet or a third party, not on foundry's issuer/verifier surface |  |
| VP-0063 | Defined Client Identifier Prefixes / `x509_san_dns` (L614) | The Client Identifier without the prefix MUST be a DNS name matching a `dNSName` SAN entry in the leaf certificate | verifier | `conforming` | `build_signed_request_object` now cross-checks the derived `client_id` host against the configured `x5c` leaf's dNSName SAN entries via `foundry_core::trust::match_san_dns`, rejecting the mismatch instead of silently signing a Request Object the wallet's own SAN check would reject | vp_0063_client_id_host_not_validated_against_x5c_certificate_san |
| VP-0064 | Defined Client Identifier Prefixes / `x509_san_dns` (L614) | The request MUST be signed with the private key of the leaf X.509 certificate carried in the `x5c` JOSE header | verifier | `conforming` | `build_signed_request_object` (request.rs) signs with `key_entry.private_key` and embeds `key_entry.x5c` from the same configured `KeyEntry`; the resulting JWS verifies against the public key of the x5c leaf's own private key | vp_0064_signing_key_and_x5c_chain_are_paired |
| VP-0065 | Defined Client Identifier Prefixes / `x509_san_dns` (L614) | The Wallet MUST validate the signature and the trust chain of the X.509 certificate | wallet | `out-of-scope` | Obligation falls on the Wallet or a third party, not on foundry's issuer/verifier surface |  |
| VP-0066 | Defined Client Identifier Prefixes / `x509_san_dns` (L614) | All Verifier metadata other than the public key MUST be obtained from the `client_metadata` parameter | wallet | `out-of-scope` | Obligation falls on the Wallet or a third party, not on foundry's issuer/verifier surface |  |
| VP-0067 | Defined Client Identifier Prefixes / `x509_san_dns` (L614) | Outside the DC API, if trust in the Client Identifier is not established the FQDN of `redirect_uri` MUST match the Client Identifier without the prefix | wallet | `out-of-scope` | Obligation falls on the Wallet or a third party, not on foundry's issuer/verifier surface |  |
| VP-0068 | Defined Client Identifier Prefixes / `x509_hash` (L616) | The Client Identifier without the prefix MUST be a hash matching the hash of the leaf certificate passed with the request | verifier | `conforming` | `build_signed_request_object` (request.rs) computes `client_id` as `x509_hash:<value>` where `<value>` is `foundry_core::trust::x509_hash_client_id_value(leaf_pem)` -- base64url(SHA-256(DER of the configured x5c leaf)), exactly matching the leaf embedded in the same request's `x5c` header | client_id_is_the_x509_hash_of_the_configured_leaf_certificate |
| VP-0069 | Defined Client Identifier Prefixes / `x509_hash` (L616) | The request MUST be signed with the private key of the leaf X.509 certificate carried in the `x5c` JOSE header | verifier | `conforming` | `build_signed_request_object` reads the signer and the `x5c` chain from the same configured `KeyEntry` (`verifier.signing_key`), so they are always paired -- same evidence as VP-0064 | vp_0064_signing_key_and_x5c_chain_are_paired |
| VP-0070 | Defined Client Identifier Prefixes / `x509_hash` (L616) | The Wallet MUST validate the signature and the trust chain of the X.509 leaf certificate | wallet | `out-of-scope` | Obligation falls on the Wallet or a third party, not on foundry's issuer/verifier surface |  |
| VP-0071 | Defined Client Identifier Prefixes / `x509_hash` (L616) | All Verifier metadata other than the public key MUST be obtained from the `client_metadata` parameter | wallet | `out-of-scope` | Obligation falls on the Wallet or a third party, not on foundry's issuer/verifier surface |  |
| VP-0072 | Defined Client Identifier Prefixes / `origin` (L618) | The Wallet MUST NOT accept the `origin` Client Identifier Prefix in requests | wallet | `out-of-scope` | Obligation falls on the Wallet or a third party, not on foundry's issuer/verifier surface |  |
| VP-0073 | Defined Client Identifier Prefixes (L620) | To use the `openid_federation`, `decentralized_identifier`, `verifier_attestation`, `x509_san_dns` or `x509_hash` prefixes Verifiers MUST be capable of securely storing private key material | verifier | `not-unit-testable` | Secure private key storage is an operational/deployment property (file permissions, HSM usage, secrets management) of the environment `config.keys[*].private_key` paths are read from, not a property expressible as a code-level assertion |  |
| VP-0074 | Request URI Method `post` (L628) | The request MUST use HTTP POST with the `https` scheme, content type `application/x-www-form-urlencoded`, and `Accept: application/oauth-authz-req+jwt` | wallet | `out-of-scope` | Obligation falls on the Wallet or a third party, not on foundry's issuer/verifier surface |  |
| VP-0075 | Request URI Method `post` (L628) | The names and values in the request body MUST be encoded using UTF-8 | wallet | `out-of-scope` | Obligation falls on the Wallet or a third party, not on foundry's issuer/verifier surface |  |
| VP-0076 | Request URI Method `post` (L636) | When a `wallet_nonce` is received the Verifier MUST use it as the `wallet_nonce` value in the signed authorization request object | verifier | `not-implemented` | `request_uri_method=post` and the associated `wallet_nonce` mechanism are not implemented anywhere in `request.rs`; the Request URI endpoint only ever supports a plain GET retrieval |  |
| VP-0077 | Request URI Method `post` (L640) | The Wallet MUST NOT include `request_object_signing_alg_values_supported` if the Client Identifier Prefix precludes signed Request Objects | wallet | `out-of-scope` | Obligation falls on the Wallet or a third party, not on foundry's issuer/verifier surface |  |
| VP-0078 | Request URI Method `post` (L643) | The Verifier MUST ignore any unrecognized parameters in the request to the Request URI endpoint | verifier | `not-unit-testable` | `get_request_object_handler` (crates/foundry/src/server.rs) extracts only the path segment `Path(id)`; it has no `Query<..>` extractor and never reads any query parameters or request body, so no parameter — recognized or not — can ever affect its behaviour. The evidence lives in the `foundry` crate's HTTP layer, not `request.rs`; an executable HTTP-level test belongs to the HTTP layer task (every row with Applies to = http), which this row is one clause short of being tagged as |  |
| VP-0079 | Request URI Method `post` / Request URI Response (L660) | The Request URI response MUST be an HTTP response with content type `application/oauth-authz-req+jwt` whose body is a signed, optionally encrypted, request object | http | `conforming` | `get_request_object_handler` (server.rs) returns `build_signed_request_object`'s compact JWS string with header `Content-Type: application/oauth-authz-req+jwt`, exactly the shape `full_verification_flow_end_to_end` (wallet_verification.rs) asserts on the real `GET /vp/request/{id}` HTTP response before parsing the JWS's three dot-separated parts | full_verification_flow_end_to_end |
| VP-0080 | Request URI Method `post` / Request URI Response (L660) | The request object MUST fulfill the requirements defined for Authorization Requests | verifier | `not-unit-testable` | Composite/redundant clause: restates that the retrieved Request Object must satisfy the Authorization Request rules already adjudicated individually above (VP-0001, VP-0025 through VP-0032, etc.) — not an independent requirement with its own executable assertion |  |
| VP-0081 | Request URI Method `post` / Request URI Response (L677) | The Wallet MUST process the request as defined in RFC9101 | wallet | `out-of-scope` | Obligation falls on the Wallet or a third party, not on foundry's issuer/verifier surface |  |
| VP-0082 | Request URI Method `post` / Request URI Response (L677) | If the Wallet passed a `wallet_nonce` it MUST validate the request object carries that value in a `wallet_nonce` claim and MUST terminate processing otherwise | wallet | `out-of-scope` | Obligation falls on the Wallet or a third party, not on foundry's issuer/verifier surface |  |
| VP-0083 | Request URI Method `post` / Request URI Response (L679) | The Wallet MUST extract the set of Authorization Request parameters from the Request Object | wallet | `out-of-scope` | Obligation falls on the Wallet or a third party, not on foundry's issuer/verifier surface |  |
| VP-0084 | Request URI Method `post` / Request URI Response (L679) | The Wallet MUST only use the parameters in the Request Object even if the same parameter was supplied as a query parameter | wallet | `out-of-scope` | Obligation falls on the Wallet or a third party, not on foundry's issuer/verifier surface |  |
| VP-0085 | Request URI Method `post` / Request URI Response (L679) | The `client_id` request parameter and the Request Object `client_id` claim MUST be identical, including the Client Identifier Prefix | verifier | `conforming` | Both the `openid4vp://` URI's `client_id` query parameter and the Request Object's `client_id` claim are derived independently but identically, from `dns_host_only(public_base_url)`, in `create_verification_request` and `build_signed_request_object` respectively | vp_0085_client_id_matches_between_uri_and_request_object |
| VP-0086 | Request URI Method `post` / Request URI Response (L679) | If any of these conditions are not met the Wallet MUST terminate request processing | wallet | `out-of-scope` | Obligation falls on the Wallet or a third party, not on foundry's issuer/verifier surface |  |
| VP-0087 | Request URI Method `post` / Request URI Error Response (L685) | If the Verifier responds with any HTTP error response the Wallet MUST terminate the process | wallet | `out-of-scope` | Obligation falls on the Wallet or a third party, not on foundry's issuer/verifier surface |  |
| VP-0088 | Verifier Info / Proof of Possession (L708) | The Wallet MUST validate proofs of possession where the profile defines them and ignore or reject attachments that fail validation | wallet | `out-of-scope` | Obligation falls on the Wallet or a third party, not on foundry's issuer/verifier surface |  |
| VP-0089 | DCQL (L723) | `credentials` is REQUIRED and MUST be a non-empty array of Credential Queries | verifier | `conforming` | `DcqlQuery.credentials` deserializes via `non_empty_credentials`, which rejects an empty array; `create_verification_request` (request.rs) parses and validates the query before ever persisting it | rejects_empty_credentials, create_rejects_empty_credentials_dcql_query |
| VP-0090 | DCQL (L731) | Implementations MUST ignore any unknown properties in the DCQL data structure | verifier | `conforming` | `DcqlQuery`/`DcqlCredentialQuery`/`DcqlClaimsQuery` (dcql_model.rs) carry no `deny_unknown_fields`, so `multiple`, `trusted_authorities`, `credential_sets`, `claim_sets`, and any future top-level or nested property are silently ignored rather than rejected | ignores_unknown_properties_at_every_level |
| VP-0091 | DCQL / Credential Query (L739) | Each entry in `credentials` MUST be an object with the defined properties | verifier | `conforming` | A non-object `credentials` entry fails `DcqlCredentialQuery` deserialization; `check_dcql_match` reports a failed check rather than panicking | vp_0091_0105_0109_malformed_dcql_shapes_fail_closed |
| VP-0092 | DCQL / Credential Query (L742) | `id` is REQUIRED in a Credential Query | verifier | `conforming` | `id: String` (dcql_model.rs) has no `#[serde(default)]`, so a missing `id` fails deserialization | rejects_missing_required_members |
| VP-0093 | DCQL / Credential Query (L743) | A Credential Query `id` MUST be a non-empty string of alphanumeric, underscore or hyphen characters | verifier | `gap` | See GAP-VP-03 | vp_0093_0094_0096_0097_dcql_structural_constraints_not_validated |
| VP-0094 | DCQL / Credential Query (L745) | Within one Authorization Request the same Credential Query `id` MUST NOT be present more than once | verifier | `gap` | See GAP-VP-03 | vp_0093_0094_0096_0097_dcql_structural_constraints_not_validated |
| VP-0095 | DCQL / Credential Query (L749) | `format` is REQUIRED in a Credential Query | verifier | `conforming` | `format: CredentialFormat` (dcql_model.rs) has no `#[serde(default)]`, so a missing `format` fails deserialization | rejects_missing_required_members |
| VP-0096 | DCQL / Credential Query (L756) | `meta` is REQUIRED in a Credential Query | verifier | `gap` | See GAP-VP-03 | vp_0093_0094_0096_0097_dcql_structural_constraints_not_validated |
| VP-0097 | DCQL / Credential Query (L780) | Verifiers MUST NOT point to the same claim more than once in a single query | verifier | `gap` | See GAP-VP-03 | vp_0093_0094_0096_0097_dcql_structural_constraints_not_validated |
| VP-0098 | DCQL / Trusted Authorities Query (L801) | Each entry in `trusted_authorities` MUST be an object with the defined properties | verifier | `not-implemented` | `trusted_authorities` is not modelled at all by `dcql_model.rs` (see the module's own doc comment); it deserializes as an ignored unknown property (VP-0090) and is never read by `dcql.rs` |  |
| VP-0099 | DCQL / Trusted Authorities Query (L804) | `type` is REQUIRED in a Trusted Authorities Query | verifier | `not-implemented` | Same evidence as VP-0098 |  |
| VP-0100 | DCQL / Trusted Authorities Query (L808) | `values` is REQUIRED and MUST be a non-empty array of strings | verifier | `not-implemented` | Same evidence as VP-0098 |  |
| VP-0101 | DCQL / Authority Key Identifier (L826) | The raw byte representation of the value MUST match the AuthorityKeyIdentifier of an X.509 certificate in the chain present in the Credential | verifier | `not-implemented` | Same evidence as VP-0098; the `aki` Trusted Authorities Query type has no matching logic anywhere in `foundry-verifier` |  |
| VP-0102 | DCQL / ETSI Trusted List (L848) | The trust chain of a matching Credential MUST contain at least one X.509 certificate matching an entry of the Trusted List or its cascading lists | verifier | `not-implemented` | Same evidence as VP-0098; the `etsi_tl` Trusted Authorities Query type has no matching logic anywhere in `foundry-verifier` |  |
| VP-0103 | DCQL / Credential Set Query (L884) | Each entry in `credential_sets` MUST be an object with the defined properties | verifier | `not-implemented` | `credential_sets` is not modelled at all by `dcql_model.rs` (see the module's own doc comment); it deserializes as an ignored unknown property (VP-0090) and `dcql.rs`'s own doc comment states multi-credential/`credential_sets` combination logic is out of scope for this phase |  |
| VP-0104 | DCQL / Credential Set Query (L887) | `options` is REQUIRED and MUST be a non-empty array of non-empty arrays of Credential Query identifiers | verifier | `not-implemented` | Same evidence as VP-0103 |  |
| VP-0105 | DCQL / Claims Query (L900) | Each entry in `claims` MUST be an object with the defined properties | verifier | `conforming` | A non-object `claims` entry fails `DcqlClaimsQuery` deserialization; `check_dcql_match` reports a failed check rather than panicking | vp_0091_0105_0109_malformed_dcql_shapes_fail_closed |
| VP-0106 | DCQL / Claims Query (L903) | A Claims Query `id` is REQUIRED when `claim_sets` is present in the Credential Query | verifier | `not-implemented` | `DcqlClaimsQuery` (dcql_model.rs) has no `id` field at all, and `claim_sets` (which this requirement is conditioned on) is not modelled either (VP-0103) — the correlation mechanism this clause governs does not exist |  |
| VP-0107 | DCQL / Claims Query (L904) | A Claims Query `id` MUST be a non-empty string of alphanumeric, underscore or hyphen characters | verifier | `not-implemented` | Same evidence as VP-0106 |  |
| VP-0108 | DCQL / Claims Query (L906) | Within one `claims` array the same `id` MUST NOT be present more than once | verifier | `not-implemented` | Same evidence as VP-0106 |  |
| VP-0109 | DCQL / Claims Query (L910) | `path` is REQUIRED and MUST be a non-empty array representing a claims path pointer | verifier | `conforming` | `path: Vec<ClaimsPathSegment>` (dcql_model.rs) has no `#[serde(default)]` (required) and deserializes via `non_empty_path` (non-empty) | rejects_empty_claims_path, vp_0091_0105_0109_malformed_dcql_shapes_fail_closed |
| VP-0110 | DCQL / Claims Query (L920) | For value matching against an ISO mdoc Credential the CBOR value MUST first be converted to JSON following RFC8949 Section 6.1 | verifier | `conforming` | The conversion itself is `foundry_mdoc::verifier::cbor_value_to_json`, called upstream of `check_dcql_match` in `verify.rs`; `dcql.rs`'s own `values` matching correctly compares against the JSON types (booleans, integers, strings) that conversion produces | vp_0110_mdoc_value_matching_matches_converted_json_types |
| VP-0111 | DCQL / Selecting Claims and Credentials (L931) | Wallets MUST NOT send selectively disclosable claims that have not been selected according to the selection rules | wallet | `out-of-scope` | Obligation falls on the Wallet or a third party, not on foundry's issuer/verifier surface |  |
| VP-0112 | DCQL / Selecting Claims (L942) | If `claims` is absent the Wallet MUST return only the claims that are mandatory to present | wallet | `out-of-scope` | Obligation falls on the Wallet or a third party, not on foundry's issuer/verifier surface |  |
| VP-0113 | DCQL / Selecting Claims (L951) | If the Wallet cannot satisfy any option in `claim_sets` it MUST NOT return any claims | wallet | `out-of-scope` | Obligation falls on the Wallet or a third party, not on foundry's issuer/verifier surface |  |
| VP-0114 | DCQL / Selecting Claims (L952) | `claim_sets` MUST NOT be present if `claims` is absent | verifier | `not-implemented` | `claim_sets` is not modelled or read anywhere in `foundry-verifier` (see VP-0103); an operator-supplied `claim_sets` alongside an absent `claims` is silently ignored as an unknown property rather than rejected, so this constraint has no enforcement mechanism to be conforming or gap against |  |
| VP-0115 | DCQL / Selecting Claims (L963) | Verifiers MUST treat restrictions expressed using `values` as a best-effort privacy improvement | verifier | `not-unit-testable` | An interpretive/policy requirement about how a Verifier should *reason about* the `values` mechanism's guarantees, not a discrete code behaviour with its own pass/fail assertion distinct from the matching logic itself (VP-0110, `check_dcql_match`'s `values` branch) |  |
| VP-0116 | DCQL / Selecting Claims (L964) | Verifiers MUST NOT rely on `values` restrictions for security checks | verifier | `not-unit-testable` | Same nature as VP-0115: `check_dcql_match` does independently re-verify `values` against the cryptographically-disclosed claim (it does not trust a Wallet's own privacy-motivated filtering), which is the correct posture this clause calls for, but the clause itself is guidance about reliance/trust semantics rather than an independently testable code path |  |
| VP-0117 | DCQL / Selecting Claims (L983) | If the Wallet cannot deliver all claims requested for a Credential it MUST NOT return that Credential | wallet | `out-of-scope` | Obligation falls on the Wallet or a third party, not on foundry's issuer/verifier surface |  |
| VP-0118 | DCQL / Selecting Credentials (L999) | To satisfy a Credential Set Query the Wallet MUST return presentations of a set of Credentials matching one of its `options` | wallet | `out-of-scope` | Obligation falls on the Wallet or a third party, not on foundry's issuer/verifier surface |  |
| VP-0119 | DCQL / Selecting Credentials (L1004) | Credentials not matching the constraints expressed within `credentials` MUST NOT be returned | wallet | `out-of-scope` | Obligation falls on the Wallet or a third party, not on foundry's issuer/verifier surface |  |
| VP-0120 | DCQL / Selecting Credentials (L1008) | If the Wallet cannot deliver all non-optional Credentials requested it MUST NOT return any Credentials | wallet | `out-of-scope` | Obligation falls on the Wallet or a third party, not on foundry's issuer/verifier surface |  |
| VP-0121 | Claims Path Pointer (L1022) | A claims path pointer MUST be a non-empty array of strings, nulls and non-negative integers | verifier | `conforming` | `ClaimsPathSegment` (dcql_model.rs) only admits `String`, `Index(u64)` (non-negative by type), and `Wildcard` (the `null` component); `path` deserializes via `non_empty_path` | rejects_empty_claims_path, rejects_negative_path_index, path_segments_cover_string_index_and_wildcard |
| VP-0122 | Response / Response Parameters (L1166) | `vp_token` is REQUIRED in a response carrying a VP Token | wallet | `out-of-scope` | Obligation falls on the Wallet or a third party, not on foundry's issuer/verifier surface |  |
| VP-0123 | Response / Response Parameters (L1166) | When `multiple` is omitted or `false` the `vp_token` entry array MUST contain only one Presentation | wallet | `out-of-scope` | Obligation falls on the Wallet or a third party, not on foundry's issuer/verifier surface |  |
| VP-0124 | Response / Response Parameters (L1166) | There MUST NOT be any `vp_token` entry for an optional Credential Query with no matching Credentials | wallet | `out-of-scope` | Obligation falls on the Wallet or a third party, not on foundry's issuer/verifier surface |  |
| VP-0125 | Response / Response Parameters (L1172) | The Client MUST ignore any unrecognized response parameters | verifier | `conforming` | `do_verify_vp_response` (verify.rs) reads only `response_json.get("vp_token")` from the decrypted JWE payload; every other top-level member is never inspected | vp_0125_unrecognized_response_parameters_are_ignored |
| VP-0126 | Response / Response Mode `direct_post` (L1217) | The Authorization Response MUST be encoded in the request body using `application/x-www-form-urlencoded` | wallet | `out-of-scope` | Obligation falls on the Wallet or a third party, not on foundry's issuer/verifier surface |  |
| VP-0127 | Response / Response Mode `direct_post` (L1217) | The parameters in the request body MUST all be encoded using UTF-8 | wallet | `out-of-scope` | Obligation falls on the Wallet or a third party, not on foundry's issuer/verifier surface |  |
| VP-0128 | Response / Response Mode `direct_post` (L1222) | `response_uri` is REQUIRED when Response Mode `direct_post` is used | verifier | `conforming` | `build_signed_request_object` (request.rs) unconditionally inserts `response_uri` into the Request Object payload | test_build_signed_request_object_and_verify_jws, vp_0128_0130_0132_response_uri_present_no_redirect_uri_same_origin_as_client_id |
| VP-0129 | Response / Response Mode `direct_post` (L1222) | The Wallet MUST send the Authorization Response to the `response_uri` using an HTTP POST request | wallet | `out-of-scope` | Obligation falls on the Wallet or a third party, not on foundry's issuer/verifier surface |  |
| VP-0130 | Response / Response Mode `direct_post` (L1222) | When `response_uri` is present the `redirect_uri` Authorization Request parameter MUST NOT be present | verifier | `conforming` | `redirect_uri` has no field anywhere in `request.rs`'s payload construction and is never emitted, so the mutual exclusion is structurally impossible to violate | vp_0128_0130_0132_response_uri_present_no_redirect_uri_same_origin_as_client_id |
| VP-0131 | Response / Response Mode `direct_post` (L1222) | If `redirect_uri` is present with Response Mode `direct_post` the Wallet MUST return an `invalid_request` error | wallet | `out-of-scope` | Obligation falls on the Wallet or a third party, not on foundry's issuer/verifier surface |  |
| VP-0132 | Response / Response Mode `direct_post` (L1222) | The `response_uri` value MUST be a value the client would be permitted to use as `redirect_uri` | verifier | `conforming` | `response_uri` and `client_id` are both derived from the same `server.wallet_facing.public_base_url` host in `build_signed_request_object`, so `response_uri` is always same-origin with the client's own `x509_san_dns` identity -- the strictest available form of "permitted to use as redirect_uri" | vp_0128_0130_0132_response_uri_present_no_redirect_uri_same_origin_as_client_id |
| VP-0133 | Response / Response Mode `direct_post` (L1229) | The Wallet MUST ignore any unrecognized parameters used with Response Mode `direct_post` | wallet | `out-of-scope` | Obligation falls on the Wallet or a third party, not on foundry's issuer/verifier surface |  |
| VP-0134 | Response / Response Mode `direct_post` (L1276) | On successful processing the Response URI MUST respond with HTTP 200, `Content-Type: application/json`, and a JSON object body | http | `conforming` | `post_response_handler` (server.rs) returns `Ok(Json(result))` on success, which axum serializes as HTTP 200 with `Content-Type: application/json`; confirmed at the real HTTP boundary (not just by code reading) by a full `direct_post.jwt` round trip through `GET /vp/request/:id` and `POST /vp/response/:id` that asserts both the status code and the `Content-Type` header explicitly | vp_0134_response_on_success_is_http_200_with_json_content_type |
| VP-0135 | Response / Response Mode `direct_post` (L1281) | When the Response URI response carries `redirect_uri` the Wallet MUST redirect the user agent to that URI | wallet | `out-of-scope` | Obligation falls on the Wallet or a third party, not on foundry's issuer/verifier surface |  |
| VP-0136 | Response / Response Mode `direct_post` (L1283) | The Wallet MUST ignore any unrecognized parameters in the Response URI response | wallet | `out-of-scope` | Obligation falls on the Wallet or a third party, not on foundry's issuer/verifier surface |  |
| VP-0137 | Response / Response Mode `direct_post` (L1287) | The Verifier MUST include a fresh, cryptographically random value in the redirect URI | verifier | `not-implemented` | Same root cause as HAIP-0059/HAIP-0061 (Task 12): `post_response_handler` never returns a `redirect_uri` in its JSON response at all, so there is no redirect URI for this clause's randomness requirement to apply to |  |
| VP-0138 | Response / Response Mode `direct_post` (L1306) | The Wallet MUST ignore any unrecognized parameters in the response from the Response Endpoint | wallet | `out-of-scope` | Obligation falls on the Wallet or a third party, not on foundry's issuer/verifier surface |  |
| VP-0139 | Response / Encrypted Responses (L1312) | To encrypt the Authorization Response implementations MUST use an unsigned, encrypted JWT | verifier | `conforming` | `do_verify_vp_response` (verify.rs) only ever expects a bare compact-serialization JWE via `josekit::jwt::decode_with_decrypter` -- an unsigned, encrypted JWT with no nested JWS signature layer -- consistent with every test in this module | test_verify_vp_response_sd_jwt_vc |
| VP-0140 | Response / Encrypted Responses (L1316) | The `alg` parameter MUST be present in the JWKs offered for response encryption | verifier | `conforming` | `annotate_encryption_jwk` (request.rs) sets `alg` (alongside `kid`/`use`) on the ephemeral encryption JWK offered in `client_metadata.jwks` | test_signed_request_object_encryption_jwk_is_wallet_selectable, test_dc_api_client_metadata_encryption_jwk_is_wallet_selectable |
| VP-0141 | Response / Encrypted Responses (L1317) | The JWE `alg` algorithm used MUST equal the `alg` value of the chosen JWK | wallet | `out-of-scope` | Obligation falls on the Wallet or a third party, not on foundry's issuer/verifier surface |  |
| VP-0142 | Response / Encrypted Responses (L1317) | If the selected public key carries a `kid` the JWE MUST include the same value in its `kid` header | wallet | `out-of-scope` | Obligation falls on the Wallet or a third party, not on foundry's issuer/verifier surface |  |
| VP-0143 | Response / Encrypted Responses (L1321) | The payload of the encrypted JWT response MUST include the response contents as top-level JSON members | wallet | `out-of-scope` | Obligation falls on the Wallet or a third party, not on foundry's issuer/verifier surface |  |
| VP-0144 | Response / Response Mode `direct_post.jwt` (L1432) | The names and values in the `direct_post.jwt` POST body MUST be encoded using UTF-8 | wallet | `out-of-scope` | Obligation falls on the Wallet or a third party, not on foundry's issuer/verifier surface |  |
| VP-0145 | Response / Transaction Data (L1457) | A Wallet that received `transaction_data` MUST include a representation or reference to the data in the respective Credential presentation | wallet | `out-of-scope` | Obligation falls on the Wallet or a third party, not on foundry's issuer/verifier surface |  |
| VP-0146 | Response / Transaction Data (L1459) | A Wallet that does not support `transaction_data` MUST return an error when a request includes it | wallet | `out-of-scope` | Obligation falls on the Wallet or a third party, not on foundry's issuer/verifier surface |  |
| VP-0147 | Response / VP Token Validation (L1516) | Verifiers MUST validate the format of the VP Token | verifier | `conforming` | `select_presentation` (verify.rs) structurally validates `vp_token`'s shape (object keyed by DCQL id, array of exactly one presentation, string/object per declared format) before any credential-format verification begins | select_presentation_rejects_bare_string_vp_token, select_presentation_requires_an_array_value, select_presentation_requires_exactly_one_presentation, select_presentation_rejects_payload_contradicting_declared_format |
| VP-0148 | Response / VP Token Validation (L1518) | Verifiers MUST validate the integrity and authenticity of each Presentation and Credential | verifier | `conforming` | `do_verify_vp_response` (verify.rs) calls `foundry_sd_jwt_vc::verifier::verify_sd_jwt_vc` or `foundry_mdoc::verifier::verify_mdoc` depending on format, both of which validate the issuer signature chain and are reported as named `CheckResult`s | test_verify_vp_response_sd_jwt_vc, test_verify_vp_response_mdoc_presentation |
| VP-0149 | Response / VP Token Validation (L1519) | Verifiers MUST validate that the returned Credentials meet all criteria defined in the query of the Authorization Request | verifier | `conforming` | `do_verify_vp_response` always calls `check_dcql_match` (Task 13) as one of its `checks`, and a mismatch fails the overall `verified` result | test_verify_vp_response_dcql_vct_mismatch_is_not_verified |
| VP-0150 | Response / VP Token Validation (L1520) | Verifiers MUST validate that all Presentations carry a cryptographic proof of Holder Binding unless specifically requested otherwise | verifier | `conforming` | Both format branches unconditionally require a Holder Binding proof -- SD-JWT VC via KB-JWT (`verify_sd_jwt_vc`) and mdoc via `device_signature` (`verify_mdoc`) -- with no code path to skip it; the opt-out this clause allows (`require_cryptographic_holder_binding: false`, VP-0033/VP-0034) is itself not implemented, so "unless specifically requested otherwise" never triggers and Holder Binding is a strict superset always enforced | test_verify_vp_response_sd_jwt_vc, test_verify_vp_response_mdoc_presentation |
| VP-0151 | Response / VP Token Validation (L1521) | Verifiers MUST validate Holder Binding including the checks required to prevent replay | verifier | `conforming` | `verify_sd_jwt_vc` validates the KB-JWT's `nonce` claim against `tx.nonce`, rejecting a mismatched (replayed/forged) nonce | test_verify_vp_response_kb_nonce_mismatch |
| VP-0152 | Response / VP Token Validation (L1522) | Verifiers MUST perform the checks required by the Verifier's trust policy, such as revocation checks, where applicable | verifier | `conforming` | `do_verify_vp_response` always calls `check_status` (status.rs) as one of its `checks`; `status.rs`'s own suite proves revoked/suspended indices fail, valid indices pass, subject mismatches fail, and a network failure is a hard error rather than a policy verdict, and a network failure surfaces the same way through the full `verify_vp_response` entry point (not just `check_status` in isolation); further confirmed through the real HTTP boundary (AGENTS.md Sec4.3's "network unavailability -> HTTP 502" mapping), where a Status List URI pointing at a closed TCP port makes `POST /vp/response/:id` itself answer 502 with `error: status_unavailable`, not a `verified: false` policy body | test_verify_vp_response_sd_jwt_vc, revoked_index_fails, valid_index_passes, subject_mismatch_fails_check, network_failure_is_hard_error, vp_0152_status_endpoint_unreachable_is_a_hard_error_through_full_verification, vp_0152_status_fetch_network_failure_maps_to_http_502 |
| VP-0153 | Response / VP Token Validation (L1523) | Verifiers MUST check that the set of Presentations returned satisfies all requirements of the Verifier's request | verifier | `gap` | Revised from Task 14's `conforming` verdict: with exactly one Presentation per `vp_token` by design, DCQL criteria and format/signature checks are indeed all satisfied (`test_verify_vp_response_dcql_vct_mismatch_is_not_verified`), but "all requirements of the Verifier's request" also includes any `transaction_data` the Verifier requested (VP-0019/VP-0020), and that binding is never checked at all. See GAP-VP-04 | gap_vp_04_transaction_data_hashes_never_validated |
| VP-0154 | Response / VP Token Validation (L1525) | If any check related to an individual Presentation fails that Presentation MUST be discarded | verifier | `conforming` | With cardinality exactly one, "discarding" the failing Presentation and rejecting the whole response are the same outcome: `test_verify_vp_response_dcql_vct_mismatch_is_not_verified` shows a failing `dcql_match` check yields `verified: false` even though the signature check independently passed and is still reported (never silently treated as satisfying the request) | test_verify_vp_response_dcql_vct_mismatch_is_not_verified |
| VP-0155 | Response / VP Token Validation (L1525) | If any check pertaining to the VP Token or the overall response fails the VP Token MUST be rejected | verifier | `conforming` | `verified` is the conjunction of every `CheckResult` (AGENTS.md §4.2, never hardcoded `true`); a policy failure returns `VerificationState::Failed` with `verified: false` rather than an error (AGENTS.md §4.3) | test_verify_vp_response_dcql_vct_mismatch_is_not_verified, test_verify_vp_response_kb_nonce_mismatch |
| VP-0156 | Wallet Metadata (L1547) | `vp_formats_supported` is REQUIRED in Wallet metadata | wallet | `out-of-scope` | Obligation falls on the Wallet or a third party, not on foundry's issuer/verifier surface |  |
| VP-0157 | Wallet Metadata (L1568) | The Verifier MUST ignore any unrecognized Wallet metadata parameters | verifier | `not-implemented` | foundry never consumes a Wallet's metadata document at all — `request.rs` builds requests without ever fetching or parsing wallet metadata |  |
| VP-0158 | Verifier Metadata (L1588) | `vp_formats_supported` is REQUIRED in Verifier metadata | verifier | `conforming` | Same evidence and mechanism as VP-0012 | test_client_metadata_advertises_vp_formats_supported |
| VP-0159 | Verifier Metadata (L1593) | The Wallet MUST ignore any unrecognized Verifier metadata parameters | wallet | `out-of-scope` | Obligation falls on the Wallet or a third party, not on foundry's issuer/verifier surface |  |
| VP-0160 | Verifier Attestation JWT (L1597) | The Verifier MUST always present a Verifier Attestation JWT together with proof of possession of its bound public key | verifier | `not-implemented` | The `verifier_attestation` Client Identifier Prefix mechanism (VP-0055/0056/0057) is not implemented at all; foundry never produces a Verifier Attestation JWT |  |
| VP-0161 | Verifier Attestation JWT (L1599) | A Verifier Attestation JWT MUST contain the claims defined for it | verifier | `not-implemented` | Same evidence as VP-0160 |  |
| VP-0162 | Verifier Attestation JWT (L1601) | `iss` is REQUIRED in a Verifier Attestation JWT | verifier | `not-implemented` | Same evidence as VP-0160 |  |
| VP-0163 | Verifier Attestation JWT (L1602) | `sub` is REQUIRED and MUST be the `client_id` of the client making the request | verifier | `not-implemented` | Same evidence as VP-0160 |  |
| VP-0164 | Verifier Attestation JWT (L1604) | `exp` is REQUIRED in a Verifier Attestation JWT | verifier | `not-implemented` | Same evidence as VP-0160 |  |
| VP-0165 | Verifier Attestation JWT (L1604) | The Wallet MUST reject any Verifier Attestation JWT whose expiration time has passed, subject to clock skew | wallet | `out-of-scope` | Obligation falls on the Wallet or a third party, not on foundry's issuer/verifier surface |  |
| VP-0166 | Verifier Attestation JWT (L1605) | A Verifier Attestation JWT MUST NOT be accepted for processing before its `nbf` time | wallet | `out-of-scope` | Obligation falls on the Wallet or a third party, not on foundry's issuer/verifier surface |  |
| VP-0167 | Verifier Attestation JWT (L1606) | `cnf` is REQUIRED in a Verifier Attestation JWT | verifier | `not-implemented` | Same evidence as VP-0160 |  |
| VP-0168 | Verifier Attestation JWT (L1606) | `cnf` MUST contain a JSON Web Key as defined in RFC7800 Section 3.2 | verifier | `not-implemented` | Same evidence as VP-0160 |  |
| VP-0169 | Verifier Attestation JWT (L1606) | The Verifier MUST prove possession of the private key corresponding to the public key in `cnf` | verifier | `not-implemented` | Same evidence as VP-0160 |  |
| VP-0170 | Verifier Attestation JWT (L1610) | The Wallet MUST ignore any unrecognized claims in the Verifier Attestation JWT | wallet | `out-of-scope` | Obligation falls on the Wallet or a third party, not on foundry's issuer/verifier surface |  |
| VP-0171 | Verifier Attestation JWT (L1612) | Verifier Attestation JWTs MUST use the media type `application/verifier-attestation+jwt` | verifier | `not-implemented` | Same evidence as VP-0160 |  |
| VP-0172 | Verifier Attestation JWT (L1614) | A Verifier Attestation JWT MUST set the `typ` JOSE header to `verifier-attestation+jwt` | verifier | `not-implemented` | Same evidence as VP-0160 |  |
| VP-0173 | Verifier Attestation JWT (L1620) | The `jwt` JOSE header MUST contain a JWT | verifier | `not-implemented` | Same evidence as VP-0160 |  |
| VP-0174 | Verifier Attestation JWT (L1622) | A JWT carried in the `jwt` JOSE header MUST set `typ` to `verifier-attestation+jwt` | verifier | `not-implemented` | Same evidence as VP-0160 |  |
| VP-0175 | Security / Preventing Replay (L1789) | Implementers MUST implement the replay detection and prevention controls defined for Verifiable Presentations | verifier | `conforming` | Umbrella clause; its individually-testable sub-requirements are VP-0177 (binding to audience/nonce), VP-0179 (every Presentation linked to `client_id`/`nonce`), and VP-0180 (reject on nonce mismatch), all of which hold | vp_0175_0177_0179_0180_presentation_replayed_against_second_transaction_is_rejected |
| VP-0176 | Security / Preventing Replay (L1791) | The cryptographic proof of possession MUST be bound by the Wallet to the intended audience and to the transaction `nonce` | wallet | `out-of-scope` | Obligation falls on the Wallet or a third party, not on foundry's issuer/verifier surface |  |
| VP-0177 | Security / Preventing Replay (L1791) | The Verifier MUST verify the binding of the proof of possession to audience and `nonce` | verifier | `conforming` | `verify_sd_jwt_vc` is always called with `client_id` (audience) and `tx.nonce`, and rejects a KB-JWT whose `aud`/`nonce` do not match; `verify_mdoc` likewise binds the device signature to `client_id`, `response_uri`, and `tx.nonce` via the session transcript | test_verify_vp_response_kb_nonce_mismatch, vp_0175_0177_0179_0180_presentation_replayed_against_second_transaction_is_rejected |
| VP-0178 | Security / Preventing Replay (L1793) | The Wallet MUST link every Verifiable Presentation in the VP Token to the `client_id` and `nonce` of the request | wallet | `out-of-scope` | Obligation falls on the Wallet or a third party, not on foundry's issuer/verifier surface |  |
| VP-0179 | Security / Preventing Replay (L1795) | The Verifier MUST validate that every individual Verifiable Presentation is linked to the `client_id` and `nonce` it used for the request | verifier | `conforming` | With exactly one Presentation verified per `vp_token` by design, "every individual Presentation" reduces to the single one this verifier checks against `tx.nonce`/`client_id`; a presentation captured for one transaction and replayed against a second, independently-created transaction (distinct `nonce` and ephemeral key) is rejected | vp_0175_0177_0179_0180_presentation_replayed_against_second_transaction_is_rejected |
| VP-0180 | Security / Preventing Replay (L1795) | If any Verifiable Presentation does not contain the correct `nonce` value the response MUST be rejected | verifier | `conforming` | Same evidence as VP-0177/VP-0179: an incorrect `nonce` (whether an arbitrary bad value or one from a different, real transaction) makes `verify_sd_jwt_vc` fail, so `verified` is `false`/the call errors rather than the response being accepted | test_verify_vp_response_kb_nonce_mismatch, vp_0175_0177_0179_0180_presentation_replayed_against_second_transaction_is_rejected |
| VP-0181 | Security / Session Fixation (L1856) | The Verifier's Response URI MUST include a fresh Response Code secret in the redirect URI returned to the Wallet | verifier | `not-implemented` | This protection technique only applies when Response Mode `direct_post` is combined with a redirect URI (the spec text is explicit that this is a conditional, opt-in mitigation, not an unconditional requirement of `direct_post` itself). Foundry never emits a `redirect_uri` at all (VP-0128/VP-0130/VP-0132, Task 14) -- same root cause as HAIP-0059/HAIP-0061 (Task 12) -- so the redirect-URI-plus-Response-Code combination this clause describes is simply not a code path that exists |  |
| VP-0182 | Security / Session Fixation (L1856) | The Verifier's Response URI MUST require the frontend to pass the respective Response Code when fetching the Authorization Response | verifier | `not-implemented` | Same root cause as VP-0181: no redirect URI is ever issued, so there is no frontend-facing Response Code handshake to require |  |
| VP-0183 | Security / Validation of the Response URI (L1870) | The Wallet MUST ensure the data in the Authorization Response cannot leak through Response URIs | wallet | `out-of-scope` | Obligation falls on the Wallet or a third party, not on foundry's issuer/verifier surface |  |
| VP-0184 | Security / Validation of the Response URI (L1870) | When using pre-registered Response URIs the Wallet MUST comply with the redirect URI validation best practices of RFC9700 | wallet | `out-of-scope` | Obligation falls on the Wallet or a third party, not on foundry's issuer/verifier surface |  |
| VP-0185 | Security / Protection of the Authorization Response Data (L1880) | Implementations MUST have security mechanisms preventing inadvertent requests against the internal interface serving Authorization Response data | verifier | `not-unit-testable` | The "internal interface" is `GET /admin/verification/requests/:id` (`crates/foundry/src/server.rs`'s `get_verification_handler`), which returns the stored `VerificationResult`. That route sits behind `admin_router`'s `authenticated` group, gated by `require_api_key` (`crates/foundry/src/admin_auth.rs`) -- the "Authentication between the different parts within the Verifier" option the spec text lists. This is HTTP-layer evidence in the `foundry` binary crate, outside `foundry-verifier`'s scope for this task; `rejects_offer_creation_without_bearer_token`/`rejects_offer_creation_with_wrong_bearer_token` (`crates/foundry/tests/issuer_offers.rs`) already prove the shared `require_api_key` middleware rejects unauthenticated/wrong-key requests on a sibling route in the same authenticated group. An endpoint-specific test belongs to Task 18's HTTP-layer adjudication |  |
| VP-0186 | Security / End-User Authentication using Credentials (L1887) | A claim used to authenticate the End-User MUST be stable, locally unique, and never reassigned to another End-User by the Credential Issuer | verifier | `out-of-scope` | Foundry's verifier engine performs no End-User authentication or session/login logic of its own: `do_verify_vp_response` returns disclosed claims to whatever caller retrieves the `VerificationResult`, and choosing which claim to treat as a stable authentication identifier (and enforcing its stability/uniqueness) is a decision for that downstream Relying Party application, not for foundry's protocol engine |  |
| VP-0187 | Security / End-User Authentication using Credentials (L1887) | Such a claim MUST only be used in combination with the Credential Issuer identifier to ensure global uniqueness | verifier | `out-of-scope` | Same rationale as VP-0186: this is a downstream Relying Party's integration choice, not logic performed anywhere in foundry's verifier engine |  |
| VP-0188 | Security / TLS Requirements (L1895) | Implementations MUST follow BCP195 | http | `not-unit-testable` | Same reasoning as VCI-0049/VCI-0161: BCP195 cipher-suite/protocol-version selection is a deployment/transport concern outside this crate's unit-test scope |  |
| VP-0189 | Security / TLS Requirements (L1897) | Whenever TLS is used a TLS server certificate check MUST be performed per RFC6125 | http | `not-unit-testable` | Same reasoning as VCI-0049/VCI-0162: certificate hostname verification is performed by the TLS client (the Wallet), not by anything foundry's server-side code controls or could unit-test |  |
| VP-0190 | Security / Always Use the Full Client Identifier (L1909) | Wallets MUST always use the full Client Identifier, including the prefix, to identify the client | wallet | `out-of-scope` | Obligation falls on the Wallet or a third party, not on foundry's issuer/verifier surface |  |
| VP-0191 | Security / Checks on Returned Credentials (L1914) | The Verifier MUST NOT rely on the Wallet to enforce the constraints expressed in its query | verifier | `conforming` | `do_verify_vp_response` independently re-derives and checks every constraint server-side -- signature/KB-JWT (`verify_sd_jwt_vc`/`verify_mdoc`), DCQL satisfaction (`check_dcql_match`), and status (`check_status`) -- regardless of what the wallet asserts; `test_verify_vp_response_dcql_vct_mismatch_is_not_verified` shows a wallet-returned credential that fails the Verifier's own `vct` constraint is rejected even though its signature independently passed | test_verify_vp_response_dcql_vct_mismatch_is_not_verified |
| VP-0192 | Security / Checks on Returned Credentials (L1916) | The Verifier MUST perform its own security checks on the returned Credentials and Presentations | verifier | `conforming` | Same evidence as VP-0191: every check in `do_verify_vp_response`'s `checks` vector is computed by the verifier itself from the decrypted payload, never taken on the wallet's word | test_verify_vp_response_dcql_vct_mismatch_is_not_verified |
| VP-0193 | Privacy / DCQL Value Matching (L1958) | Wallets MUST take precautions against leaking information about claim values when processing `values` | wallet | `out-of-scope` | Obligation falls on the Wallet or a third party, not on foundry's issuer/verifier surface |  |
| VP-0194 | Privacy / Wallet to Verifier Communication (L2003) | Wallets MUST NOT include personally identifiable information in HTTP requests to Verifiers unless required for the flow and authorized by the End-User | wallet | `out-of-scope` | Obligation falls on the Wallet or a third party, not on foundry's issuer/verifier surface |  |
| VP-0195 | Privacy / Authorization Requests with Request URI (L2013) | If the Wallet cannot establish that the Request URI belongs to the Client Identifier within its trust framework it MUST refuse the request | wallet | `out-of-scope` | Obligation falls on the Wallet or a third party, not on foundry's issuer/verifier surface |  |
| VP-0196 | DC API / Protocol (L2397) | The value `1` MUST be used for the `<version>` field of the protocol identifier | verifier | `not-implemented` | The `protocol` identifier string (e.g. `openid4vp-v1-unsigned`) is a W3C Digital Credentials API invocation-layer value the calling web page's JavaScript attaches when wrapping foundry's `dc_api_request` payload as `data` -- `create_verification_request` (request.rs) never emits a `protocol` field or any version identifier at all; a grep for `openid4vp-v1`/`"protocol"` across the verifier crate returns no results |  |
| VP-0197 | DC API / Protocol (L2397) | The `<request-type>` field MUST be `unsigned`, `signed` or `multisigned` according to the request form used | verifier | `not-implemented` | Same evidence as VP-0196: foundry's `dc_api` transport only ever produces the unsigned-request shape (no `client_id`, no JWS wrapping) and never exposes a way to select `signed` or `multisigned` -- there is no `<request-type>` selection logic anywhere |  |
| VP-0198 | DC API / Request (L2433) | The `client_id` parameter MUST be omitted in unsigned DC API requests | verifier | `conforming` | The `dc_api_obj` JSON built in `create_verification_request` (request.rs) for `transport: "dc_api"` carries only `response_type`, `response_mode`, `dcql_query`, `nonce`, and `client_metadata` -- there is no `client_id` key in the object literal at all | vp_0198_0201_dc_api_unsigned_request_shape |
| VP-0199 | DC API / Request (L2433) | The Wallet MUST ignore any `client_id` parameter present in an unsigned request | wallet | `out-of-scope` | Obligation falls on the Wallet or a third party, not on foundry's issuer/verifier surface |  |
| VP-0200 | DC API / Request (L2437) | The `client_id` parameter MUST be present in signed DC API requests | verifier | `not-implemented` | Same root cause as VP-0197: foundry has no signed DC API request path at all, so this mandate's precondition never triggers |  |
| VP-0201 | DC API / Request (L2438) | `response_mode` MUST be `dc_api` when the response is not encrypted and `dc_api.jwt` when it is | verifier | `conforming` | `create_verification_request` (request.rs) hardcodes `"response_mode": "dc_api.jwt"` in `dc_api_obj`, and `response_encryption_params` (Task 12/15) always returns a valid `(alg, enc)` pair unconditionally -- responses are never unencrypted in any deployment, so the always-encrypted branch (`dc_api.jwt`) is always the correct value | vp_0198_0201_dc_api_unsigned_request_shape |
| VP-0202 | DC API / Request (L2442) | `expected_origins` is REQUIRED when signed requests are used with the DC API | verifier | `not-implemented` | Same root cause as VP-0197/VP-0200: signed DC API requests do not exist in this implementation, so `expected_origins`'s precondition never triggers; a grep for `expected_origins` across the workspace returns no results |  |
| VP-0203 | DC API / Request (L2442) | The Wallet MUST compare the values in `expected_origins` to the Origin to detect replay of the request | wallet | `out-of-scope` | Obligation falls on the Wallet or a third party, not on foundry's issuer/verifier surface |  |
| VP-0204 | DC API / Request (L2442) | If the Origin does not match any entry in `expected_origins` the Wallet MUST return an error | wallet | `out-of-scope` | Obligation falls on the Wallet or a third party, not on foundry's issuer/verifier surface |  |
| VP-0205 | DC API / Request (L2442) | A Wallet MUST ignore `expected_origins` if it is present in an unsigned request | wallet | `out-of-scope` | Obligation falls on the Wallet or a third party, not on foundry's issuer/verifier surface |  |
| VP-0206 | DC API / Request (L2448) | The Wallet MUST ignore any unrecognized DC API request parameters | wallet | `out-of-scope` | Obligation falls on the Wallet or a third party, not on foundry's issuer/verifier surface |  |
| VP-0207 | DC API / JWS JSON Serialization (L2486) | `client_id`, `verifier_info` and prefix-specific parameters, if used, MUST be present only in the protected header of the respective `signature` object | verifier | `not-implemented` | JWS JSON Serialization (the `multisigned` request type) is not implemented anywhere in this workspace -- same root cause as VP-0197/VP-0200/VP-0202 |  |
| VP-0208 | DC API / JWS JSON Serialization (L2492) | All other request parameters MUST be present in the `payload` element of the JWS object | verifier | `not-implemented` | Same root cause as VP-0207: JWS JSON Serialization does not exist in this implementation |  |
| VP-0209 | DC API / Response (L2543) | The audience for a DC API response MUST be the Origin prefixed with `origin:`, even for signed requests | verifier | `conforming` | Both format halves are now conformant. SD-JWT VC: `do_verify_vp_response` computes an Origin-prefixed `expected_audiences` for `tx.transport == "dc_api"` and `verify_sd_jwt_vc` matches against it (see VP-0265). mdoc: the DC API presentation is bound by the `OpenID4VPDCAPIHandover` whose `OpenID4VPDCAPIHandoverInfo` carries the Origin (see VP-0246). Note the two mechanisms differ in prefixing -- the KB-JWT `aud` carries `origin:`, the Handover element MUST NOT (L2997) -- so they are reconciled separately, not by a shared value | dc_api_mdoc_accepts_a_later_configured_origin |
| VP-0210 | Format / W3C Verifiable Credentials (L2572) | If `require_cryptographic_holder_binding` is `true` the Wallet MUST return a Verifiable Presentation of a Verifiable Credential | verifier | `out-of-scope` | W3C VC format profile excluded 2026-07-31 by scope decision; foundry presents only `dc+sd-jwt` and `mso_mdoc` |  |
| VP-0211 | Format / W3C Verifiable Credentials (L2572) | Otherwise a Verifiable Credential without Holder Binding MUST be returned | verifier | `out-of-scope` | W3C VC format profile excluded 2026-07-31 by scope decision; foundry presents only `dc+sd-jwt` and `mso_mdoc` |  |
| VP-0212 | Format / W3C Verifiable Credentials / `meta` (L2579) | `type_values` is REQUIRED and MUST be a non-empty array of string arrays | verifier | `out-of-scope` | W3C VC format profile excluded 2026-07-31 by scope decision; foundry presents only `dc+sd-jwt` and `mso_mdoc` |  |
| VP-0213 | Format / W3C Verifiable Credentials / `meta` (L2579) | Each inner array of `type_values` specifies fully expanded types that MUST be present in the expanded `type` property of the Verifiable Credential | verifier | `out-of-scope` | W3C VC format profile excluded 2026-07-31 by scope decision; foundry presents only `dc+sd-jwt` and `mso_mdoc` |  |
| VP-0214 | Format / W3C VC signed as a JWT (L2659) | `vp_formats_supported` MUST carry the Credential Format Identifier as a key whose value MUST be an object with the defined name/value pair | verifier | `out-of-scope` | W3C VC format profile excluded 2026-07-31 by scope decision; foundry presents only `dc+sd-jwt` and `mso_mdoc` |  |
| VP-0215 | Format / W3C VC signed as a JWT (L2661) | If `alg_values` is present the `alg` JOSE header of the presented Credential or Presentation MUST match one of its values | verifier | `out-of-scope` | W3C VC format profile excluded 2026-07-31 by scope decision; foundry presents only `dc+sd-jwt` and `mso_mdoc` |  |
| VP-0216 | Format / W3C VC signed as a JWT (L2679) | The `nonce` claim MUST be the value of `nonce` from the Authorization Request | verifier | `out-of-scope` | W3C VC format profile excluded 2026-07-31 by scope decision; foundry presents only `dc+sd-jwt` and `mso_mdoc` |  |
| VP-0217 | Format / W3C VC signed as a JWT (L2680) | The `aud` claim MUST be the Client Identifier, or the Origin prefixed with `origin:` over the DC API | verifier | `out-of-scope` | W3C VC format profile excluded 2026-07-31 by scope decision; foundry presents only `dc+sd-jwt` and `mso_mdoc` |  |
| VP-0218 | Format / LDP VCs (L2712) | `vp_formats_supported` MUST carry the Credential Format Identifier as a key whose value MUST be an object with the defined name/value pairs | verifier | `out-of-scope` | W3C VC format profile excluded 2026-07-31 by scope decision; foundry presents only `dc+sd-jwt` and `mso_mdoc` |  |
| VP-0219 | Format / LDP VCs (L2714) | If `proof_type_values` is present the proof `type` of the presented Credential or Presentation MUST match one of its values | verifier | `out-of-scope` | W3C VC format profile excluded 2026-07-31 by scope decision; foundry presents only `dc+sd-jwt` and `mso_mdoc` |  |
| VP-0220 | Format / LDP VCs (L2715) | If `cryptosuite_values` is present the proof `cryptosuite` of the presented Credential or Presentation MUST match one of its values | verifier | `out-of-scope` | W3C VC format profile excluded 2026-07-31 by scope decision; foundry presents only `dc+sd-jwt` and `mso_mdoc` |  |
| VP-0221 | Format / LDP VCs (L2733) | The `challenge` claim MUST be the value of `nonce` from the Authorization Request | verifier | `out-of-scope` | W3C VC format profile excluded 2026-07-31 by scope decision; foundry presents only `dc+sd-jwt` and `mso_mdoc` |  |
| VP-0222 | Format / LDP VCs (L2734) | The `domain` claim MUST be the value of the Client Identifier, or the Origin prefixed with `origin:` over the DC API | verifier | `out-of-scope` | W3C VC format profile excluded 2026-07-31 by scope decision; foundry presents only `dc+sd-jwt` and `mso_mdoc` |  |
| VP-0223 | Format / mdoc / Transaction Data (L2751) | A Wallet receiving a request with a `transaction_data` type whose data element is unauthorized MUST reject the request as an unsupported transaction data type | wallet | `out-of-scope` | Obligation falls on the Wallet or a third party, not on foundry's issuer/verifier surface |  |
| VP-0224 | Format / mdoc / Metadata (L2755) | `vp_formats_supported` MUST carry the mdoc Credential Format Identifier as a key whose value MUST be an object with the defined name/value pairs | verifier | `conforming` | `vp_formats_supported()` (request.rs) always includes `"mso_mdoc": {"issuerauth_alg_values": [-7], "deviceauth_alg_values": [-7]}` in every transport's client_metadata | test_client_metadata_advertises_vp_formats_supported |
| VP-0225 | Format / mdoc / Metadata (L2757) | A Credential MUST be considered to fulfill `issuerauth_alg_values` when one of the defined matching conditions on the `IssuerAuth` COSE header holds | verifier | `conforming` | `cose_alg_str` (foundry-mdoc/src/verifier.rs) is an exhaustive match that only ever maps `iana::Algorithm::ES256` to `"ES256"` (COSE `-7`) -- any other IssuerAuth algorithm is a hard `FormatError::SignatureVerification` rejection, so a Credential this verifier successfully verifies always fulfills the declared `issuerauth_alg_values: [-7]` by construction | test_verify_vp_response_mdoc_presentation |
| VP-0226 | Format / mdoc / Metadata (L2758) | A Credential MUST be considered to fulfill `deviceauth_alg_values` when one of the defined matching conditions on the `DeviceSignature` or `DeviceMac` COSE header holds | verifier | `conforming` | Same `cose_alg_str` restriction applies identically to the DeviceSignature COSE header (`curve_for_alg`/`cose_alg_str`, foundry-mdoc/src/verifier.rs); only `-7`/ES256 is ever accepted | test_verify_vp_response_mdoc_presentation |
| VP-0227 | Format / mdoc / `meta` (L2802) | `doctype_value` is REQUIRED | verifier | `gap` | `credential_query_satisfied` (dcql.rs) only checks `doctype_value` against the presented `doc_type` *if present* in `cq.meta()` -- an mso_mdoc Credential Query with no `doctype_value` in `meta` matches any doctype at all, since `DcqlCredentialQuery`'s opaque `meta: Option<Value>` carries no required-field validation. Same underlying absence of DCQL structural validation as GAP-VP-03, extended to this format-specific field | vp_0093_0094_0096_0097_dcql_structural_constraints_not_validated |
| VP-0228 | Format / mdoc / `meta` (L2803) | `doctype_value` MUST be a valid doctype identifier as defined in ISO/IEC 18013-5 | verifier | `gap` | Same finding as VP-0227: when present, `doctype_value` is read as an opaque string and compared verbatim against the presented `doc_type` -- its own format/registration validity per ISO/IEC 18013-5 is never checked. Same GAP-VP-03 root cause | vp_0093_0094_0096_0097_dcql_structural_constraints_not_validated |
| VP-0229 | Format / mdoc / Invocation via Redirects (L2829) | For invocation via redirects the ISO/IEC 18013-5 `SessionTranscript` structure MUST be used with the changes defined here | verifier | `conforming` | `build_session_transcript` (foundry-mdoc/src/types.rs) emits `[null, null, OpenID4VPHandover]`, reproducing OpenID4VP's published redirects `SessionTranscript` vector byte-for-byte | redirect_session_transcript_matches_openid4vp_vector |
| VP-0230 | Format / mdoc / Invocation via Redirects (L2831) | `DeviceEngagementBytes` MUST be `null` | verifier | `conforming` | `build_session_transcript` (foundry-mdoc/src/types.rs)'s outer array is always `[Null, Null, Handover]` -- `DeviceEngagementBytes` (the first element) is always CBOR `Null` | redirect_session_transcript_matches_openid4vp_vector |
| VP-0231 | Format / mdoc / Invocation via Redirects (L2832) | `EReaderKeyBytes` MUST be `null` | verifier | `conforming` | Same array construction: the second element is always CBOR `Null` | redirect_session_transcript_matches_openid4vp_vector |
| VP-0232 | Format / mdoc / Invocation via Redirects (L2833) | `Handover` MUST be the `OpenID4VPHandover` CBOR structure | verifier | `conforming` | `build_session_transcript` (foundry-mdoc/src/types.rs)'s `Redirect` variant emits the `OpenID4VPHandover` structure; `foundry-verifier` selects it for every transport other than `dc_api` | redirect_session_transcript_matches_openid4vp_vector |
| VP-0233 | Format / mdoc / Invocation via Redirects (L2865) | The first element of `OpenID4VPHandover` MUST be the string `OpenID4VPHandover` | verifier | `conforming` | The literal is the first element of the encoded Handover; the audit's own byte-search test now finds it | gap_vp_06_mdoc_session_transcript_handover_should_contain_the_spec_defined_literal |
| VP-0234 | Format / mdoc / Invocation via Redirects (L2866) | The second element MUST be a Byte String containing the SHA-256 hash of the CBOR-encoded `OpenID4VPHandoverInfo` | verifier | `conforming` | The second element is `sha256` over the plain CBOR encoding of `OpenID4VPHandoverInfo` -- no tag-24 wrapper, an ambiguity in the prose settled against the spec's published vector, which the test reproduces including the intermediate `HandoverInfo` encoding | redirect_session_transcript_matches_openid4vp_vector |
| VP-0235 | Format / mdoc / Invocation via Redirects (L2868) | The first element of `OpenID4VPHandoverInfo` MUST be the `client_id` request parameter, including the prefix where applicable | verifier | `conforming` | `SessionTranscriptParams::Redirect.client_id` is the first `OpenID4VPHandoverInfo` element; `foundry-verifier` passes the `x509_san_dns:<host>` Client Identifier including its prefix | redirect_session_transcript_matches_openid4vp_vector |
| VP-0236 | Format / mdoc / Invocation via Redirects (L2869) | The second element MUST be the value of the `nonce` request parameter | verifier | `conforming` | `SessionTranscriptParams::Redirect.nonce` is the second `OpenID4VPHandoverInfo` element, sourced from `tx.nonce` | redirect_session_transcript_matches_openid4vp_vector |
| VP-0237 | Format / mdoc / Invocation via Redirects (L2870) | If the response is encrypted the third element MUST be the JWK SHA-256 Thumbprint of the Verifier's encryption public key, otherwise `null` | verifier | `conforming` | `jwk_thumbprint: Some(..)` for the encrypted Response Mode `direct_post.jwt` and `None` -- encoded as CBOR `null`, not an omitted element -- otherwise. The thumbprint is `foundry_core::obs::thumbprint_bytes(&tx.ephem_public_jwk)`, fail-closed so a malformed key aborts rather than hashing a placeholder | absent_thumbprint_encodes_as_cbor_null_not_omission |
| VP-0238 | Format / mdoc / Invocation via Redirects (L2871) | The fourth element MUST be either the `redirect_uri` or the `response_uri` request parameter, depending on which is present | verifier | `conforming` | `SessionTranscriptParams::Redirect.response_uri` is the fourth element. foundry only ever issues `response_uri`, never `redirect_uri`, so the clause's either/or is satisfied for every Response Mode foundry supports | redirect_session_transcript_matches_openid4vp_vector |
| VP-0239 | Format / mdoc / Invocation via Redirects (L2873) | The referenced request parameter values MUST be taken from the query parameters when the request is unsigned and from the signed Request Object when it is signed | verifier | `conforming` | All four values are read from the `VerificationTransaction` the signed Request Object was built from (`tx.nonce`, `tx.id`, `tx.ephem_public_jwk`) plus the `client_id` derived from `public_base_url` -- i.e. from the signed request, never from unauthenticated query parameters | test_verify_vp_response_mdoc_presentation |
| VP-0240 | Format / mdoc / Invocation via the DC API (L2959) | For invocation via the DC API the ISO/IEC 18013-5 `SessionTranscript` structure MUST be used with the changes defined here | verifier | `conforming` | `build_session_transcript` (foundry-mdoc/src/types.rs)'s `DcApi` variant is a distinct branch, reproducing OpenID4VP's published DC API `SessionTranscript` vector byte-for-byte | dc_api_session_transcript_matches_openid4vp_vector |
| VP-0241 | Format / mdoc / Invocation via the DC API (L2961) | `DeviceEngagementBytes` MUST be `null` | verifier | `conforming` | Same builder and outer array construction as VP-0230, via the `DcApi` variant | dc_api_session_transcript_matches_openid4vp_vector |
| VP-0242 | Format / mdoc / Invocation via the DC API (L2962) | `EReaderKeyBytes` MUST be `null` | verifier | `conforming` | Same array construction as VP-0231, via the `DcApi` variant | dc_api_session_transcript_matches_openid4vp_vector |
| VP-0243 | Format / mdoc / Invocation via the DC API (L2963) | `Handover` MUST be the `OpenID4VPDCAPIHandover` CBOR structure | verifier | `conforming` | The two variants are separate enum arms producing different fixed identifiers and different `HandoverInfo` arities, so they can no longer collide | redirect_and_dc_api_transcripts_are_distinct |
| VP-0244 | Format / mdoc / Invocation via the DC API (L2994) | The first element of `OpenID4VPDCAPIHandover` MUST be the string `OpenID4VPDCAPIHandover` | verifier | `conforming` | `OpenID4VPDCAPIHandover`'s first element is the DC-API-specific literal, pinned against the spec's published vector | dc_api_session_transcript_matches_openid4vp_vector |
| VP-0245 | Format / mdoc / Invocation via the DC API (L2995) | The second element MUST be a Byte String containing the SHA-256 hash of the CBOR-encoded `OpenID4VPDCAPIHandoverInfo` | verifier | `conforming` | Same hashing rule as VP-0234 applied to `OpenID4VPDCAPIHandoverInfo`, pinned against the spec's published vector | dc_api_session_transcript_matches_openid4vp_vector |
| VP-0246 | Format / mdoc / Invocation via the DC API (L2997) | The first element of `OpenID4VPDCAPIHandoverInfo` MUST be the string representing the Origin of the request | verifier | `conforming` | `SessionTranscriptParams::DcApi.origin` is the first element. Because the Origin is inside the hash the verifier cannot compare it, so it builds one candidate transcript per configured `verifier.dc_api_expected_origins` entry (falling back to a `public_base_url`-derived origin when unset) and lets the Device Signature select | dc_api_mdoc_accepts_a_later_configured_origin |
| VP-0247 | Format / mdoc / Invocation via the DC API (L2997) | The Origin element MUST NOT be prefixed with `origin:` | verifier | `conforming` | The bare origin is passed through unmodified; the `origin:` prefix is applied only to the KB-JWT `expected_audiences`, a separate code path. The published DC API vector encodes `https://example.com` unprefixed and is matched byte-for-byte | dc_api_session_transcript_matches_openid4vp_vector |
| VP-0248 | Format / mdoc / Invocation via the DC API (L2998) | The second element MUST be the value of the `nonce` request parameter | verifier | `conforming` | `SessionTranscriptParams::DcApi.nonce` is the second element, sourced from `tx.nonce` | dc_api_session_transcript_matches_openid4vp_vector |
| VP-0249 | Format / mdoc / Invocation via the DC API (L2999) | For Response Mode `dc_api.jwt` the third element MUST be the JWK SHA-256 Thumbprint of the Verifier's encryption public key | verifier | `conforming` | `tx.response_mode == "dc_api.jwt"` selects `Some(thumbprint_bytes(&tx.ephem_public_jwk))`. An unrecognised Response Mode is a typed error rather than a silent `None` | dc_api_response_mode_selects_null_or_thumbprint |
| VP-0250 | Format / mdoc / Invocation via the DC API (L2999) | For Response Mode `dc_api` the third element MUST be `null` | verifier | `conforming` | `tx.response_mode == "dc_api"` selects `None`, encoded as CBOR `null` in the third position rather than omitting the element | dc_api_response_mode_selects_null_or_thumbprint |
| VP-0251 | Format / IETF SD-JWT VC (L3083) | If `require_cryptographic_holder_binding` is `true` the Wallet MUST return an SD-JWT with a Key Binding JWT as the Verifiable Presentation | wallet | `out-of-scope` | Obligation falls on the Wallet or a third party, not on foundry's issuer/verifier surface |  |
| VP-0252 | Format / IETF SD-JWT VC / Transaction Data (L3135) | Wallets MUST reject requests with transaction data types whose `require_cryptographic_holder_binding` is `false` | wallet | `out-of-scope` | Obligation falls on the Wallet or a third party, not on foundry's issuer/verifier surface |  |
| VP-0253 | Format / IETF SD-JWT VC / Transaction Data (L3144) | One of the `transaction_data_hashes_alg` values MUST be used to calculate the hashes in `transaction_data_hashes` | wallet | `out-of-scope` | Obligation falls on the Wallet or a third party, not on foundry's issuer/verifier surface |  |
| VP-0254 | Format / IETF SD-JWT VC / Transaction Data (L3144) | A `transaction_data_hashes_alg` identifier MUST be a value from the IANA Named Information Hash Algorithm registry or defined by another specification or profile | verifier | `not-implemented` | `config.verifier.transaction_data_hashes_alg` (`Vec<String>`, foundry-core/src/config/model.rs) is never validated against the IANA registry, and more fundamentally is never read by any code that would use it -- see GAP-VP-04: the whole `transaction_data_hashes` response-side mechanism this field exists to configure is unimplemented |  |
| VP-0255 | Format / IETF SD-JWT VC / Transaction Data (L3144) | If `transaction_data_hashes_alg` is absent the default value `sha-256` MUST be used | wallet | `out-of-scope` | Obligation falls on the Wallet or a third party, not on foundry's issuer/verifier surface |  |
| VP-0256 | Format / IETF SD-JWT VC / Transaction Data (L3144) | Implementations MUST support the `sha-256` hash algorithm | verifier | `not-implemented` | Same root cause as VP-0254: no code ever computes or checks a `transaction_data_hashes` value against any algorithm, sha-256 included, on the verification side (see GAP-VP-04) |  |
| VP-0257 | Format / IETF SD-JWT VC / Transaction Data (L3145) | If `transaction_data_hashes_alg` was specified in the request the hash function MUST be one of its values | wallet | `out-of-scope` | Obligation falls on the Wallet or a third party, not on foundry's issuer/verifier surface |  |
| VP-0258 | Format / IETF SD-JWT VC / Transaction Data (L3145) | If `transaction_data_hashes_alg` was not specified the hash function MUST be `sha-256` | wallet | `out-of-scope` | Obligation falls on the Wallet or a third party, not on foundry's issuer/verifier surface |  |
| VP-0259 | Format / IETF SD-JWT VC / Transaction Data (L3145) | `transaction_data_hashes_alg` is REQUIRED in the Key Binding JWT when it was present in the request | wallet | `out-of-scope` | Obligation falls on the Wallet or a third party, not on foundry's issuer/verifier surface |  |
| VP-0260 | Format / IETF SD-JWT VC / Metadata (L3149) | `vp_formats_supported` MUST carry the SD-JWT VC Credential Format Identifier as a key whose value MUST be an object with the defined name/value pairs | verifier | `conforming` | `vp_formats_supported()` (request.rs) always includes `"dc+sd-jwt": {"sd-jwt_alg_values": ["ES256"], "kb-jwt_alg_values": ["ES256"]}` in every transport's client_metadata | test_client_metadata_advertises_vp_formats_supported |
| VP-0261 | Format / IETF SD-JWT VC / `meta` (L3163) | `vct_values` is REQUIRED and MUST be a non-empty array of strings | verifier | `gap` | `credential_query_satisfied` (dcql.rs) only checks `vct_values` against the presented `vct` *if present* in `cq.meta()` -- a dc+sd-jwt Credential Query with no `vct_values` matches any vct at all. Same GAP-VP-03 root cause, extended to this format-specific field | vp_0093_0094_0096_0097_dcql_structural_constraints_not_validated |
| VP-0262 | Format / IETF SD-JWT VC / `meta` (L3164) | All elements of `vct_values` MUST be valid type identifiers as defined in IETF SD-JWT VC | verifier | `gap` | Same finding as VP-0261: when present, each `vct_values` element is compared verbatim against the presented `vct` string with no validation of its own format validity. Same GAP-VP-03 root cause | vp_0093_0094_0096_0097_dcql_structural_constraints_not_validated |
| VP-0263 | Format / IETF SD-JWT VC / Presentation Response (L3178) | The Key Binding JWT `nonce` claim MUST be the value of `nonce` from the Authorization Request | verifier | `conforming` | `verify_sd_jwt_vc` is always called with `&tx.nonce` as `expected_nonce`, and a mismatch is rejected | test_verify_vp_response_kb_nonce_mismatch |
| VP-0264 | Format / IETF SD-JWT VC / Presentation Response (L3179) | The Key Binding JWT `aud` claim MUST be the value of the Client Identifier | verifier | `conforming` | For the `request_uri`/redirect transport, `do_verify_vp_response` always computes `expected_audience` as the `x509_san_dns` Client Identifier | test_verify_vp_response_sd_jwt_vc |
| VP-0265 | Format / IETF SD-JWT VC / Presentation Response (L3179) | Over the DC API the `aud` claim MUST instead be the Origin prefixed with `origin:` | verifier | `conforming` | `do_verify_vp_response` (verify.rs) now branches on `tx.transport == "dc_api"`: it computes an Origin-prefixed `expected_audiences` list (`verifier.dc_api_expected_origins`, each entry formatted as `origin:<origin>`, falling back to a single `public_base_url`-derived origin when unconfigured) and `verify_sd_jwt_vc`/`verify_kb_jwt` (foundry-sd-jwt-vc) match the KB-JWT `aud` against that list instead of the `x509_san_dns` Client Identifier | gap_vp_07_dc_api_transport_never_accepts_origin_prefixed_kb_jwt_audience |
| VP-0266 | Format / IETF SD-JWT VC / Format (L3197) | SD-JWT VCLD Credentials MUST use the registered claims `vct`, `exp`, `nbf`, `iss` and `status` instead of any W3C VCDM counterpart properties | verifier | `conforming` | `build_sd_jwt_vc` (foundry-sd-jwt-vc/src/builder.rs) only ever emits `iss`/`iat`/`exp`/`vct`/`status` -- no W3C VCDM counterpart claim (e.g. `validFrom`/`validUntil`) is used anywhere in this workspace; `nbf` (an optional not-before claim this clause does not itself mandate the presence of) is simply never populated, never expressed via a wrong-named substitute | test_verify_vp_response_sd_jwt_vc |

## Clause Inventory — HAIP

| ID | § | Requirement | Applies to | Verdict | Evidence | Test |
|---|---|---|---|---|---|---|
| HAIP-0001 | Scope (L97) | Implementations MUST comply with all requirements of every flow they choose to implement and with the non-flow-specific sections | other | `not-unit-testable` | Top-level umbrella requirement restating the document's entire body; it has no single executable assertion of its own -- conformance to it is the sum of every individually-adjudicated HAIP row in this report |  |
| HAIP-0002 | Scope (L99) | For each flow at least one of the Credential profiles IETF SD-JWT VC or ISO mdoc MUST be supported | other | `conforming` | foundry supports both `dc+sd-jwt` and `mso_mdoc` end to end on the issuer side (`issues_sd_jwt_vc_credential_successfully`, `vci_0071_mdoc_credential_string_is_base64url_encoded`) and the verifier side (`test_verify_vp_response_sd_jwt_vc`, `test_verify_vp_response_mdoc_presentation`), satisfying "at least one" for every flow with margin to spare | issues_sd_jwt_vc_credential_successfully, vci_0071_mdoc_credential_string_is_base64url_encoded, test_verify_vp_response_sd_jwt_vc, test_verify_vp_response_mdoc_presentation |
| HAIP-0003 | OpenID4VCI (L157) | MUST support the authorization code flow | issuer | `conforming` | Same evidence as HAIP-0022: the full offer to `/authorize` to `/token` authorization code flow succeeds end to end, and it is the only issuance flow foundry implements | haip_0022_authorization_code_grant_type_is_supported_end_to_end |
| HAIP-0004 | OpenID4VCI (L158) | MUST support at least one of the Credential Format Profiles IETF SD-JWT VC or ISO mdoc | issuer | `conforming` | Same evidence as HAIP-0002's issuer half: `handle_credential_request` successfully issues both `dc+sd-jwt` and `mso_mdoc` credentials | issues_sd_jwt_vc_credential_successfully, vci_0071_mdoc_credential_string_is_base64url_encoded |
| HAIP-0005 | OpenID4VCI (L159) | MUST comply with the provisions of FAPI 2.0 Security Profile applicable to this specification | issuer | `not-unit-testable` | Broad umbrella requirement encompassing many operational practices; its individually-testable sub-requirements are tracked separately (PKCE S256 = HAIP-0006, PAR = HAIP-0007, RFC9207 `iss` = HAIP-0008, DPoP = HAIP-0009) — the umbrella itself has no single executable assertion |  |
| HAIP-0006 | OpenID4VCI (L159) | MUST use PKCE with `S256` as the code challenge method | issuer | `conforming` | `handle_authorize_request` (authorize.rs) rejects any `code_challenge_method` other than the literal string `S256`, and `code_verifier_matches` (token.rs) only ever computes the S256 comparison | wrong_code_challenge_method_is_an_error_redirect, haip_0022_authorization_code_grant_type_is_supported_end_to_end |
| HAIP-0007 | OpenID4VCI (L159) | MUST use Pushed Authorization Requests where applicable | issuer | `ambiguous` | foundry has no `/par` (Pushed Authorization Request) endpoint anywhere in `crates/foundry/src/server.rs`; see Unresolved Ambiguities for the two competing readings of "where applicable" |  |
| HAIP-0008 | OpenID4VCI (L159) | MUST return the `iss` value in the Authorization response per RFC9207 | issuer | `conforming` | `AuthorizeOutcome::Success` and `AuthorizeOutcome::ErrorRedirect` (authorize.rs) now both carry `iss`, populated from an explicit `issuer_identifier` parameter threaded through `handle_authorize_request`; `append_query` (server.rs) appends it on both redirect paths. `AuthorizationServerMetadata` also now sets `authorization_response_iss_parameter_supported: true` per RFC 9207 §2.3. Closes both `iss` on error responses (§2 "including error responses") and the metadata flag, wider than this row's original description | haip_0008_authorization_response_includes_iss |
| HAIP-0009 | OpenID4VCI (L163) | MUST support DPoP per RFC9449 for sender-constrained access tokens | issuer | `gap` | GAP-HAIP-03 |  |
| HAIP-0010 | OpenID4VCI (L173) | If Issuer-initiated flows are supported they MUST use the Credential Offer | issuer | `conforming` | Narrows OpenID4VCI Section 4.1. `create_offer` is foundry's only issuer-initiated issuance entry point and always produces a well-formed `CredentialOffer` plus its deep-link | haip_0010_issuer_initiated_flow_always_produces_a_credential_offer |
| HAIP-0011 | OpenID4VCI (L177) | The Issuer MUST indicate whether batch issuance is supported by including or omitting `batch_credential_issuance` | issuer | `conforming` | foundry always omits `batch_credential_issuance` (see VCI-0140, `not-implemented`), which is itself a fully valid indication under the letter of this clause — VCI defines "supported" by the object's presence, not by some independent ground truth. `handle_credential_request` (credential.rs) does functionally accept more than one proof and issue one Credential per proof for the same Credential Dataset in a single request/response, demonstrating the omission is a coherent (if conservative) design choice rather than an oversight that breaks multi-proof requests | haip_0011_multiple_proofs_yield_one_credential_per_proof |
| HAIP-0012 | OpenID4VCI / Issuer Metadata (L183) | The Authorization Server MUST support metadata according to RFC8414 | issuer | `conforming` | `build_authorization_server_metadata` (metadata.rs) populates the RFC8414 fields (`issuer`, `authorization_endpoint`, `token_endpoint`, `grant_types_supported`, `response_types_supported`, `code_challenge_methods_supported`), served at the RFC8414 well-known location `/.well-known/oauth-authorization-server` (server.rs) | builds_authorization_server_metadata |
| HAIP-0013 | OpenID4VCI / Issuer Metadata (L185) | The Credential Issuer MUST support metadata retrieval | issuer | `conforming` | Narrows OpenID4VCI Section 12.2.2. `build_issuer_metadata` (metadata.rs) is served at `/.well-known/openid-credential-issuer` (server.rs) | builds_issuer_metadata_from_credential_types |
| HAIP-0014 | OpenID4VCI / Issuer Metadata (L186) | The Credential Issuer metadata MUST include a scope for every Credential Configuration it supports | issuer | `gap` | Same root cause as HAIP-0023/HAIP-0027/HAIP-0028: `CredentialConfigurationSupported` (metadata.rs) has no `scope` field at all — GAP-HAIP-01 | haip_0023_credential_configuration_metadata_carries_a_scope_value |
| HAIP-0015 | OpenID4VCI / Issuer Metadata (L189) | Where Ecosystem policy requires issuer authentication stronger than TLS, signed Credential Issuer Metadata MUST be supported by both Wallet and Issuer | issuer | `not-implemented` | Narrows OpenID4VCI Section 11.2.3. Signed metadata is not implemented anywhere in `metadata.rs` (same evidence as VCI-0121); this is conditional on an Ecosystem policy that foundry's configuration model has no representation for |  |
| HAIP-0016 | OpenID4VCI / Issuer Metadata (L189) | Key resolution to validate signed Issuer Metadata MUST be supported using the `x5c` JOSE header parameter | issuer | `not-implemented` | Same evidence as HAIP-0015: signed metadata is not implemented, so no key-resolution mechanism for it exists |  |
| HAIP-0017 | OpenID4VCI / Issuer Metadata (L190) | The X.509 certificate of the trust anchor MUST NOT be included in the `x5c` JOSE header of the signed request | issuer | `not-implemented` | Same evidence as HAIP-0015: signed metadata is not implemented, so there is no `x5c` header to violate or comply with this rule |  |
| HAIP-0018 | OpenID4VCI / Issuer Metadata (L190) | The X.509 certificate signing the request MUST NOT be self-signed | issuer | `not-implemented` | Same evidence as HAIP-0015: signed metadata is not implemented, so there is no signed-metadata certificate to be self-signed or not |  |
| HAIP-0019 | OpenID4VCI / Issuer Metadata (L192) | Wallets that render Credential Issuer metadata images MUST support both the SVG and PNG formats | wallet | `out-of-scope` | Obligation falls on the Wallet or a third party, not on foundry's issuer/verifier surface |  |
| HAIP-0020 | OpenID4VCI / Issuer Metadata (L192) | Such Wallets MUST support images conveyed through both data URIs and HTTPS URLs | wallet | `out-of-scope` | Obligation falls on the Wallet or a third party, not on foundry's issuer/verifier surface |  |
| HAIP-0021 | OpenID4VCI / Issuer Metadata (L194) | If the Issuer supports Credential Configurations requiring key binding the `nonce_endpoint` MUST be present in the Credential Issuer Metadata | issuer | `conforming` | Same evidence as VCI-0044: `nonce_endpoint` is always present, unconditionally | builds_issuer_metadata_from_credential_types |
| HAIP-0022 | OpenID4VCI / Credential Offer (L198) | The Grant Type `authorization_code` MUST be supported | issuer | `conforming` | Narrows OpenID4VCI Section 4.1.1. `AuthorizationServerMetadata.grant_types_supported` (metadata.rs) advertises `authorization_code`, and the full offer → `/authorize` → `/token` flow succeeds end to end | haip_0022_authorization_code_grant_type_is_supported_end_to_end |
| HAIP-0023 | OpenID4VCI / Credential Offer (L199) | For Grant Type `authorization_code` the Issuer MUST include a scope value so the Wallet can identify the desired Credential Type | issuer | `gap` | foundry identifies the Credential Type via `issuer_state`, not `scope` — GAP-HAIP-01 |  |
| HAIP-0024 | OpenID4VCI / Credential Offer (L199) | The Wallet MUST use that scope value in the `scope` Authorization parameter | wallet | `out-of-scope` | Obligation falls on the Wallet or a third party, not on foundry's issuer/verifier surface |  |
| HAIP-0025 | OpenID4VCI / Credential Offer (L204) | Both Issuer and Wallet MUST support the Credential Offer in same-device and cross-device flows | issuer | `conforming` | The offer is a self-contained deep link with no device-mode signal, so the identical value serves a QR-scanned cross-device flow and a directly-opened same-device flow alike | haip_0025_credential_offer_uri_format_is_transport_agnostic |
| HAIP-0026 | OpenID4VCI / Authorization Endpoint (L208) | Wallets MUST authenticate themselves at the PAR endpoint using the same rules as for client authentication at the token endpoint | wallet | `out-of-scope` | Obligation falls on the Wallet or a third party, not on foundry's issuer/verifier surface |  |
| HAIP-0027 | OpenID4VCI / Authorization Endpoint (L209) | The `scope` parameter MUST be used to communicate the Credential Types to be issued | issuer | `gap` | Restates HAIP-0023's mandate — GAP-HAIP-01 |  |
| HAIP-0028 | OpenID4VCI / Authorization Endpoint (L209) | The scope value MUST map to a specific Credential Type | issuer | `gap` | Same underlying absence as HAIP-0027 — GAP-HAIP-01 |  |
| HAIP-0029 | OpenID4VCI / Wallet Attestation (L219) | Wallets MUST use an OAuth2 client authentication mechanism at OAuth2 endpoints that support client authentication | wallet | `out-of-scope` | Obligation falls on the Wallet or a third party, not on foundry's issuer/verifier surface |  |
| HAIP-0030 | OpenID4VCI / Wallet Attestation (L219) | Issuers MUST require an OAuth2 client authentication mechanism at OAuth2 endpoints that support client authentication | issuer | `not-unit-testable` | `DefaultAttestationVerifier` correctly enforces presence when `wallet_attestation.mode = Required` (`required_mode_still_rejects_absence`); which mode an operator sets for a given deployment is a configuration/deployment decision, not a unit-testable code property. Whether what is required is itself *valid* is a separate question — see HAIP-0031, now `conforming` |  |
| HAIP-0031 | OpenID4VCI / Wallet Attestation (L225) | The public key certificate, and optionally a trust chain excluding the trust anchor, validating the Wallet Attestation signature MUST be included in the `x5c` JOSE header of the Client Attestation JWT | issuer | `conforming` | `DefaultAttestationVerifier::verify_wallet_attestation` (attestation.rs) now cryptographically validates the attestation JWT via `validate_wallet_attestation_jwt`: a missing or empty `x5c` header is rejected, the leaf certificate's public key must verify the JWS signature, and the chain is checked against `issuer.wallet_attestation.trusted_anchors` | rejects_missing_x5c |
| HAIP-0032 | OpenID4VCI / Wallet Attestation (L226) | Wallet Attestations MUST NOT be reused across different Issuers | wallet | `out-of-scope` | Obligation falls on the Wallet or a third party, not on foundry's issuer/verifier surface |  |
| HAIP-0033 | OpenID4VCI / Wallet Attestation (L226) | Wallet Attestations MUST NOT introduce a unique identifier specific to a single Wallet instance | wallet | `out-of-scope` | Obligation falls on the Wallet or a third party, not on foundry's issuer/verifier surface |  |
| HAIP-0034 | OpenID4VCI / Wallet Attestation (L226) | The Wallet Attestation subject claim MUST be a value shared by all Wallet instances of that wallet implementation | wallet | `out-of-scope` | Obligation falls on the Wallet or a third party, not on foundry's issuer/verifier surface |  |
| HAIP-0035 | OpenID4VCI / Wallet Attestation (L227) | Where applicable the `client_id` in the PAR request MUST be the `sub` value of the client attestation JWT | issuer | `not-implemented` | foundry has no PAR endpoint, so no "PAR request" exists for this constraint to apply to |  |
| HAIP-0036 | OpenID4VCI / Wallet Attestation (L228) | Wallets MUST perform client authentication with the Wallet Attestation at OAuth2 endpoints that support client authentication | wallet | `out-of-scope` | Obligation falls on the Wallet or a third party, not on foundry's issuer/verifier surface |  |
| HAIP-0037 | OpenID4VCI / Key Attestation (L234) | Wallets MUST support key attestations | wallet | `out-of-scope` | Obligation falls on the Wallet or a third party, not on foundry's issuer/verifier surface |  |
| HAIP-0038 | OpenID4VCI / Key Attestation (L241) | The public key validating the key attestation signature MUST be included in the `x5c` JOSE header of the key attestation | issuer | `conforming` | `verify_key_attestation_jwt` (attestation.rs) validates the key attestation's signature against the public key carried in its `x5c` JOSE header | verifies_valid_key_attestation_and_returns_attested_keys |
| HAIP-0039 | OpenID4VCI / Key Attestation (L242) | The X.509 certificate of the trust anchor MUST NOT be included in the `x5c` JOSE header of the key attestation | issuer | `ambiguous` | foundry's `validate_chain` (trust/mod.rs) does not check whether the trust anchor's own certificate is redundantly included among the intermediates; see Unresolved Ambiguities |  |
| HAIP-0040 | OpenID4VCI / Key Attestation (L243) | The X.509 certificate signing the key attestation MUST NOT be self-signed | issuer | `conforming` | `validate_chain` (foundry-core trust/mod.rs) explicitly rejects a self-signed leaf via `is_self_signed`, returning `TrustError::SelfSignedLeaf` | self_signed_leaf_is_rejected |
| HAIP-0041 | OpenID4VP (L254) | The Wallet and Verifier MUST support at least one of the Credential Format Profiles IETF SD-JWT VC or ISO mdoc | verifier | `conforming` | Same evidence as HAIP-0002's verifier half: `do_verify_vp_response` successfully verifies both `dc+sd-jwt` and `mso_mdoc` presentations | test_verify_vp_response_sd_jwt_vc, test_verify_vp_response_mdoc_presentation |
| HAIP-0042 | OpenID4VP (L255) | The Response Type MUST be `vp_token` | verifier | `conforming` | `RESPONSE_TYPE_VP_TOKEN` (request.rs) is the literal constant `"vp_token"`, unconditionally written into every Authorization Request's `response_type`, whether via `request_uri` or `dc_api` transport | test_create_verification_request_dcql |
| HAIP-0043 | OpenID4VP (L256) | For signed requests the Verifier MUST use the Client Identifier Prefix `x509_hash` | verifier | `conforming` | Closed 2026-08-03 (GAP-HAIP-05): `build_signed_request_object` (request.rs) now emits `client_id: "x509_hash:<base64url(SHA-256(DER leaf))>"` for every signed request via `foundry_core::trust::x509_hash_client_id_value`; `do_verify_vp_response` (verify.rs) derives the expected KB-JWT audience from the same helper against the same leaf, so the two sides cannot drift apart. `x5c` is now required for signed requests (absent `x5c` is a typed `VerificationError::Crypto`) | gap_haip_05_signed_request_object_never_uses_x509_hash_prefix, client_id_is_the_x509_hash_of_the_configured_leaf_certificate |
| HAIP-0044 | OpenID4VP (L256) | For signed requests the Wallet MUST accept the Client Identifier Prefix `x509_hash` | wallet | `out-of-scope` | Obligation falls on the Wallet or a third party, not on foundry's issuer/verifier surface |  |
| HAIP-0045 | OpenID4VP (L256) | The X.509 certificate of the trust anchor MUST NOT be included in the `x5c` JOSE header of the signed request | verifier | `conforming` | `build_signed_request_object` calls `foundry_core::trust::build_x5c(&[pem_bytes])` with only the leaf certificate PEM -- never the CA that issued it -- so the header's `x5c` array always has exactly one entry, whose DER never matches the trust anchor's | haip_0045_signed_request_x5c_excludes_the_trust_anchor |
| HAIP-0046 | OpenID4VP (L256) | The X.509 certificate signing the request MUST NOT be self-signed | verifier | `not-implemented` | Same root cause as HAIP-0045: no `x509_hash`-identified signed request exists |  |
| HAIP-0047 | OpenID4VP (L257) | The DCQL query and response MUST be used | verifier | `conforming` | Narrows OpenID4VP Section 6. Every Authorization Request this verifier builds carries a `dcql_query`, and `verify_vp_response` always evaluates the response against it via `check_dcql_match` -- DCQL is the only query/response mechanism this codebase has (Task 13) | test_create_verification_request_dcql, test_verify_vp_response_dcql_vct_mismatch_is_not_verified |
| HAIP-0048 | OpenID4VP (L258) | Response encryption MUST be performed as specified for encrypted responses | verifier | `conforming` | Narrows OpenID4VP Section 8.3. `response_mode` is always `direct_post.jwt` or `dc_api.jwt` (HAIP-0056/HAIP-0065), and every response this verifier accepts is a JWE decrypted via `do_verify_vp_response`; the individually testable sub-requirements are HAIP-0049/0050/0052/0053 and VP-0139/0140 | test_verify_vp_response_sd_jwt_vc |
| HAIP-0049 | OpenID4VP (L258) | The JWE `alg` value `ECDH-ES` with key agreement on the `P-256` curve MUST be supported | verifier | `conforming` | `create_verification_request` (request.rs) always generates the ephemeral response-encryption key via `EcKeyPair::generate(EcCurve::P256)`, and decryption is always attempted via `josekit::jwe::ECDH_ES.decrypter_from_jwk` | haip_0049_0050_0053_ecdh_es_p256_and_a256gcm_supported |
| HAIP-0050 | OpenID4VP (L258) | The JWE `enc` values `A128GCM` and `A256GCM` MUST be supported by Verifiers | verifier | `conforming` | `do_verify_vp_response`'s decrypter is generic over the JWE `enc` header (josekit resolves content decryption from the header itself); a response encrypted with `A256GCM` decrypts and verifies exactly like the `A128GCM` default | haip_0049_0050_0053_ecdh_es_p256_and_a256gcm_supported |
| HAIP-0051 | OpenID4VP (L258) | Wallets MUST support `A128GCM` or `A256GCM`, or both | wallet | `out-of-scope` | Obligation falls on the Wallet or a third party, not on foundry's issuer/verifier surface |  |
| HAIP-0052 | OpenID4VP (L258) | Verifiers MUST list both `A128GCM` and `A256GCM` in `encrypted_response_enc_values_supported` in their client metadata | verifier | `gap` | See GAP-VP-05 | haip_0052_encrypted_response_enc_values_supported_lists_only_one_value |
| HAIP-0053 | OpenID4VP (L259) | Verifiers MUST supply ephemeral encryption public keys specific to each Authorization Request via client metadata | verifier | `conforming` | Narrows OpenID4VP Section 8.3. `create_verification_request` generates a fresh `EcKeyPair` per call, so two requests never share an ephemeral encryption key | haip_0049_0050_0053_ecdh_es_p256_and_a256gcm_supported |
| HAIP-0054 | OpenID4VP (L260) | The Authority Key Identifier based Trusted Authorities Query MUST be supported | verifier | `not-implemented` | Narrows OpenID4VP Section 6.1.1.1. Same evidence as VP-0098/VP-0101 (Task 13): `trusted_authorities` is not modelled anywhere in `dcql_model.rs`, so the `aki` type this clause requires has no matching logic at all |  |
| HAIP-0055 | OpenID4VP / via Redirects (L274) | Signed Authorization Requests MUST be used, using JAR with the `request_uri` parameter | verifier | `conforming` | The `request_uri` transport always builds the request via `create_verification_request` and serves it as a signed JWS via `build_signed_request_object`, which (since GAP-HAIP-05) signs with the `x509_hash` Client Identifier Prefix HAIP-0043 requires | test_build_signed_request_object_and_verify_jws |
| HAIP-0056 | OpenID4VP / via Redirects (L275) | Response encryption MUST be used via response mode `direct_post.jwt` | verifier | `conforming` | Narrows OpenID4VP Section 8.3. `response_mode` (request.rs) is hardcoded to `"direct_post.jwt"` for every non-`dc_api` transport | test_create_verification_request_dcql |
| HAIP-0057 | OpenID4VP / via Redirects (L275) | The security considerations for encrypted responses MUST be applied | verifier | `not-unit-testable` | Narrows OpenID4VP Section 14.3. Broad umbrella requirement; the individually testable JWE-decryption mechanics live in `verify.rs` and are adjudicated under the Encrypted Responses clauses (a later task in this audit), not under Authorization Request construction |  |
| HAIP-0058 | OpenID4VP / via Redirects (L276) | Verifiers and Wallets MUST support the same-device flow | verifier | `conforming` | `create_verification_request` (request.rs) always produces an `openid4vp://` deep link the Wallet can open on the same device that renders the Verifier's page | test_create_verification_request_dcql |
| HAIP-0059 | OpenID4VP / via Redirects (L277) | For the same-device flow Verifiers MUST include `redirect_uri` in the HTTP response to the Wallet's POST to the `response_uri` | verifier | `not-implemented` | Narrows OpenID4VP Section 8.2. Neither `CreateVerificationRequest` nor the `/vp/response/:id` handler (`crates/foundry/src/server.rs`) has any `redirect_uri` field at all — the response never echoes a redirect target back to the Wallet |  |
| HAIP-0060 | OpenID4VP / via Redirects (L277) | Wallets MUST follow the redirect to `redirect_uri` | wallet | `out-of-scope` | Obligation falls on the Wallet or a third party, not on foundry's issuer/verifier surface |  |
| HAIP-0061 | OpenID4VP / via Redirects (L278) | Verifiers MUST reject presentations if the Wallet does not follow the redirect back or the redirect arrives in a different user session than the one the request was initiated in | verifier | `not-implemented` | Same root cause as HAIP-0059: there is no `redirect_uri`/session-tracking mechanism at all to enforce this against |  |
| HAIP-0062 | OpenID4VP / via the W3C DC API (L285) | The Wallet MUST support Wallet Invocation via the W3C Digital Credentials API or an equivalent platform API | wallet | `out-of-scope` | Obligation falls on the Wallet or a third party, not on foundry's issuer/verifier surface |  |
| HAIP-0063 | OpenID4VP / via the W3C DC API (L285) | The Verifier MUST use Wallet Invocation via the W3C Digital Credentials API or an equivalent platform API | verifier | `conforming` | The `dc_api` transport (`create_verification_request`, request.rs) builds a `dc_api_request` object designed to be passed directly to the DC API | test_create_verification_request_dc_api |
| HAIP-0064 | OpenID4VP / via the W3C DC API (L286) | The Wallet MUST support the Response Mode `dc_api.jwt` | wallet | `out-of-scope` | Obligation falls on the Wallet or a third party, not on foundry's issuer/verifier surface |  |
| HAIP-0065 | OpenID4VP / via the W3C DC API (L286) | The Verifier MUST use the Response Mode `dc_api.jwt` | verifier | `conforming` | `response_mode` (request.rs) is hardcoded to `"dc_api.jwt"` whenever `transport == "dc_api"` | test_create_verification_request_dc_api |
| HAIP-0066 | OpenID4VP / via the W3C DC API (L287) | The Verifier and Wallet MUST follow the OpenID4VP appendix defining OpenID4VP over the W3C Digital Credentials API | verifier | `not-unit-testable` | Broad umbrella requirement referencing the entire DC API appendix; individually testable sub-requirements are tracked separately (HAIP-0063, HAIP-0065, HAIP-0068) |  |
| HAIP-0067 | OpenID4VP / via the W3C DC API (L288) | The Wallet MUST support unsigned, signed and multi-signed requests | wallet | `out-of-scope` | Obligation falls on the Wallet or a third party, not on foundry's issuer/verifier surface |  |
| HAIP-0068 | OpenID4VP / via the W3C DC API (L288) | The Verifier MUST support at least one of unsigned, signed or multi-signed requests | verifier | `conforming` | The `dc_api` transport (request.rs) always builds `dc_api_request` as a plain unsigned JSON object — satisfying "at least one of" via the unsigned option | test_create_verification_request_dc_api |
| HAIP-0069 | OpenID4VP / mdoc (L298) | The Credential Format identifier MUST be `mso_mdoc` | verifier | `conforming` | `CredentialFormat::MsoMdoc` (dcql_model.rs) serializes/deserializes as the exact literal string `"mso_mdoc"`, the only mdoc format identifier this codebase recognizes | parses_spec_mdoc_example |
| HAIP-0070 | OpenID4VP / mdoc (L299) | When multiple ISO mdocs are returned each MUST be returned in a separate `DeviceResponse` matching its respective DCQL query | verifier | `not-implemented` | Same root cause as VP-0103: foundry presents exactly one credential per `vp_token` by design (`dcql.rs`'s own doc comment, Task 13); the multi-mdoc scenario this clause governs cannot arise |  |
| HAIP-0071 | OpenID4VP / mdoc (L300) | A Credential Issuer including an MSO revocation mechanism MUST use one of the mechanisms defined in ISO/IEC 18013-5 | issuer | `out-of-scope` | ISO/IEC 18013-5's own MSO revocation mechanisms are mdoc format internals; the defining spec is not vendored and not vendorable (see Audit Boundary) |  |
| HAIP-0072 | OpenID4VP / IETF SD-JWT VC (L306) | The Credential Format identifier MUST be `dc+sd-jwt` | verifier | `conforming` | `CredentialFormat::DcSdJwt` (dcql_model.rs) serializes/deserializes as the exact literal string `"dc+sd-jwt"`, the only SD-JWT VC format identifier this codebase recognizes | parses_spec_multi_credential_example |
| HAIP-0073 | SD-JWT VC Profile (L323) | Compact serialization MUST be supported | issuer | `conforming` | `build_sd_jwt_vc` (foundry-sd-jwt-vc builder.rs) always produces the `<jwt>~<disclosure>~...~` compact serialization; there is no other SD-JWT VC output format anywhere in this codebase | issues_sd_jwt_vc_credential_successfully |
| HAIP-0074 | SD-JWT VC Profile (L324) | An Issuer limiting the validity period MUST use an `exp` claim, a `status` claim, or both | issuer | `conforming` | `build_sd_jwt_vc` unconditionally inserts an `exp` claim into every issued SD-JWT VC (`IssuerClaims.exp: i64` is a required, non-`Option` field), satisfying "an `exp` claim ... or both" on every issuance | issues_sd_jwt_vc_credential_successfully |
| HAIP-0075 | SD-JWT VC Profile (L325) | The `cnf` claim MUST conform to the definition given in IETF SD-JWT VC | issuer | `not-unit-testable` | Checking field-by-field conformance against the IETF SD-JWT VC `cnf` claim definition would require the defining spec, which is out of scope per the Audit Boundary (SD-JWT VC format internals). `build_sd_jwt_vc` always emits `cnf: {"jwk": <holder public key>}`, the RFC7800-shaped confirmation claim this codebase's own verifier (`verify_sd_jwt_vc`) requires and consumes -- the shape is internally consistent, but a spec-text-level conformance check is outside this audit's vendored-spec boundary |  |
| HAIP-0076 | SD-JWT VC Profile (L325) | Implementations MUST include the JSON Web Key in the `jwk` member when the Credential Configuration requires cryptographic holder binding | issuer | `conforming` | `handle_credential_request` (credential.rs) always derives `cnf_jwk` from the holder's proof-of-possession JWK for every issued credential -- cryptographic holder binding is unconditionally required by this codebase's issuance flow (no non-key-bound path exists), and `cnf.jwk` is always populated to match | issues_sd_jwt_vc_credential_successfully |
| HAIP-0077 | SD-JWT VC Profile (L326) | The `status` claim, if present, MUST contain `status_list` | issuer | `conforming` | `build_sd_jwt_vc` only ever emits `status: {"status_list": {"idx": ..., "uri": ...}}` when a status list index/URI pair is supplied; no other `status` claim shape exists in this codebase | valid_index_passes |
| HAIP-0078 | SD-JWT VC Profile (L327) | The public key validating the Status List Token signature MUST be included in the `x5c` JOSE header of the Token | issuer | `conforming` | `verify_status_list_token` (foundry-core status_list/mod.rs) requires a non-empty `x5c` header ("status list token x5c missing"/"empty x5c header" errors otherwise) and derives the verifying public key from its leaf certificate | verify_round_trips_and_status_at_matches_original |
| HAIP-0079 | SD-JWT VC Profile (L327) | The X.509 certificate of the trust anchor MUST NOT be included in the `x5c` JOSE header of the Status List Token | issuer | `ambiguous` | Same ambiguity as HAIP-0039: `verify_status_list_token` calls the same `validate_chain` (foundry-core trust/mod.rs), which does not check whether the trust anchor's own certificate is redundantly present among the supplied intermediates. See Unresolved Ambiguities |  |
| HAIP-0080 | SD-JWT VC Profile (L327) | The X.509 certificate signing the request MUST NOT be self-signed | issuer | `conforming` | `verify_status_list_token` calls the same `validate_chain` as HAIP-0040, which explicitly rejects a self-signed leaf via `is_self_signed`/`TrustError::SelfSignedLeaf` | self_signed_leaf_is_rejected |
| HAIP-0081 | SD-JWT VC Profile (L329) | Each Credential MUST have its own unique, unpredictable status list index even when multiple Credentials reference the same status list URI | issuer | `conforming` | `allocate_status_index` (status_index.rs) now deduplicates its CSPRNG draw against the storage key `"{list_id}:{idx}"` — the physical status list every credential type actually shares (`credential.rs`'s literal `".../1"` URI) — rather than `credential_type_id`, so allocation scope is identical to physical-list scope by construction | gap_haip_06_status_index_can_collide_across_credential_types_sharing_one_list |
| HAIP-0082 | SD-JWT VC Profile / Issuer key resolution (L341) | X.509 certificate-based key resolution to validate the issuer signature of an SD-JWT VC MUST be supported by all entities | other | `conforming` | `verify_sd_jwt_vc` always resolves the issuer's verifying key from the `x5c` JOSE header via `validate_chain` -- this is the only issuer key resolution mechanism this codebase has, so "supported" holds unconditionally | test_verify_vp_response_sd_jwt_vc |
| HAIP-0083 | SD-JWT VC Profile / Issuer key resolution (L341) | The SD-JWT VC MUST contain the credential issuer's signing certificate along with a trust chain in the `x5c` JOSE header parameter | issuer | `conforming` | `verify_sd_jwt_vc` requires a non-empty `x5c` header ("issuer x5c missing"/"empty x5c header" errors otherwise) and validates it as a chain via `validate_chain` | test_verify_vp_response_sd_jwt_vc |
| HAIP-0084 | SD-JWT VC Profile / Issuer key resolution (L341) | The X.509 certificate of the trust anchor MUST NOT be included in the `x5c` JOSE header of the SD-JWT VC | issuer | `ambiguous` | Same ambiguity as HAIP-0039/HAIP-0079: `verify_sd_jwt_vc` also calls `validate_chain`, which does not check for redundant trust-anchor presence among the intermediates. See Unresolved Ambiguities |  |
| HAIP-0085 | SD-JWT VC Profile / Issuer key resolution (L341) | The X.509 certificate signing the request MUST NOT be self-signed | issuer | `conforming` | Same evidence as HAIP-0040/HAIP-0080: `validate_chain` rejects a self-signed leaf regardless of caller | self_signed_leaf_is_rejected |
| HAIP-0086 | SD-JWT VC Profile / Cryptographic Holder Binding (L345) | If the credential has cryptographic holder binding a KB-JWT MUST always be present when presenting an SD-JWT VC | verifier | `conforming` | `verify_sd_jwt_vc` requires `cnf.jwk` unconditionally ("holder cnf.jwk missing" otherwise) -- every credential this codebase issues has cryptographic holder binding, per HAIP-0076 -- and then requires a KB-JWT unconditionally whenever `cnf.jwk` is present ("KB-JWT missing from presentation" otherwise) | test_verify_vp_response_sd_jwt_vc, test_verify_vp_response_kb_nonce_mismatch |
| HAIP-0087 | Requirements for Digital Signatures (L350) | Issuers, Verifiers and Wallets MUST at a minimum support ECDSA with P-256 and SHA-256 (`ES256`; COSE `-7` or `-9`) for the validations listed below | other | `not-unit-testable` | Umbrella baseline requirement; its individually-testable sub-requirements are HAIP-0088 through HAIP-0092, and HAIP-0095 for the SHA-256 half |  |
| HAIP-0088 | Requirements for Digital Signatures (L353) | Issuers MUST support `ES256` for validating Wallet Attestations including proof of possession | issuer | `conforming` | Closed 2026-08-01 (GAP-VCI-14): the attestation JWT half was already conforming (`validate_wallet_attestation_jwt` verifies ES256 signatures via `ES256.verifier_from_pem`); "including proof of possession" now also holds — `validate_client_attestation_pop_jwt` (attestation.rs) requires and verifies `alg == ES256` on the Client Attestation PoP JWT (ABCA §9 rule 4, narrowed by HAIP-0088), rejecting any other algorithm including a genuinely HS256-signed JWT | rejects_pop_with_hs256_alg_even_when_genuinely_hs256_signed |
| HAIP-0089 | Requirements for Digital Signatures (L354) | Issuers MUST support `ES256` for validating Key Attestations | issuer | `conforming` | `verify_key_attestation_jwt` (attestation.rs) hardcodes ES256 for key attestation signature verification | verifies_valid_key_attestation_and_returns_attested_keys |
| HAIP-0090 | Requirements for Digital Signatures (L355) | Issuers MUST support `ES256` for validating the `jwt` proof type | issuer | `conforming` | Same mechanism as VCI-0200 | vci_0200_0212_0226_haip_0090_proof_alg_must_be_es256 |
| HAIP-0091 | Requirements for Digital Signatures (L357) | Verifiers MUST support `ES256` for validating the signature of the Verifiable Presentation, such as the KB-JWT of an SD-JWT VC or the `deviceSignature` of an ISO mdoc | verifier | `conforming` | `curve_for_alg` in both `foundry-sd-jwt-vc`'s verifier (KB-JWT) and `foundry-mdoc`'s verifier (`deviceSignature`) recognizes `"ES256"` among the algorithms it accepts, and every existing presentation-verification test uses ES256 throughout | test_verify_vp_response_sd_jwt_vc, test_verify_vp_response_mdoc_presentation |
| HAIP-0092 | Requirements for Digital Signatures (L358) | Verifiers MUST support `ES256` for validating the status information of the Verifiable Credential or Wallet Attestation | verifier | `conforming` | `foundry-core`'s `verify_status_list_token` resolves the Status List Token's verifier via `curve_for_alg`, which recognizes only `"ES256"` (mapped to curve `P-256`) and rejects every other `alg` value; `foundry-verifier`'s `check_status` exercises this end to end with ES256-signed Status List Tokens | verify_round_trips_and_status_at_matches_original, valid_index_passes, revoked_index_fails |
| HAIP-0093 | Requirements for Digital Signatures (L360) | Wallets MUST support `ES256` for validating signed presentation requests | wallet | `out-of-scope` | Obligation falls on the Wallet or a third party, not on foundry's issuer/verifier surface |  |
| HAIP-0094 | Requirements for Digital Signatures (L361) | Wallets MUST support `ES256` for validating signed Issuer metadata | wallet | `out-of-scope` | Obligation falls on the Wallet or a third party, not on foundry's issuer/verifier surface |  |
| HAIP-0095 | Hash Algorithms (L369) | SHA-256 MUST be supported by all entities to generate and validate the digests in IETF SD-JWT VC and ISO mdoc | other | `conforming` | `foundry-sd-jwt-vc`'s builder/verifier hash every disclosure and `sd_hash` via `sha2::Sha256` unconditionally (no other digest algorithm is configurable), and `foundry-mdoc`'s verifier likewise hashes digests via `Sha256` -- SHA-256 is the only digest algorithm either format implementation ever uses | issues_sd_jwt_vc_credential_successfully, test_verify_vp_response_mdoc_presentation |
| HAIP-0096 | Privacy / Interoperable Key Attestations (L458) | A backend service transforming platform attestation data into the key attestation format MUST be designed considering the privacy of its users | wallet | `out-of-scope` | Obligation falls on the Wallet or a third party, not on foundry's issuer/verifier surface |  |

## Unresolved Ambiguities

| ID | Spec § | Reading A | Reading B | Why it matters |
|---|---|---|---|---|
| HAIP-0007 | OpenID4VCI (L159) | "PAR (where applicable)" is conditioned on FAPI2's confidential-client requirements, which foundry's architecture (offer-fixed claims, no wallet-supplied sensitive parameters beyond redirect_uri/PKCE/issuer_state, all sent over TLS) never triggers — PAR is simply not applicable here, so its absence is not a violation. | HAIP bundles PAR into the same MUST sentence as PKCE and RFC9207 `iss`, both of which are unconditional; FAPI2 for confidential clients typically mandates PAR without a comparable escape hatch, so "applicable" may mean "always, for any Issuer/AS acting as a confidential OAuth2 client" — making its total absence (no `/par` route exists) a gap. | Resolving this changes whether foundry needs a PAR endpoint at all, which is a architectural addition, not a bug fix — worth a deliberate decision rather than a guessed verdict. |
| HAIP-0039 | OpenID4VCI / Key Attestation (L242) | `validate_chain` (foundry-core trust/mod.rs) does not check whether the trust anchor's own certificate is redundantly included among the supplied intermediates; walking the DN chain still requires reaching a configured anchor's subject regardless, so admitting the anchor cert causes no exploitable weakness — the strict MUST NOT is unenforced but immaterial. | The MUST NOT is unconditional and protocol-hygiene motivated (parsing simplicity, unambiguous chain length), independent of whether the specific bypass is exploitable; a strict reading counts this as non-conformant regardless of impact. | Resolving this decides whether `validate_chain` needs an explicit check for anchor-cert presence in the intermediates array — a small but real code change, not merely a documentation update. |
| HAIP-0079 | SD-JWT VC Profile (L327) | Same underlying mechanism as HAIP-0039: `verify_status_list_token` calls the same `validate_chain`, which never checks whether the trust anchor's certificate is redundantly present among the Status List Token's `x5c` intermediates. | Same reading as HAIP-0039 -- the MUST NOT is unconditional regardless of whether the specific redundancy is exploitable. | Resolving HAIP-0039 (a single shared `validate_chain` fix) resolves this row and HAIP-0084 simultaneously, since all three call sites share one trust-chain validation function. |
| HAIP-0084 | SD-JWT VC Profile / Issuer key resolution (L341) | Same underlying mechanism as HAIP-0039: `verify_sd_jwt_vc` calls the same `validate_chain`, which never checks whether the trust anchor's certificate is redundantly present among the SD-JWT VC's `x5c` intermediates. | Same reading as HAIP-0039. | Same resolution path as HAIP-0079: one shared `validate_chain` fix closes all three rows at once. |
